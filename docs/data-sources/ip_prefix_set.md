---
page_title: "f5xc_ip_prefix_set Data Source - terraform-provider-f5xc"
subcategory: ""
description: |-
  Create ip_prefix_set creates a new object in the storage backend for metadata.namespace.
---

# f5xc_ip_prefix_set (Data Source)

Create ip_prefix_set creates a new object in the storage backend for metadata.namespace.

~> **Note** For more information about this data source, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Ip Prefix Set Data Source Example
# Retrieves information about an existing Ip Prefix Set

# Look up an existing Ip Prefix Set by name
data "f5xc_ip_prefix_set" "example" {
  name      = "my-ip-prefix-set"
  namespace = "system"
}

# Example: Use the data source in another resource
# output "ip_prefix_set_id" {
#   value = data.f5xc_ip_prefix_set.example.id
# }
```


<!-- schema generated by tfplugindocs -->
## Argument Reference

The following arguments are required:

`name` - (Required) Name of the IPPrefixSet (`String`).

`namespace` - (Required) Namespace where the IPPrefixSet exists (`String`).


### Attributes Reference

In addition to all arguments above, the following attributes are exported:

`annotations` - (Optional) Annotations applied to this resource (`Map`).

`description` - (Optional) Description of the IPPrefixSet (`String`).

`id` - (Optional) Unique identifier for the resource (`String`).

`labels` - (Optional) Labels applied to this resource (`Map`).

