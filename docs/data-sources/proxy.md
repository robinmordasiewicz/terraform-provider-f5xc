---
page_title: "f5xc_proxy Data Source - terraform-provider-f5xc"
subcategory: ""
description: |-
  Shape of the TCP loadbalancer create specification
---

# f5xc_proxy (Data Source)

Shape of the TCP loadbalancer create specification

~> **Note** For more information about this data source, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Proxy Data Source Example
# Retrieves information about an existing Proxy

# Look up an existing Proxy by name
data "f5xc_proxy" "example" {
  name      = "my-proxy"
  namespace = "system"
}

# Example: Use the data source in another resource
# output "proxy_id" {
#   value = data.f5xc_proxy.example.id
# }
```


<!-- schema generated by tfplugindocs -->
## Argument Reference

The following arguments are required:

`name` - (Required) Name of the Proxy (`String`).

`namespace` - (Required) Namespace where the Proxy exists (`String`).


### Attributes Reference

In addition to all arguments above, the following attributes are exported:

`annotations` - (Optional) Annotations applied to this resource (`Map`).

`description` - (Optional) Description of the Proxy (`String`).

`id` - (Optional) Unique identifier for the resource (`String`).

`labels` - (Optional) Labels applied to this resource (`Map`).

