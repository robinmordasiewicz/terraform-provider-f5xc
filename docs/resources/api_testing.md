---
page_title: "f5xc_api_testing Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages a APITesting resource in F5 Distributed Cloud.
---

# f5xc_api_testing (Resource)

Manages a APITesting resource in F5 Distributed Cloud.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Api Testing Resource Example
# Manages a APITesting resource in F5 Distributed Cloud.

# Basic Api Testing configuration
resource "f5xc_api_testing" "example" {
  name      = "example-api-testing"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
    # Testing Environments. Add and configure testing domains a...
    domains {
      # Configure domains settings
    }
    # Credentials. Add credentials for API testing to use in th...
    credentials {
      # Configure credentials settings
    }
    # Empty. This can be used for messages where no values are ...
    admin {
      # Configure admin settings
    }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the APITesting. Must be unique within the namespace.
- `namespace` (String) Namespace where the APITesting will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `custom_header_value` (String) Custom Header. Add x-f5-api-testing-identifier header value to prevent security flags on API testing traffic
- `domains` (Block List) Testing Environments. Add and configure testing domains and credentials (see [below for nested schema](#nestedblock--domains))
- `every_day` (Block, Optional) [OneOf: every_day, every_month, every_week] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--every_day))
- `every_month` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--every_month))
- `every_week` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--every_week))
- `labels` (Map of String) Labels to apply to this resource.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--domains"></a>
### Nested Schema for `domains`

Optional:

- `allow_destructive_methods` (Boolean) Run API tests for destructive methods (e.g., DELETE, PUT). Enable to allow API test to execute destructive methods. Be cautious as these can alter or delete data
- `credentials` (Block List) Credentials. Add credentials for API testing to use in the selected environment. (see [below for nested schema](#nestedblock--domains--credentials))
- `domain` (String) Domain. Add your testing environment domain. Be aware that running tests on a production domain can impact live applications, as API testing cannot distinguish between production and testing environments.

<a id="nestedblock--domains--credentials"></a>
### Nested Schema for `domains.credentials`

Optional:

- `admin` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--domains--credentials--admin))
- `api_key` (Block, Optional) Api Key. (see [below for nested schema](#nestedblock--domains--credentials--api_key))
- `basic_auth` (Block, Optional) Basic Authentication. (see [below for nested schema](#nestedblock--domains--credentials--basic_auth))
- `bearer_token` (Block, Optional) Bearer. (see [below for nested schema](#nestedblock--domains--credentials--bearer_token))
- `credential_name` (String) Name. Enter a unique name for the credentials used in API testing
- `login_endpoint` (Block, Optional) Login Endpoint. (see [below for nested schema](#nestedblock--domains--credentials--login_endpoint))
- `standard` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--domains--credentials--standard))

<a id="nestedblock--domains--credentials--admin"></a>
### Nested Schema for `domains.credentials.admin`


<a id="nestedblock--domains--credentials--api_key"></a>
### Nested Schema for `domains.credentials.api_key`

Optional:

- `key` (String) Key.
- `value` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--domains--credentials--api_key--value))

<a id="nestedblock--domains--credentials--api_key--value"></a>
### Nested Schema for `domains.credentials.api_key.value`



<a id="nestedblock--domains--credentials--basic_auth"></a>
### Nested Schema for `domains.credentials.basic_auth`

Optional:

- `password` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--domains--credentials--basic_auth--password))
- `user` (String) User.

<a id="nestedblock--domains--credentials--basic_auth--password"></a>
### Nested Schema for `domains.credentials.basic_auth.password`



<a id="nestedblock--domains--credentials--bearer_token"></a>
### Nested Schema for `domains.credentials.bearer_token`

Optional:

- `token` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--domains--credentials--bearer_token--token))

<a id="nestedblock--domains--credentials--bearer_token--token"></a>
### Nested Schema for `domains.credentials.bearer_token.token`



<a id="nestedblock--domains--credentials--login_endpoint"></a>
### Nested Schema for `domains.credentials.login_endpoint`

Optional:

- `json_payload` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--domains--credentials--login_endpoint--json_payload))
- `method` (String) HTTP Method. Specifies the HTTP method used to access a resource. Any HTTP Method. Possible values include `ANY`, `GET`, `HEAD`, `POST`, `PUT`, `DELETE`, `CONNECT`, `OPTIONS`, `TRACE`, `PATCH`, and others.
- `path` (String) Path.
- `token_response_key` (String) Token Response Key.

<a id="nestedblock--domains--credentials--login_endpoint--json_payload"></a>
### Nested Schema for `domains.credentials.login_endpoint.json_payload`



<a id="nestedblock--domains--credentials--standard"></a>
### Nested Schema for `domains.credentials.standard`




<a id="nestedblock--every_day"></a>
### Nested Schema for `every_day`


<a id="nestedblock--every_month"></a>
### Nested Schema for `every_month`


<a id="nestedblock--every_week"></a>
### Nested Schema for `every_week`


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_api_testing.example system/example
```
