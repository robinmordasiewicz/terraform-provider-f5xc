---
page_title: "f5xc_authentication Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages a Authentication resource in F5 Distributed Cloud.
---

# f5xc_authentication (Resource)

Manages a Authentication resource in F5 Distributed Cloud.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Authentication Resource Example
# Manages a Authentication resource in F5 Distributed Cloud.

# Basic Authentication configuration
resource "f5xc_authentication" "example" {
  name      = "example-authentication"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
    # Cookie Parameters. Specifies different cookie related con...
    cookie_params {
      # Configure cookie_params settings
    }
    # HMAC Key Pair. HMAC primary and secondary keys to be used...
    auth_hmac {
      # Configure auth_hmac settings
    }
    # Secret. SecretType is used in an object to indicate a sen...
    prim_key {
      # Configure prim_key settings
    }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the Authentication. Must be unique within the namespace.
- `namespace` (String) Namespace where the Authentication will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `cookie_params` (Block, Optional) Cookie Parameters. Specifies different cookie related config parameters for authentication (see [below for nested schema](#nestedblock--cookie_params))
- `labels` (Map of String) Labels to apply to this resource.
- `oidc_auth` (Block, Optional) OIDCAuthType. (see [below for nested schema](#nestedblock--oidc_auth))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--cookie_params"></a>
### Nested Schema for `cookie_params`

Optional:

- `auth_hmac` (Block, Optional) HMAC Key Pair. HMAC primary and secondary keys to be used for hashing the Cookie. Each key also have an associated expiry timestamp, beyond which key is invalid. (see [below for nested schema](#nestedblock--cookie_params--auth_hmac))
- `cookie_expiry` (Number) Cookie Expiry duration. specifies in seconds max duration of the allocated cookie. This maps to “Max-Age” attribute in the session cookie. This will act as an expiry duration on the client side after which client will not be setting the cookie as part of the request. Default cookie expiry is 3600 seconds
- `cookie_refresh_interval` (Number) Cookie Refresh Interval. Specifies in seconds refresh interval for session cookie. This is used to keep the active user active and reduce re-login. When an incoming cookie's session expiry is still valid, and time to expire falls behind this interval, re-issue a cookie with new expiry and with the same original session expiry. Default refresh interval is 3000 seconds
- `kms_key_hmac` (Block, Optional) KMS Key Reference. Reference to KMS Key Object (see [below for nested schema](#nestedblock--cookie_params--kms_key_hmac))
- `session_expiry` (Number) Session Expiry duration. specifies in seconds max lifetime of an authenticated session after which the user will be forced to login again. Default session expiry is 86400 seconds(24 hours).

<a id="nestedblock--cookie_params--auth_hmac"></a>
### Nested Schema for `cookie_params.auth_hmac`

Optional:

- `prim_key` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--cookie_params--auth_hmac--prim_key))
- `prim_key_expiry` (String) HMAC Primary Key Expiry. Primary HMAC Key Expiry time
- `sec_key` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--cookie_params--auth_hmac--sec_key))
- `sec_key_expiry` (String) HMAC Secondary Key Expiry. Secondary HMAC Key Expiry time

<a id="nestedblock--cookie_params--auth_hmac--prim_key"></a>
### Nested Schema for `cookie_params.auth_hmac.prim_key`

Optional:

- `blindfold_secret_info` (Block, Optional) Blindfold Secret. BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management (see [below for nested schema](#nestedblock--cookie_params--auth_hmac--prim_key--blindfold_secret_info))
- `clear_secret_info` (Block, Optional) In-Clear Secret. ClearSecretInfoType specifies information about the Secret that is not encrypted. (see [below for nested schema](#nestedblock--cookie_params--auth_hmac--prim_key--clear_secret_info))

<a id="nestedblock--cookie_params--auth_hmac--prim_key--blindfold_secret_info"></a>
### Nested Schema for `cookie_params.auth_hmac.prim_key.blindfold_secret_info`


<a id="nestedblock--cookie_params--auth_hmac--prim_key--clear_secret_info"></a>
### Nested Schema for `cookie_params.auth_hmac.prim_key.clear_secret_info`



<a id="nestedblock--cookie_params--auth_hmac--sec_key"></a>
### Nested Schema for `cookie_params.auth_hmac.sec_key`

Optional:

- `blindfold_secret_info` (Block, Optional) Blindfold Secret. BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management (see [below for nested schema](#nestedblock--cookie_params--auth_hmac--sec_key--blindfold_secret_info))
- `clear_secret_info` (Block, Optional) In-Clear Secret. ClearSecretInfoType specifies information about the Secret that is not encrypted. (see [below for nested schema](#nestedblock--cookie_params--auth_hmac--sec_key--clear_secret_info))

<a id="nestedblock--cookie_params--auth_hmac--sec_key--blindfold_secret_info"></a>
### Nested Schema for `cookie_params.auth_hmac.sec_key.blindfold_secret_info`


<a id="nestedblock--cookie_params--auth_hmac--sec_key--clear_secret_info"></a>
### Nested Schema for `cookie_params.auth_hmac.sec_key.clear_secret_info`




<a id="nestedblock--cookie_params--kms_key_hmac"></a>
### Nested Schema for `cookie_params.kms_key_hmac`



<a id="nestedblock--oidc_auth"></a>
### Nested Schema for `oidc_auth`

Optional:

- `client_secret` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--oidc_auth--client_secret))
- `oidc_auth_params` (Block, Optional) OIDCAuthParams. (see [below for nested schema](#nestedblock--oidc_auth--oidc_auth_params))
- `oidc_client_id` (String) OIDC Client ID. Client ID used while sending the Authorization Request to OIDC server
- `oidc_well_known_config_url` (String) Well-known Configuration URL. An OIDC well-known configuration URL that will be used to fetch authentication related endpoints

<a id="nestedblock--oidc_auth--client_secret"></a>
### Nested Schema for `oidc_auth.client_secret`

Optional:

- `blindfold_secret_info` (Block, Optional) Blindfold Secret. BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management (see [below for nested schema](#nestedblock--oidc_auth--client_secret--blindfold_secret_info))
- `clear_secret_info` (Block, Optional) In-Clear Secret. ClearSecretInfoType specifies information about the Secret that is not encrypted. (see [below for nested schema](#nestedblock--oidc_auth--client_secret--clear_secret_info))

<a id="nestedblock--oidc_auth--client_secret--blindfold_secret_info"></a>
### Nested Schema for `oidc_auth.client_secret.blindfold_secret_info`

Optional:

- `decryption_provider` (String) Decryption Provider. Name of the Secret Management Access object that contains information about the backend Secret Management service.
- `location` (String) Location. Location is the uri_ref. It could be in url format for string:/// Or it could be a path if the store provider is an http/https location
- `store_provider` (String) Store Provider. Name of the Secret Management Access object that contains information about the store to get encrypted bytes This field needs to be provided only if the url scheme is not string:///


<a id="nestedblock--oidc_auth--client_secret--clear_secret_info"></a>
### Nested Schema for `oidc_auth.client_secret.clear_secret_info`

Optional:

- `provider_ref` (String) Provider. Name of the Secret Management Access object that contains information about the store to get encrypted bytes This field needs to be provided only if the url scheme is not string:///
- `url` (String) URL. URL of the secret. Currently supported URL schemes is string:///. For string:/// scheme, Secret needs to be encoded Base64 format. When asked for this secret, caller will get Secret bytes after Base64 decoding.



<a id="nestedblock--oidc_auth--oidc_auth_params"></a>
### Nested Schema for `oidc_auth.oidc_auth_params`

Optional:

- `auth_endpoint_url` (String) Authorization Endpoint. URL of the authorization server's authorization endpoint.
- `end_session_endpoint_url` (String) Logout Endpoint. URL of the authorization server's Logout endpoint.
- `token_endpoint_url` (String) Token Endpoint. URL of the authorization server's Token endpoint.



<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_authentication.example system/example
```
