---
page_title: "f5xc_bgp Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages a BGP resource in F5 Distributed Cloud for bgp object is the configuration for peering with external bgp servers. it is created by users in system namespace. configuration.
---

# f5xc_bgp (Resource)

Manages a BGP resource in F5 Distributed Cloud for bgp object is the configuration for peering with external bgp servers. it is created by users in system namespace. configuration.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Bgp Resource Example
# Manages a BGP resource in F5 Distributed Cloud for bgp object is the configuration for peering with external bgp servers. it is created by users in system namespace. configuration.

# Basic Bgp configuration
resource "f5xc_bgp" "example" {
  name      = "example-bgp"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # BGP configuration
  bgp_router_id = "192.168.1.1"

  bgp_peers {
    metadata {
      name = "upstream-peer"
    }
    spec {
      peer_asn = 65000
      peer_address = "192.168.1.2"
    }
  }

  local_asn = 65001

  # Site reference
  site {
    name      = "example-site"
    namespace = "system"
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the BGP. Must be unique within the namespace.
- `namespace` (String) Namespace where the BGP will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `bgp_parameters` (Block, Optional) BGP Parameters. BGP parameters for the local site (see [below for nested schema](#nestedblock--bgp_parameters))
- `labels` (Map of String) Labels to apply to this resource.
- `peers` (Block List) Peers. List of peers (see [below for nested schema](#nestedblock--peers))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `where` (Block, Optional) Site or Virtual Site Reference. VirtualSiteSiteRefSelector defines a union of reference to site or reference to virtual_site It used to refer site or a group of sites indicated by virtual site. (see [below for nested schema](#nestedblock--where))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--bgp_parameters"></a>
### Nested Schema for `bgp_parameters`

Optional:

- `asn` (Number) ASN. Autonomous System Number
- `from_site` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--bgp_parameters--from_site))
- `ip_address` (String) IP Address. Use the configured IPv4 Address as Router ID.
- `local_address` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--bgp_parameters--local_address))

<a id="nestedblock--bgp_parameters--from_site"></a>
### Nested Schema for `bgp_parameters.from_site`


<a id="nestedblock--bgp_parameters--local_address"></a>
### Nested Schema for `bgp_parameters.local_address`



<a id="nestedblock--peers"></a>
### Nested Schema for `peers`

Optional:

- `bfd_disabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--bfd_disabled))
- `bfd_enabled` (Block, Optional) BFD. BFD parameters. (see [below for nested schema](#nestedblock--peers--bfd_enabled))
- `disable` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--disable))
- `external` (Block, Optional) External BGP Peer. External BGP Peer parameters. (see [below for nested schema](#nestedblock--peers--external))
- `label` (String) Label. Specify whether this peer should be
- `metadata` (Block, Optional) Message Metadata. MessageMetaType is metadata (common attributes) of a message that only certain messages have. This information is propagated to the metadata of a child object that gets created from the containing message during view processing. The information in this type can be specified by user during create and replace APIs. (see [below for nested schema](#nestedblock--peers--metadata))
- `passive_mode_disabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--passive_mode_disabled))
- `passive_mode_enabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--passive_mode_enabled))
- `routing_policies` (Block, Optional) BGP Routing Policy. List of rules which can be applied on all or particular nodes (see [below for nested schema](#nestedblock--peers--routing_policies))

<a id="nestedblock--peers--bfd_disabled"></a>
### Nested Schema for `peers.bfd_disabled`


<a id="nestedblock--peers--bfd_enabled"></a>
### Nested Schema for `peers.bfd_enabled`

Optional:

- `multiplier` (Number) Multiplier. Specify Number of missed packets to bring session down'
- `receive_interval_milliseconds` (Number) Minimum Receive Interval. BFD receive interval timer, in milliseconds
- `transmit_interval_milliseconds` (Number) Transmit Interval. BFD transmit interval timer, in milliseconds


<a id="nestedblock--peers--disable"></a>
### Nested Schema for `peers.disable`


<a id="nestedblock--peers--external"></a>
### Nested Schema for `peers.external`

Optional:

- `address` (String) Peer Address. Specify IPV4 peer address.
- `address_ipv6` (String) Peer IPv6 Address. Specify peer IPv6 address.
- `asn` (Number) ASN. Autonomous System Number for BGP peer
- `default_gateway` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--external--default_gateway))
- `default_gateway_v6` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--external--default_gateway_v6))
- `disable` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--external--disable))
- `disable_v6` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--external--disable_v6))
- `external_connector` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--external--external_connector))
- `family_inet` (Block, Optional) BGP Family Inet. Parameters for inet family. (see [below for nested schema](#nestedblock--peers--external--family_inet))
- `from_site` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--external--from_site))
- `from_site_v6` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--external--from_site_v6))
- `interface` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--peers--external--interface))
- `interface_list` (Block, Optional) Interface List. List of network interfaces. (see [below for nested schema](#nestedblock--peers--external--interface_list))
- `md5_auth_key` (String) MD5 Authentication Key. MD5 key for protecting BGP Sessions (RFC 2385)
- `no_authentication` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--external--no_authentication))
- `port` (Number) Peer Port. Peer TCP port number.
- `subnet_begin_offset` (Number) Offset From Beginning Of Subnet. Calculate peer address using offset from the beginning of the subnet.
- `subnet_begin_offset_v6` (Number) Offset From Beginning Of Subnet. Calculate peer address using offset from the beginning of the subnet.
- `subnet_end_offset` (Number) Offset From End Of Subnet. Calculate peer address using offset from the end of the subnet.
- `subnet_end_offset_v6` (Number) Offset From End Of Subnet. Calculate peer address using offset from the end of the subnet.

<a id="nestedblock--peers--external--default_gateway"></a>
### Nested Schema for `peers.external.default_gateway`


<a id="nestedblock--peers--external--default_gateway_v6"></a>
### Nested Schema for `peers.external.default_gateway_v6`


<a id="nestedblock--peers--external--disable"></a>
### Nested Schema for `peers.external.disable`


<a id="nestedblock--peers--external--disable_v6"></a>
### Nested Schema for `peers.external.disable_v6`


<a id="nestedblock--peers--external--external_connector"></a>
### Nested Schema for `peers.external.external_connector`


<a id="nestedblock--peers--external--family_inet"></a>
### Nested Schema for `peers.external.family_inet`

Optional:

- `disable` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--external--family_inet--disable))
- `enable` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--external--family_inet--enable))

<a id="nestedblock--peers--external--family_inet--disable"></a>
### Nested Schema for `peers.external.family_inet.disable`


<a id="nestedblock--peers--external--family_inet--enable"></a>
### Nested Schema for `peers.external.family_inet.enable`



<a id="nestedblock--peers--external--from_site"></a>
### Nested Schema for `peers.external.from_site`


<a id="nestedblock--peers--external--from_site_v6"></a>
### Nested Schema for `peers.external.from_site_v6`


<a id="nestedblock--peers--external--interface"></a>
### Nested Schema for `peers.external.interface`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--peers--external--interface_list"></a>
### Nested Schema for `peers.external.interface_list`

Optional:

- `interfaces` (Block List) Interface List. List of network interfaces. (see [below for nested schema](#nestedblock--peers--external--interface_list--interfaces))

<a id="nestedblock--peers--external--interface_list--interfaces"></a>
### Nested Schema for `peers.external.interface_list.interfaces`



<a id="nestedblock--peers--external--no_authentication"></a>
### Nested Schema for `peers.external.no_authentication`



<a id="nestedblock--peers--metadata"></a>
### Nested Schema for `peers.metadata`

Optional:

- `description` (String) Description. Human readable description.
- `name` (String) Name. This is the name of the message. The value of name has to follow DNS-1035 format.


<a id="nestedblock--peers--passive_mode_disabled"></a>
### Nested Schema for `peers.passive_mode_disabled`


<a id="nestedblock--peers--passive_mode_enabled"></a>
### Nested Schema for `peers.passive_mode_enabled`


<a id="nestedblock--peers--routing_policies"></a>
### Nested Schema for `peers.routing_policies`

Optional:

- `route_policy` (Block List) BGP Routing policy. Route policy to be applied (see [below for nested schema](#nestedblock--peers--routing_policies--route_policy))

<a id="nestedblock--peers--routing_policies--route_policy"></a>
### Nested Schema for `peers.routing_policies.route_policy`

Optional:

- `all_nodes` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--routing_policies--route_policy--all_nodes))
- `inbound` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--routing_policies--route_policy--inbound))
- `node_name` (Block, Optional) Nodes. List of nodes on which BGP routing policy has to be applied (see [below for nested schema](#nestedblock--peers--routing_policies--route_policy--node_name))
- `object_refs` (Block List) BGP routing policy. Select route policy to apply. (see [below for nested schema](#nestedblock--peers--routing_policies--route_policy--object_refs))
- `outbound` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--peers--routing_policies--route_policy--outbound))

<a id="nestedblock--peers--routing_policies--route_policy--all_nodes"></a>
### Nested Schema for `peers.routing_policies.route_policy.all_nodes`


<a id="nestedblock--peers--routing_policies--route_policy--inbound"></a>
### Nested Schema for `peers.routing_policies.route_policy.inbound`


<a id="nestedblock--peers--routing_policies--route_policy--node_name"></a>
### Nested Schema for `peers.routing_policies.route_policy.node_name`


<a id="nestedblock--peers--routing_policies--route_policy--object_refs"></a>
### Nested Schema for `peers.routing_policies.route_policy.object_refs`


<a id="nestedblock--peers--routing_policies--route_policy--outbound"></a>
### Nested Schema for `peers.routing_policies.route_policy.outbound`





<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).


<a id="nestedblock--where"></a>
### Nested Schema for `where`

Optional:

- `site` (Block, Optional) Site Reference. This specifies a direct reference to a site configuration object (see [below for nested schema](#nestedblock--where--site))
- `virtual_site` (Block, Optional) Virtual Site. A reference to virtual_site object (see [below for nested schema](#nestedblock--where--virtual_site))

<a id="nestedblock--where--site"></a>
### Nested Schema for `where.site`

Optional:

- `disable_internet_vip` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--where--site--disable_internet_vip))
- `enable_internet_vip` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--where--site--enable_internet_vip))
- `network_type` (String) Virtual Network Type. Different types of virtual networks understood by the system Virtual-network of type VIRTUAL_NETWORK_SITE_LOCAL provides connectivity to public (outside) network. This is an insecure network and is connected to public internet via NAT Gateways/firwalls Virtual-network of this type is local to every site. Two virtual networks of this type on different sites are neither related nor connected. Constraints: There can be atmost one virtual network of this type in a given site... Possible values include `VIRTUAL_NETWORK_SITE_LOCAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE`, `VIRTUAL_NETWORK_PER_SITE`, `VIRTUAL_NETWORK_PUBLIC`, `VIRTUAL_NETWORK_GLOBAL`, `VIRTUAL_NETWORK_SITE_SERVICE`, `VIRTUAL_NETWORK_VER_INTERNAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE_OUTSIDE`, `VIRTUAL_NETWORK_IP_AUTO`, `VIRTUAL_NETWORK_VOLTADN_PRIVATE_NETWORK`, and others.
- `ref` (Block List) Reference. A site direct reference (see [below for nested schema](#nestedblock--where--site--ref))

<a id="nestedblock--where--site--disable_internet_vip"></a>
### Nested Schema for `where.site.disable_internet_vip`


<a id="nestedblock--where--site--enable_internet_vip"></a>
### Nested Schema for `where.site.enable_internet_vip`


<a id="nestedblock--where--site--ref"></a>
### Nested Schema for `where.site.ref`

Optional:

- `kind` (String) Kind. When a configuration object(e.g. virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')
- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.
- `uid` (String) UID. When a configuration object(e.g. virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. route's) uid.



<a id="nestedblock--where--virtual_site"></a>
### Nested Schema for `where.virtual_site`

Optional:

- `disable_internet_vip` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--where--virtual_site--disable_internet_vip))
- `enable_internet_vip` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--where--virtual_site--enable_internet_vip))
- `network_type` (String) Virtual Network Type. Different types of virtual networks understood by the system Virtual-network of type VIRTUAL_NETWORK_SITE_LOCAL provides connectivity to public (outside) network. This is an insecure network and is connected to public internet via NAT Gateways/firwalls Virtual-network of this type is local to every site. Two virtual networks of this type on different sites are neither related nor connected. Constraints: There can be atmost one virtual network of this type in a given site... Possible values include `VIRTUAL_NETWORK_SITE_LOCAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE`, `VIRTUAL_NETWORK_PER_SITE`, `VIRTUAL_NETWORK_PUBLIC`, `VIRTUAL_NETWORK_GLOBAL`, `VIRTUAL_NETWORK_SITE_SERVICE`, `VIRTUAL_NETWORK_VER_INTERNAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE_OUTSIDE`, `VIRTUAL_NETWORK_IP_AUTO`, `VIRTUAL_NETWORK_VOLTADN_PRIVATE_NETWORK`, and others.
- `ref` (Block List) Reference. A virtual_site direct reference (see [below for nested schema](#nestedblock--where--virtual_site--ref))

<a id="nestedblock--where--virtual_site--disable_internet_vip"></a>
### Nested Schema for `where.virtual_site.disable_internet_vip`


<a id="nestedblock--where--virtual_site--enable_internet_vip"></a>
### Nested Schema for `where.virtual_site.enable_internet_vip`


<a id="nestedblock--where--virtual_site--ref"></a>
### Nested Schema for `where.virtual_site.ref`

Optional:

- `kind` (String) Kind. When a configuration object(e.g. virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')
- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.
- `uid` (String) UID. When a configuration object(e.g. virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. route's) uid.

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_bgp.example system/example
```
