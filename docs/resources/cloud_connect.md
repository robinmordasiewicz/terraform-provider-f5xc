---
page_title: "f5xc_cloud_connect Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages a CloudConnect resource in F5 Distributed Cloud for establishing connectivity to cloud provider networks.
---

# f5xc_cloud_connect (Resource)

Manages a CloudConnect resource in F5 Distributed Cloud for establishing connectivity to cloud provider networks.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Cloud Connect Resource Example
# Manages a CloudConnect resource in F5 Distributed Cloud for establishing connectivity to cloud provider networks.

# Basic Cloud Connect configuration
resource "f5xc_cloud_connect" "example" {
  name      = "example-cloud-connect"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
  # [OneOf: aws_tgw_site, azure_vnet_site] AWS TGW Site Type....
  aws_tgw_site {
    # Configure aws_tgw_site settings
  }
  # Object reference. This type establishes a direct referenc...
  cred {
    # Configure cred settings
  }
  # Object reference. This type establishes a direct referenc...
  site {
    # Configure site settings
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the CloudConnect. Must be unique within the namespace.
- `namespace` (String) Namespace where the CloudConnect will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `aws_tgw_site` (Block, Optional) [OneOf: aws_tgw_site, azure_vnet_site] AWS TGW Site Type. Cloud Connect AWS TGW Site Type (see [below for nested schema](#nestedblock--aws_tgw_site))
- `azure_vnet_site` (Block, Optional) Azure VNET Site Type. Cloud Connect Azure VNET Site Type (see [below for nested schema](#nestedblock--azure_vnet_site))
- `labels` (Map of String) Labels to apply to this resource.
- `segment` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--segment))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--aws_tgw_site"></a>
### Nested Schema for `aws_tgw_site`

Optional:

- `cred` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--aws_tgw_site--cred))
- `site` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--aws_tgw_site--site))
- `vpc_attachments` (Block, Optional) VPC Attachments. (see [below for nested schema](#nestedblock--aws_tgw_site--vpc_attachments))

<a id="nestedblock--aws_tgw_site--cred"></a>
### Nested Schema for `aws_tgw_site.cred`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--aws_tgw_site--site"></a>
### Nested Schema for `aws_tgw_site.site`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--aws_tgw_site--vpc_attachments"></a>
### Nested Schema for `aws_tgw_site.vpc_attachments`

Optional:

- `vpc_list` (Block List) VPC List. (see [below for nested schema](#nestedblock--aws_tgw_site--vpc_attachments--vpc_list))

<a id="nestedblock--aws_tgw_site--vpc_attachments--vpc_list"></a>
### Nested Schema for `aws_tgw_site.vpc_attachments.vpc_list`

Optional:

- `custom_routing` (Block, Optional) AWS Route Table List. AWS Route Table List (see [below for nested schema](#nestedblock--aws_tgw_site--vpc_attachments--vpc_list--custom_routing))
- `default_route` (Block, Optional) Override Default Route Choice. Select Override Default Route Choice (see [below for nested schema](#nestedblock--aws_tgw_site--vpc_attachments--vpc_list--default_route))
- `labels` (Block, Optional) Labels. Add labels for the VPC attachment. These labels can then be used in policies such as enhanced firewall. (see [below for nested schema](#nestedblock--aws_tgw_site--vpc_attachments--vpc_list--labels))
- `manual_routing` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--aws_tgw_site--vpc_attachments--vpc_list--manual_routing))
- `vpc_id` (String) VPC ID. Enter the VPC ID of the VPC to be attached

<a id="nestedblock--aws_tgw_site--vpc_attachments--vpc_list--custom_routing"></a>
### Nested Schema for `aws_tgw_site.vpc_attachments.vpc_list.custom_routing`


<a id="nestedblock--aws_tgw_site--vpc_attachments--vpc_list--default_route"></a>
### Nested Schema for `aws_tgw_site.vpc_attachments.vpc_list.default_route`


<a id="nestedblock--aws_tgw_site--vpc_attachments--vpc_list--labels"></a>
### Nested Schema for `aws_tgw_site.vpc_attachments.vpc_list.labels`


<a id="nestedblock--aws_tgw_site--vpc_attachments--vpc_list--manual_routing"></a>
### Nested Schema for `aws_tgw_site.vpc_attachments.vpc_list.manual_routing`





<a id="nestedblock--azure_vnet_site"></a>
### Nested Schema for `azure_vnet_site`

Optional:

- `site` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--azure_vnet_site--site))
- `vnet_attachments` (Block, Optional) VNET Attachments. (see [below for nested schema](#nestedblock--azure_vnet_site--vnet_attachments))

<a id="nestedblock--azure_vnet_site--site"></a>
### Nested Schema for `azure_vnet_site.site`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--azure_vnet_site--vnet_attachments"></a>
### Nested Schema for `azure_vnet_site.vnet_attachments`

Optional:

- `vnet_list` (Block List) VNET List. (see [below for nested schema](#nestedblock--azure_vnet_site--vnet_attachments--vnet_list))

<a id="nestedblock--azure_vnet_site--vnet_attachments--vnet_list"></a>
### Nested Schema for `azure_vnet_site.vnet_attachments.vnet_list`

Optional:

- `custom_routing` (Block, Optional) List Azure Route Table with Static Route. List Azure Route Table with Static Route (see [below for nested schema](#nestedblock--azure_vnet_site--vnet_attachments--vnet_list--custom_routing))
- `default_route` (Block, Optional) Override Default Route Choice. Select Override Default Route Choice (see [below for nested schema](#nestedblock--azure_vnet_site--vnet_attachments--vnet_list--default_route))
- `labels` (Block, Optional) Labels. Add labels for the VNET attachments. These labels can then be used in policies such as enhanced firewall policies. (see [below for nested schema](#nestedblock--azure_vnet_site--vnet_attachments--vnet_list--labels))
- `manual_routing` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--azure_vnet_site--vnet_attachments--vnet_list--manual_routing))
- `subscription_id` (String) Subscription ID. Enter the Subscription ID of the VNET to be attached
- `vnet_id` (String) VNET ID. Enter the vnet ID of the VNET to be attached in format /<resource-group-name>/<vnet-name>

<a id="nestedblock--azure_vnet_site--vnet_attachments--vnet_list--custom_routing"></a>
### Nested Schema for `azure_vnet_site.vnet_attachments.vnet_list.custom_routing`


<a id="nestedblock--azure_vnet_site--vnet_attachments--vnet_list--default_route"></a>
### Nested Schema for `azure_vnet_site.vnet_attachments.vnet_list.default_route`


<a id="nestedblock--azure_vnet_site--vnet_attachments--vnet_list--labels"></a>
### Nested Schema for `azure_vnet_site.vnet_attachments.vnet_list.labels`


<a id="nestedblock--azure_vnet_site--vnet_attachments--vnet_list--manual_routing"></a>
### Nested Schema for `azure_vnet_site.vnet_attachments.vnet_list.manual_routing`





<a id="nestedblock--segment"></a>
### Nested Schema for `segment`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_cloud_connect.example system/example
```
