---
page_title: "f5xc_cloud_link Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages new CloudLink with configured parameters in F5 Distributed Cloud.
---

# f5xc_cloud_link (Resource)

Manages new CloudLink with configured parameters in F5 Distributed Cloud.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Cloud Link Resource Example
# Manages new CloudLink with configured parameters in F5 Distributed Cloud.

# Basic Cloud Link configuration
resource "f5xc_cloud_link" "example" {
  name      = "example-cloud-link"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
    # [OneOf: aws, gcp] Amazon Web Services(AWS) CloudLink Prov...
    aws {
      # Configure aws settings
    }
    # Object reference. This type establishes a direct referenc...
    aws_cred {
      # Configure aws_cred settings
    }
    # Bring Your Own Connections. List of Bring You Own Connection
    byoc {
      # Configure byoc settings
    }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the CloudLink. Must be unique within the namespace.
- `namespace` (String) Namespace where the CloudLink will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `aws` (Block, Optional) [OneOf: aws, gcp] Amazon Web Services(AWS) CloudLink Provider. CloudLink for AWS Cloud Provider (see [below for nested schema](#nestedblock--aws))
- `disabled` (Block, Optional) [OneOf: disabled, enabled] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--disabled))
- `enabled` (Block, Optional) CloudLink ADN Network Config. (see [below for nested schema](#nestedblock--enabled))
- `gcp` (Block, Optional) Google Cloud Platform (GCP) CloudLink Provider. CloudLink for GCP Cloud Provider (see [below for nested schema](#nestedblock--gcp))
- `labels` (Map of String) Labels to apply to this resource.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--aws"></a>
### Nested Schema for `aws`

Optional:

- `aws_cred` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--aws--aws_cred))
- `byoc` (Block, Optional) Bring Your Own Connections. List of Bring You Own Connection (see [below for nested schema](#nestedblock--aws--byoc))
- `custom_asn` (Number) Custom ASN. F5XC will use custom ASN to create a Direct Connect Gateway 4200000000-4294967294

<a id="nestedblock--aws--aws_cred"></a>
### Nested Schema for `aws.aws_cred`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--aws--byoc"></a>
### Nested Schema for `aws.byoc`

Optional:

- `connections` (Block List) Bring Your Own Connections. List of Bring You Own Connections. These AWS Direct Connect connections are not managed by F5XC but will be used for connecting sites and REs. (see [below for nested schema](#nestedblock--aws--byoc--connections))

<a id="nestedblock--aws--byoc--connections"></a>
### Nested Schema for `aws.byoc.connections`

Optional:

- `auth_key` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--aws--byoc--connections--auth_key))
- `bgp_asn` (Number) BGP ASN. The Border Gateway Protocol (BGP) Autonomous System Number (ASN) of your on-premises router for the new virtual interface to be configured on AWS.
- `connection_id` (String) Direct Connect Connection Id. Id of the existing AWS Direct Connect Connection
- `ipv4` (Block, Optional) IPv4 Peering. Configure BGP IPv4 peering for endpoints (see [below for nested schema](#nestedblock--aws--byoc--connections--ipv4))
- `metadata` (Block, Optional) Message Metadata. MessageMetaType is metadata (common attributes) of a message that only certain messages have. This information is propagated to the metadata of a child object that gets created from the containing message during view processing. The information in this type can be specified by user during create and replace APIs. (see [below for nested schema](#nestedblock--aws--byoc--connections--metadata))
- `region` (String) Region. Region where the connection is setup
- `system_generated_name` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--aws--byoc--connections--system_generated_name))
- `tags` (Block, Optional) AWS Tags. AWS Tags is a label consisting of a user-defined key and value. It helps to manage, identify, organize, search for, and filter resources in AWS console. Specified tags will be added to Virtual interface along with any F5XC specific tags (see [below for nested schema](#nestedblock--aws--byoc--connections--tags))
- `user_assigned_name` (String) User Assigned. User is managing the AWS resource name
- `virtual_interface_type` (String) Virtual Interface Type. Defines the type of virtual interface that needs to be configured on AWS - PRIVATE: Private A private virtual interface should be used to access an Amazon VPC using private IP addresses. - TRANSIT: Transit A transit virtual interface is a VLAN that transports traffic from a Direct Connect gateway to one or more transit gateways. The only possible value is `PRIVATE`.
- `vlan` (Number) Virtual Local Area Network (VLAN). Virtual Local Area Network number for the new virtual interface to be configured on the AWS. This tag is required for any traffic traversing the AWS Direct Connect connection

<a id="nestedblock--aws--byoc--connections--auth_key"></a>
### Nested Schema for `aws.byoc.connections.auth_key`


<a id="nestedblock--aws--byoc--connections--ipv4"></a>
### Nested Schema for `aws.byoc.connections.ipv4`


<a id="nestedblock--aws--byoc--connections--metadata"></a>
### Nested Schema for `aws.byoc.connections.metadata`


<a id="nestedblock--aws--byoc--connections--system_generated_name"></a>
### Nested Schema for `aws.byoc.connections.system_generated_name`


<a id="nestedblock--aws--byoc--connections--tags"></a>
### Nested Schema for `aws.byoc.connections.tags`





<a id="nestedblock--disabled"></a>
### Nested Schema for `disabled`


<a id="nestedblock--enabled"></a>
### Nested Schema for `enabled`

Optional:

- `cloudlink_network_name` (String) Private ADN Network. Establish private connectivity with the F5 Distributed Cloud Global Network using a Private ADN network. To provision a Private ADN network, please contact F5 Distributed Cloud support.


<a id="nestedblock--gcp"></a>
### Nested Schema for `gcp`

Optional:

- `byoc` (Block, Optional) GCP Bring Your Own Connections. List of GCP Bring You Own Connections (see [below for nested schema](#nestedblock--gcp--byoc))
- `gcp_cred` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--gcp--gcp_cred))

<a id="nestedblock--gcp--byoc"></a>
### Nested Schema for `gcp.byoc`

Optional:

- `connections` (Block List) Bring Your Own Connections. Each 'Bring Your Own Connection' represents a virtual connection that the customer has provisioned in the Cloud (example: AWS Direct Connect). F5XC will orchestrate networking resources in the cloud to facilitate seamless private connectivity. (see [below for nested schema](#nestedblock--gcp--byoc--connections))

<a id="nestedblock--gcp--byoc--connections"></a>
### Nested Schema for `gcp.byoc.connections`

Optional:

- `interconnect_attachment_name` (String) Interconnect Attachment Name. Name of already-existing GCP Cloud Interconnect Attachment
- `metadata` (Block, Optional) Message Metadata. MessageMetaType is metadata (common attributes) of a message that only certain messages have. This information is propagated to the metadata of a child object that gets created from the containing message during view processing. The information in this type can be specified by user during create and replace APIs. (see [below for nested schema](#nestedblock--gcp--byoc--connections--metadata))
- `project` (String) Specified Project. Specify a GCP Project for the interconnect attachment
- `region` (String) Region. GCP Region in which the GCP Cloud Interconnect attachment is configured
- `same_as_credential` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--gcp--byoc--connections--same_as_credential))

<a id="nestedblock--gcp--byoc--connections--metadata"></a>
### Nested Schema for `gcp.byoc.connections.metadata`


<a id="nestedblock--gcp--byoc--connections--same_as_credential"></a>
### Nested Schema for `gcp.byoc.connections.same_as_credential`




<a id="nestedblock--gcp--gcp_cred"></a>
### Nested Schema for `gcp.gcp_cred`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.



<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_cloud_link.example system/example
```
