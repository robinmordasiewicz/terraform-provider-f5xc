---
page_title: "f5xc_discovery Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages a Discovery resource in F5 Distributed Cloud for api to create discovery object for a site or virtual site in system namespace configuration.
---

# f5xc_discovery (Resource)

Manages a Discovery resource in F5 Distributed Cloud for api to create discovery object for a site or virtual site in system namespace configuration.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Discovery Resource Example
# Manages a Discovery resource in F5 Distributed Cloud for api to create discovery object for a site or virtual site in system namespace configuration.

# Basic Discovery configuration
resource "f5xc_discovery" "example" {
  name      = "example-discovery"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Discovery configuration
  discovery_k8s {
    access_info {
      kubeconfig_url {
        clear_secret_info {
          url = "string:///base64-kubeconfig"
        }
      }
      isolated {}
    }
    publish_info {
      disable {}
    }
  }

  # Site selection
  where {
    site {
      ref {
        name      = "example-site"
        namespace = "system"
      }
      network_type = "VIRTUAL_NETWORK_SITE_LOCAL_INSIDE"
    }
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the Discovery. Must be unique within the namespace.
- `namespace` (String) Namespace where the Discovery will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `cluster_id` (String) [OneOf: cluster_id, no_cluster_id] Discovery cluster Identifier. Specify identifier for discovery cluster. This identifier can be specified in endpoint object to discover only from this discovery object.
- `discovery_consul` (Block, Optional) [OneOf: discovery_consul, discovery_k8s] Consul Discovery Configuration. Discovery configuration for Hashicorp Consul (see [below for nested schema](#nestedblock--discovery_consul))
- `discovery_k8s` (Block, Optional) K8S Discovery Configuration. Discovery configuration for K8s. (see [below for nested schema](#nestedblock--discovery_k8s))
- `labels` (Map of String) Labels to apply to this resource.
- `no_cluster_id` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--no_cluster_id))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `where` (Block, Optional) Network or Site Reference. NetworkSiteRefSelector defines a union of reference to site or reference to virtual_network or reference to virtual_site It is used to determine virtual network using following rules * Direct reference to virtual_network object * Site local network when refering to site object * All site local networks for sites selected by refering to virtual_site object (see [below for nested schema](#nestedblock--where))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--discovery_consul"></a>
### Nested Schema for `discovery_consul`

Optional:

- `access_info` (Block, Optional) Hashicorp Consul Parameters. Hashicorp Consul API server information (see [below for nested schema](#nestedblock--discovery_consul--access_info))
- `publish_info` (Block, Optional) VIP Publish Configuration. Consul Configuration to publish VIPs (see [below for nested schema](#nestedblock--discovery_consul--publish_info))

<a id="nestedblock--discovery_consul--access_info"></a>
### Nested Schema for `discovery_consul.access_info`

Optional:

- `connection_info` (Block, Optional) REST API Config. Configuration details to access discovery service rest API. (see [below for nested schema](#nestedblock--discovery_consul--access_info--connection_info))
- `http_basic_auth_info` (Block, Optional) Hashicorp Consul Credentials. Authentication parameters to access Hashicorp Consul. (see [below for nested schema](#nestedblock--discovery_consul--access_info--http_basic_auth_info))

<a id="nestedblock--discovery_consul--access_info--connection_info"></a>
### Nested Schema for `discovery_consul.access_info.connection_info`

Optional:

- `api_server` (String) API Server and Port. API server must be a fully qualified domain string and port specified as host:port pair
- `tls_info` (Block, Optional) Client TLS Config. TLS config for client of discovery service (see [below for nested schema](#nestedblock--discovery_consul--access_info--connection_info--tls_info))

<a id="nestedblock--discovery_consul--access_info--connection_info--tls_info"></a>
### Nested Schema for `discovery_consul.access_info.connection_info.tls_info`



<a id="nestedblock--discovery_consul--access_info--http_basic_auth_info"></a>
### Nested Schema for `discovery_consul.access_info.http_basic_auth_info`

Optional:

- `passwd_url` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--discovery_consul--access_info--http_basic_auth_info--passwd_url))
- `user_name` (String) User Name. username in consul

<a id="nestedblock--discovery_consul--access_info--http_basic_auth_info--passwd_url"></a>
### Nested Schema for `discovery_consul.access_info.http_basic_auth_info.passwd_url`




<a id="nestedblock--discovery_consul--publish_info"></a>
### Nested Schema for `discovery_consul.publish_info`

Optional:

- `disable` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--discovery_consul--publish_info--disable))
- `publish` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--discovery_consul--publish_info--publish))

<a id="nestedblock--discovery_consul--publish_info--disable"></a>
### Nested Schema for `discovery_consul.publish_info.disable`


<a id="nestedblock--discovery_consul--publish_info--publish"></a>
### Nested Schema for `discovery_consul.publish_info.publish`




<a id="nestedblock--discovery_k8s"></a>
### Nested Schema for `discovery_k8s`

Optional:

- `access_info` (Block, Optional) K8s API Server. K8S API server access (see [below for nested schema](#nestedblock--discovery_k8s--access_info))
- `default_all` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--discovery_k8s--default_all))
- `namespace_mapping` (Block, Optional) K8S Namespace Mapping. Select the mapping between K8s namespaces from which services will be discovered and App Namespace to which the discovered services will be shared. (see [below for nested schema](#nestedblock--discovery_k8s--namespace_mapping))
- `publish_info` (Block, Optional) VIP Publish Configurations. K8S Configuration to publish VIPs (see [below for nested schema](#nestedblock--discovery_k8s--publish_info))

<a id="nestedblock--discovery_k8s--access_info"></a>
### Nested Schema for `discovery_k8s.access_info`

Optional:

- `connection_info` (Block, Optional) REST API Config. Configuration details to access discovery service rest API. (see [below for nested schema](#nestedblock--discovery_k8s--access_info--connection_info))
- `isolated` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--discovery_k8s--access_info--isolated))
- `kubeconfig_url` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--discovery_k8s--access_info--kubeconfig_url))
- `reachable` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--discovery_k8s--access_info--reachable))

<a id="nestedblock--discovery_k8s--access_info--connection_info"></a>
### Nested Schema for `discovery_k8s.access_info.connection_info`

Optional:

- `api_server` (String) API Server and Port. API server must be a fully qualified domain string and port specified as host:port pair
- `tls_info` (Block, Optional) Client TLS Config. TLS config for client of discovery service (see [below for nested schema](#nestedblock--discovery_k8s--access_info--connection_info--tls_info))

<a id="nestedblock--discovery_k8s--access_info--connection_info--tls_info"></a>
### Nested Schema for `discovery_k8s.access_info.connection_info.tls_info`



<a id="nestedblock--discovery_k8s--access_info--isolated"></a>
### Nested Schema for `discovery_k8s.access_info.isolated`


<a id="nestedblock--discovery_k8s--access_info--kubeconfig_url"></a>
### Nested Schema for `discovery_k8s.access_info.kubeconfig_url`

Optional:

- `blindfold_secret_info` (Block, Optional) Blindfold Secret. BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management (see [below for nested schema](#nestedblock--discovery_k8s--access_info--kubeconfig_url--blindfold_secret_info))
- `clear_secret_info` (Block, Optional) In-Clear Secret. ClearSecretInfoType specifies information about the Secret that is not encrypted. (see [below for nested schema](#nestedblock--discovery_k8s--access_info--kubeconfig_url--clear_secret_info))

<a id="nestedblock--discovery_k8s--access_info--kubeconfig_url--blindfold_secret_info"></a>
### Nested Schema for `discovery_k8s.access_info.kubeconfig_url.blindfold_secret_info`


<a id="nestedblock--discovery_k8s--access_info--kubeconfig_url--clear_secret_info"></a>
### Nested Schema for `discovery_k8s.access_info.kubeconfig_url.clear_secret_info`



<a id="nestedblock--discovery_k8s--access_info--reachable"></a>
### Nested Schema for `discovery_k8s.access_info.reachable`



<a id="nestedblock--discovery_k8s--default_all"></a>
### Nested Schema for `discovery_k8s.default_all`


<a id="nestedblock--discovery_k8s--namespace_mapping"></a>
### Nested Schema for `discovery_k8s.namespace_mapping`

Optional:

- `items` (Block List) Regex Matching. Map K8s namespace(s) to App Namespaces. In Shared Configuration, Discovered Services can only be mapped to a single App Namespace, which is determined by the first matched regex. (see [below for nested schema](#nestedblock--discovery_k8s--namespace_mapping--items))

<a id="nestedblock--discovery_k8s--namespace_mapping--items"></a>
### Nested Schema for `discovery_k8s.namespace_mapping.items`

Optional:

- `namespace` (String) F5XC Application Namespaces. Select a namespace
- `namespace_regex` (String) K8S Namespaces. The regex here will be used to match K8s namespace(s).



<a id="nestedblock--discovery_k8s--publish_info"></a>
### Nested Schema for `discovery_k8s.publish_info`

Optional:

- `disable` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--discovery_k8s--publish_info--disable))
- `dns_delegation` (Block, Optional) K8SDelegationType. (see [below for nested schema](#nestedblock--discovery_k8s--publish_info--dns_delegation))
- `publish` (Block, Optional) K8SPublishType. (see [below for nested schema](#nestedblock--discovery_k8s--publish_info--publish))
- `publish_fqdns` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--discovery_k8s--publish_info--publish_fqdns))

<a id="nestedblock--discovery_k8s--publish_info--disable"></a>
### Nested Schema for `discovery_k8s.publish_info.disable`


<a id="nestedblock--discovery_k8s--publish_info--dns_delegation"></a>
### Nested Schema for `discovery_k8s.publish_info.dns_delegation`

Optional:

- `dns_mode` (String) DNS Mode. Two modes are possible CoreDNS: Whether external K8s cluster is running core-dns KubeDNS: External K8s cluster is running kube-dns. Possible values are `CORE_DNS`, `KUBE_DNS`.
- `subdomain` (String) Subdomain. The DNS subdomain for which F5XC will respond to DNS queries.


<a id="nestedblock--discovery_k8s--publish_info--publish"></a>
### Nested Schema for `discovery_k8s.publish_info.publish`

Optional:

- `namespace` (String) Default Namespace. The namespace where the service/endpoints need to be created if it's not included in the domain. The external K8S administrator needs to ensure that the namespace exists.


<a id="nestedblock--discovery_k8s--publish_info--publish_fqdns"></a>
### Nested Schema for `discovery_k8s.publish_info.publish_fqdns`




<a id="nestedblock--no_cluster_id"></a>
### Nested Schema for `no_cluster_id`


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).


<a id="nestedblock--where"></a>
### Nested Schema for `where`

Optional:

- `site` (Block, Optional) Site Reference. This specifies a direct reference to a site configuration object (see [below for nested schema](#nestedblock--where--site))
- `virtual_network` (Block, Optional) Network Reference. This specifies a direct reference to a network configuration object (see [below for nested schema](#nestedblock--where--virtual_network))
- `virtual_site` (Block, Optional) Virtual Site. A reference to virtual_site object (see [below for nested schema](#nestedblock--where--virtual_site))

<a id="nestedblock--where--site"></a>
### Nested Schema for `where.site`

Optional:

- `disable_internet_vip` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--where--site--disable_internet_vip))
- `enable_internet_vip` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--where--site--enable_internet_vip))
- `network_type` (String) Virtual Network Type. Different types of virtual networks understood by the system Virtual-network of type VIRTUAL_NETWORK_SITE_LOCAL provides connectivity to public (outside) network. This is an insecure network and is connected to public internet via NAT Gateways/firwalls Virtual-network of this type is local to every site. Two virtual networks of this type on different sites are neither related nor connected. Constraints: There can be atmost one virtual network of this type in a given site... Possible values include `VIRTUAL_NETWORK_SITE_LOCAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE`, `VIRTUAL_NETWORK_PER_SITE`, `VIRTUAL_NETWORK_PUBLIC`, `VIRTUAL_NETWORK_GLOBAL`, `VIRTUAL_NETWORK_SITE_SERVICE`, `VIRTUAL_NETWORK_VER_INTERNAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE_OUTSIDE`, `VIRTUAL_NETWORK_IP_AUTO`, `VIRTUAL_NETWORK_VOLTADN_PRIVATE_NETWORK`, and others.
- `ref` (Block List) Reference. A site direct reference (see [below for nested schema](#nestedblock--where--site--ref))

<a id="nestedblock--where--site--disable_internet_vip"></a>
### Nested Schema for `where.site.disable_internet_vip`


<a id="nestedblock--where--site--enable_internet_vip"></a>
### Nested Schema for `where.site.enable_internet_vip`


<a id="nestedblock--where--site--ref"></a>
### Nested Schema for `where.site.ref`

Optional:

- `kind` (String) Kind. When a configuration object(e.g. virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')
- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.
- `uid` (String) UID. When a configuration object(e.g. virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. route's) uid.



<a id="nestedblock--where--virtual_network"></a>
### Nested Schema for `where.virtual_network`

Optional:

- `ref` (Block List) Reference. A virtual network direct reference (see [below for nested schema](#nestedblock--where--virtual_network--ref))

<a id="nestedblock--where--virtual_network--ref"></a>
### Nested Schema for `where.virtual_network.ref`

Optional:

- `kind` (String) Kind. When a configuration object(e.g. virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')
- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.
- `uid` (String) UID. When a configuration object(e.g. virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. route's) uid.



<a id="nestedblock--where--virtual_site"></a>
### Nested Schema for `where.virtual_site`

Optional:

- `disable_internet_vip` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--where--virtual_site--disable_internet_vip))
- `enable_internet_vip` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--where--virtual_site--enable_internet_vip))
- `network_type` (String) Virtual Network Type. Different types of virtual networks understood by the system Virtual-network of type VIRTUAL_NETWORK_SITE_LOCAL provides connectivity to public (outside) network. This is an insecure network and is connected to public internet via NAT Gateways/firwalls Virtual-network of this type is local to every site. Two virtual networks of this type on different sites are neither related nor connected. Constraints: There can be atmost one virtual network of this type in a given site... Possible values include `VIRTUAL_NETWORK_SITE_LOCAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE`, `VIRTUAL_NETWORK_PER_SITE`, `VIRTUAL_NETWORK_PUBLIC`, `VIRTUAL_NETWORK_GLOBAL`, `VIRTUAL_NETWORK_SITE_SERVICE`, `VIRTUAL_NETWORK_VER_INTERNAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE_OUTSIDE`, `VIRTUAL_NETWORK_IP_AUTO`, `VIRTUAL_NETWORK_VOLTADN_PRIVATE_NETWORK`, and others.
- `ref` (Block List) Reference. A virtual_site direct reference (see [below for nested schema](#nestedblock--where--virtual_site--ref))

<a id="nestedblock--where--virtual_site--disable_internet_vip"></a>
### Nested Schema for `where.virtual_site.disable_internet_vip`


<a id="nestedblock--where--virtual_site--enable_internet_vip"></a>
### Nested Schema for `where.virtual_site.enable_internet_vip`


<a id="nestedblock--where--virtual_site--ref"></a>
### Nested Schema for `where.virtual_site.ref`

Optional:

- `kind` (String) Kind. When a configuration object(e.g. virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')
- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.
- `uid` (String) UID. When a configuration object(e.g. virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. route's) uid.

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_discovery.example system/example
```
