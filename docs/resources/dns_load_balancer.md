---
page_title: "f5xc_dns_load_balancer Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages DNS Load Balancer in a given namespace. If one already exist it will give a error. in F5 Distributed Cloud.
---

# f5xc_dns_load_balancer (Resource)

Manages DNS Load Balancer in a given namespace. If one already exist it will give a error. in F5 Distributed Cloud.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Dns Load Balancer Resource Example
# Manages DNS Load Balancer in a given namespace. If one already exist it will give a error. in F5 Distributed Cloud.

# Basic Dns Load Balancer configuration
resource "f5xc_dns_load_balancer" "example" {
  name      = "example-dns-load-balancer"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # DNS Load Balancer configuration
  record_type = "A"

  # DNS zone reference
  dns_zone {
    name      = "example-dns-zone"
    namespace = "system"
  }

  # Rule-based load balancing
  rule_list {
    rules {
      geo_location_set {
        name      = "us-geo"
        namespace = "system"
      }
      pool {
        name      = "us-pool"
        namespace = "system"
      }
      score = 100
    }
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the DNSLoadBalancer. Must be unique within the namespace.
- `namespace` (String) Namespace where the DNSLoadBalancer will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `fallback_pool` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--fallback_pool))
- `labels` (Map of String) Labels to apply to this resource.
- `record_type` (String) Resource Record Type. Resource Record Type - A: A - AAAA: AAAA - MX: MX - CNAME: CNAME - SRV: SRV. Possible values are `A`, `AAAA`, `MX`, `CNAME`, `SRV`.
- `response_cache` (Block, Optional) Response Cache. Response Cache x-required (see [below for nested schema](#nestedblock--response_cache))
- `rule_list` (Block, Optional) Load Balancing Rule List. List of the Load Balancing Rules (see [below for nested schema](#nestedblock--rule_list))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--fallback_pool"></a>
### Nested Schema for `fallback_pool`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--response_cache"></a>
### Nested Schema for `response_cache`

Optional:

- `default_response_cache_parameters` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--response_cache--default_response_cache_parameters))
- `disable` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--response_cache--disable))
- `response_cache_parameters` (Block, Optional) Response Cache Parameters. (see [below for nested schema](#nestedblock--response_cache--response_cache_parameters))

<a id="nestedblock--response_cache--default_response_cache_parameters"></a>
### Nested Schema for `response_cache.default_response_cache_parameters`


<a id="nestedblock--response_cache--disable"></a>
### Nested Schema for `response_cache.disable`


<a id="nestedblock--response_cache--response_cache_parameters"></a>
### Nested Schema for `response_cache.response_cache_parameters`

Optional:

- `cache_cidr_ipv4` (Number) Length of IPv4 CIDR masks. Length of CIDR masks used to group IPv4 clients
- `cache_cidr_ipv6` (Number) Length of IPv6 CIDR masks. Length of CIDR masks used to group IPv6 clients
- `cache_ttl` (Number) TTL. TTL for response cache



<a id="nestedblock--rule_list"></a>
### Nested Schema for `rule_list`

Optional:

- `rules` (Block List) Load Balancing Rules. Rules to perform load balancing (see [below for nested schema](#nestedblock--rule_list--rules))

<a id="nestedblock--rule_list--rules"></a>
### Nested Schema for `rule_list.rules`

Optional:

- `asn_list` (Block, Optional) ASN Match List. An unordered set of RFC 6793 defined 4-byte AS numbers that can be used to create allow or deny lists for use in network policy or service policy. It can be used to create the allow list only for DNS Load Balancer. (see [below for nested schema](#nestedblock--rule_list--rules--asn_list))
- `asn_matcher` (Block, Optional) ASN Matcher. Match any AS number contained in the list of bgp_asn_sets. (see [below for nested schema](#nestedblock--rule_list--rules--asn_matcher))
- `geo_location_label_selector` (Block, Optional) Label Selector. This type can be used to establish a 'selector reference' from one object(called selector) to a set of other objects(called selectees) based on the value of expresssions. A label selector is a label query over a set of resources. An empty label selector matches all objects. A null label selector matches no objects. Label selector is immutable. expressions is a list of strings of label selection expression. Each string has ',' separated values which are 'AND' and all strings ar... (see [below for nested schema](#nestedblock--rule_list--rules--geo_location_label_selector))
- `geo_location_set` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--rule_list--rules--geo_location_set))
- `ip_prefix_list` (Block, Optional) IP Prefix Match List. List of IP Prefix strings to match against. (see [below for nested schema](#nestedblock--rule_list--rules--ip_prefix_list))
- `ip_prefix_set` (Block, Optional) IP Prefix Matcher. Match any ip prefix contained in the list of ip_prefix_sets. The result of the match is inverted if invert_matcher is true. (see [below for nested schema](#nestedblock--rule_list--rules--ip_prefix_set))
- `pool` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--rule_list--rules--pool))
- `score` (Number) Score. When multiple load balancing rules match a query, the one with the highest score is chosen

<a id="nestedblock--rule_list--rules--asn_list"></a>
### Nested Schema for `rule_list.rules.asn_list`

Optional:

- `as_numbers` (List of String) AS Numbers. An unordered set of RFC 6793 defined 4-byte AS numbers that can be used to create allow or deny lists for use in network policy or service policy. It can be used to create the allow list only for DNS Load Balancer.


<a id="nestedblock--rule_list--rules--asn_matcher"></a>
### Nested Schema for `rule_list.rules.asn_matcher`

Optional:

- `asn_sets` (Block List) BGP ASN Sets. A list of references to bgp_asn_set objects. (see [below for nested schema](#nestedblock--rule_list--rules--asn_matcher--asn_sets))

<a id="nestedblock--rule_list--rules--asn_matcher--asn_sets"></a>
### Nested Schema for `rule_list.rules.asn_matcher.asn_sets`



<a id="nestedblock--rule_list--rules--geo_location_label_selector"></a>
### Nested Schema for `rule_list.rules.geo_location_label_selector`

Optional:

- `expressions` (List of String) Selector Expression. expressions contains the kubernetes style label expression for selections.


<a id="nestedblock--rule_list--rules--geo_location_set"></a>
### Nested Schema for `rule_list.rules.geo_location_set`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--rule_list--rules--ip_prefix_list"></a>
### Nested Schema for `rule_list.rules.ip_prefix_list`

Optional:

- `invert_match` (Boolean) Invert Match Result. Invert the match result.
- `ip_prefixes` (List of String) IPv4 Prefix List. List of IPv4 prefix strings.


<a id="nestedblock--rule_list--rules--ip_prefix_set"></a>
### Nested Schema for `rule_list.rules.ip_prefix_set`

Optional:

- `invert_matcher` (Boolean) Invert IP Matcher. Invert the match result.
- `prefix_sets` (Block List) IP Prefix Sets. A list of references to ip_prefix_set objects. (see [below for nested schema](#nestedblock--rule_list--rules--ip_prefix_set--prefix_sets))

<a id="nestedblock--rule_list--rules--ip_prefix_set--prefix_sets"></a>
### Nested Schema for `rule_list.rules.ip_prefix_set.prefix_sets`



<a id="nestedblock--rule_list--rules--pool"></a>
### Nested Schema for `rule_list.rules.pool`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.




<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_dns_load_balancer.example system/example
```
