---
page_title: "f5xc_external_connector Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages a ExternalConnector resource in F5 Distributed Cloud for external_connector configuration configuration.
---

# f5xc_external_connector (Resource)

Manages a ExternalConnector resource in F5 Distributed Cloud for external_connector configuration configuration.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# External Connector Resource Example
# Manages a ExternalConnector resource in F5 Distributed Cloud for external_connector configuration configuration.

# Basic External Connector configuration
resource "f5xc_external_connector" "example" {
  name      = "example-external-connector"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
    # Object reference. This type establishes a direct referenc...
    ce_site_reference {
      # Configure ce_site_reference settings
    }
    # IPSec. External Connector with IPSec tunnel
    ipsec {
      # Configure ipsec settings
    }
    # IKE Parameters. IKE configuration parameters required for...
    ike_parameters {
      # Configure ike_parameters settings
    }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the ExternalConnector. Must be unique within the namespace.
- `namespace` (String) Namespace where the ExternalConnector will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `ce_site_reference` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--ce_site_reference))
- `ipsec` (Block, Optional) IPSec. External Connector with IPSec tunnel (see [below for nested schema](#nestedblock--ipsec))
- `labels` (Map of String) Labels to apply to this resource.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--ce_site_reference"></a>
### Nested Schema for `ce_site_reference`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--ipsec"></a>
### Nested Schema for `ipsec`

Optional:

- `ike_parameters` (Block, Optional) IKE Parameters. IKE configuration parameters required for IPSec Connection type (see [below for nested schema](#nestedblock--ipsec--ike_parameters))
- `ipsec_tunnel_parameters` (Block, Optional) Configure IPSec Tunnel Parameters. In this section, we will configure the tunnel parameters, source, destination, IP addresses, and segment. (see [below for nested schema](#nestedblock--ipsec--ipsec_tunnel_parameters))

<a id="nestedblock--ipsec--ike_parameters"></a>
### Nested Schema for `ipsec.ike_parameters`

Optional:

- `dpd_disabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--ipsec--ike_parameters--dpd_disabled))
- `dpd_keep_alive_timer` (Block, Optional) Keepalive Timer. (see [below for nested schema](#nestedblock--ipsec--ike_parameters--dpd_keep_alive_timer))
- `ike_phase1_profile` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--ipsec--ike_parameters--ike_phase1_profile))
- `ike_phase2_profile` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--ipsec--ike_parameters--ike_phase2_profile))
- `initiator` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--ipsec--ike_parameters--initiator))
- `responder` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--ipsec--ike_parameters--responder))
- `rm_hostname` (String) Hostname. Configure an hostname Remote IKE ID
- `rm_ip_address` (Block, Optional) IP Address. IP Address used to specify an IPv4 or IPv6 address (see [below for nested schema](#nestedblock--ipsec--ike_parameters--rm_ip_address))
- `use_default_local_ike_id` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--ipsec--ike_parameters--use_default_local_ike_id))
- `use_default_remote_ike_id` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--ipsec--ike_parameters--use_default_remote_ike_id))

<a id="nestedblock--ipsec--ike_parameters--dpd_disabled"></a>
### Nested Schema for `ipsec.ike_parameters.dpd_disabled`


<a id="nestedblock--ipsec--ike_parameters--dpd_keep_alive_timer"></a>
### Nested Schema for `ipsec.ike_parameters.dpd_keep_alive_timer`

Optional:

- `timeout` (Number) Keepalive Timer.


<a id="nestedblock--ipsec--ike_parameters--ike_phase1_profile"></a>
### Nested Schema for `ipsec.ike_parameters.ike_phase1_profile`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--ipsec--ike_parameters--ike_phase2_profile"></a>
### Nested Schema for `ipsec.ike_parameters.ike_phase2_profile`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--ipsec--ike_parameters--initiator"></a>
### Nested Schema for `ipsec.ike_parameters.initiator`


<a id="nestedblock--ipsec--ike_parameters--responder"></a>
### Nested Schema for `ipsec.ike_parameters.responder`


<a id="nestedblock--ipsec--ike_parameters--rm_ip_address"></a>
### Nested Schema for `ipsec.ike_parameters.rm_ip_address`

Optional:

- `ipv4` (Block, Optional) IPv4 Address. IPv4 Address in dot-decimal notation (see [below for nested schema](#nestedblock--ipsec--ike_parameters--rm_ip_address--ipv4))
- `ipv6` (Block, Optional) IPv6 Address. IPv6 Address specified as hexadecimal numbers separated by ':' (see [below for nested schema](#nestedblock--ipsec--ike_parameters--rm_ip_address--ipv6))

<a id="nestedblock--ipsec--ike_parameters--rm_ip_address--ipv4"></a>
### Nested Schema for `ipsec.ike_parameters.rm_ip_address.ipv4`


<a id="nestedblock--ipsec--ike_parameters--rm_ip_address--ipv6"></a>
### Nested Schema for `ipsec.ike_parameters.rm_ip_address.ipv6`



<a id="nestedblock--ipsec--ike_parameters--use_default_local_ike_id"></a>
### Nested Schema for `ipsec.ike_parameters.use_default_local_ike_id`


<a id="nestedblock--ipsec--ike_parameters--use_default_remote_ike_id"></a>
### Nested Schema for `ipsec.ike_parameters.use_default_remote_ike_id`



<a id="nestedblock--ipsec--ipsec_tunnel_parameters"></a>
### Nested Schema for `ipsec.ipsec_tunnel_parameters`

Optional:

- `peer_ip_address` (Block, Optional) IPv4 Address. IPv4 Address in dot-decimal notation (see [below for nested schema](#nestedblock--ipsec--ipsec_tunnel_parameters--peer_ip_address))
- `psk` (String) Pre-Shared Key. The IKE pre-shared key (PSK) is required to ensure the IKE peers can authenticate one another within IKE phase 1 negotiation.
- `segment` (Block, Optional) Segment Reference Type. Reference to Segment Object (see [below for nested schema](#nestedblock--ipsec--ipsec_tunnel_parameters--segment))
- `site_local_inside_network` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--ipsec--ipsec_tunnel_parameters--site_local_inside_network))
- `site_local_network` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--ipsec--ipsec_tunnel_parameters--site_local_network))
- `tunnel_eps` (Block List) Tunnel Endpoint. Configure tunnel parameters, local and remote IP addresses (see [below for nested schema](#nestedblock--ipsec--ipsec_tunnel_parameters--tunnel_eps))
- `tunnel_mtu` (Number) MTU. The tunnel MTU defines the maximum size of the packet that can be sent through the tunnel without needing to be fragmented

<a id="nestedblock--ipsec--ipsec_tunnel_parameters--peer_ip_address"></a>
### Nested Schema for `ipsec.ipsec_tunnel_parameters.peer_ip_address`

Optional:

- `addr` (String) IPv4 Address. IPv4 Address in string form with dot-decimal notation


<a id="nestedblock--ipsec--ipsec_tunnel_parameters--segment"></a>
### Nested Schema for `ipsec.ipsec_tunnel_parameters.segment`

Optional:

- `refs` (Block List) Segment. Reference to Segment Object (see [below for nested schema](#nestedblock--ipsec--ipsec_tunnel_parameters--segment--refs))

<a id="nestedblock--ipsec--ipsec_tunnel_parameters--segment--refs"></a>
### Nested Schema for `ipsec.ipsec_tunnel_parameters.segment.refs`



<a id="nestedblock--ipsec--ipsec_tunnel_parameters--site_local_inside_network"></a>
### Nested Schema for `ipsec.ipsec_tunnel_parameters.site_local_inside_network`


<a id="nestedblock--ipsec--ipsec_tunnel_parameters--site_local_network"></a>
### Nested Schema for `ipsec.ipsec_tunnel_parameters.site_local_network`


<a id="nestedblock--ipsec--ipsec_tunnel_parameters--tunnel_eps"></a>
### Nested Schema for `ipsec.ipsec_tunnel_parameters.tunnel_eps`

Optional:

- `interface` (String) Interface. For the chosen node, specify the interface that will be the tunnel source.
- `local_tunnel_ip` (String) Local Tunnel IP Address/Prefix Length. For a particular tunnel on a node, specify the local tunnel IP Address i.e. the IP address of the tunnel on the CE node itself and a subnet prefix length
- `node` (String) Node. A CE site is composed of multiple nodes. Choose a node that will be part of this external connection.
- `remote_tunnel_ip` (String) Remote Tunnel IP Address/Prefix Length. For a particular tunnel on a node, specify the remote tunnel IP Address i.e. the IP address of the tunnel on the remote gateway and a subnet prefix length




<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_external_connector.example system/example
```
