---
page_title: "f5xc_fleet Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages fleet will create a fleet object in 'system' namespace of the user in F5 Distributed Cloud.
---

# f5xc_fleet (Resource)

Manages fleet will create a fleet object in 'system' namespace of the user in F5 Distributed Cloud.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Fleet Resource Example
# Manages fleet will create a fleet object in 'system' namespace of the user in F5 Distributed Cloud.

# Basic Fleet configuration
resource "f5xc_fleet" "example" {
  name      = "example-fleet"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Fleet configuration
  fleet_label = "env=production"

  # Network connectors
  inside_virtual_network {
    name      = "inside-network"
    namespace = "system"
  }

  outside_virtual_network {
    name      = "outside-network"
    namespace = "system"
  }

  # Default config
  default_config {}
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the Fleet. Must be unique within the namespace.
- `namespace` (String) Namespace where the Fleet will be created.

### Optional

- `allow_all_usb` (Block, Optional) [OneOf: allow_all_usb, deny_all_usb, usb_policy] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--allow_all_usb))
- `annotations` (Map of String) Annotations to apply to this resource.
- `blocked_services` (Block List) Disable Node Local Services. Disable node local services on this site. (see [below for nested schema](#nestedblock--blocked_services))
- `bond_device_list` (Block, Optional) [OneOf: bond_device_list, no_bond_devices] Bond Devices List. List of bond devices for this fleet (see [below for nested schema](#nestedblock--bond_device_list))
- `dc_cluster_group` (Block, Optional) [OneOf: dc_cluster_group, dc_cluster_group_inside, no_dc_cluster_group] Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--dc_cluster_group))
- `dc_cluster_group_inside` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--dc_cluster_group_inside))
- `default_config` (Block, Optional) [OneOf: default_config, device_list, interface_list] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--default_config))
- `default_sriov_interface` (Block, Optional) [OneOf: default_sriov_interface, sriov_interfaces] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--default_sriov_interface))
- `default_storage_class` (Block, Optional) [OneOf: default_storage_class, storage_class_list] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--default_storage_class))
- `deny_all_usb` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--deny_all_usb))
- `device_list` (Block, Optional) List of Devices. Add device for all interfaces belonging to this fleet (see [below for nested schema](#nestedblock--device_list))
- `disable_gpu` (Block, Optional) [OneOf: disable_gpu, enable_gpu, enable_vgpu] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--disable_gpu))
- `disable_vm` (Block, Optional) [OneOf: disable_vm, enable_vm] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--disable_vm))
- `enable_default_fleet_config_download` (Boolean) Enable Default Fleet Config Download. Enable default fleet config, It must be set for storage config and gpu config
- `enable_gpu` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--enable_gpu))
- `enable_vgpu` (Block, Optional) vGPU Configuration. Licensing configuration for NVIDIA vGPU (see [below for nested schema](#nestedblock--enable_vgpu))
- `enable_vm` (Block, Optional) VM Configuration. VMs support configuration (see [below for nested schema](#nestedblock--enable_vm))
- `fleet_label` (String) Fleet Label Value. fleet_label value is used to create known_label 'ves.io/fleet=<fleet_label>' The known_label is created in the 'shared' namespace for the tenant. A virtual_site object with name <fleet_label> is also created in 'shared' namespace for tenant. The virtual_site object will select all sites configured with the known_label above fleet_label with 'sfo' will create a known_label 'ves.io/fleet=sfo' in tenant for the fleet
- `inside_virtual_network` (Block List) Site Local Inside Virtual Network. Default inside (site local) virtual network for the fleet (see [below for nested schema](#nestedblock--inside_virtual_network))
- `interface_list` (Block, Optional) List of Interfaces. Add all interfaces belonging to this fleet (see [below for nested schema](#nestedblock--interface_list))
- `kubernetes_upgrade_drain` (Block, Optional) Node by Node Upgrade. Specify how worker nodes within a site will be upgraded. (see [below for nested schema](#nestedblock--kubernetes_upgrade_drain))
- `labels` (Map of String) Labels to apply to this resource.
- `log_receiver` (Block, Optional) [OneOf: log_receiver, logs_streaming_disabled] Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--log_receiver))
- `logs_streaming_disabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--logs_streaming_disabled))
- `network_connectors` (Block List) Network Connectors. Network Connector defines connection between two virtual networks in a given site. Fleet defines one or more such network connectors. The network connectors configuration is applied on all sites that are member of the fleet. (see [below for nested schema](#nestedblock--network_connectors))
- `network_firewall` (Block List) Network Firewall. Network Firewall defines firewall to be applied for the virtual networks in the fleet. The network firewall configuration is applied on all sites that are member of the fleet. Constraints The Network Firewall is applied on Virtual Networks of type site local network and site local inside network (see [below for nested schema](#nestedblock--network_firewall))
- `no_bond_devices` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--no_bond_devices))
- `no_dc_cluster_group` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--no_dc_cluster_group))
- `no_storage_device` (Block, Optional) [OneOf: no_storage_device, storage_device_list] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--no_storage_device))
- `no_storage_interfaces` (Block, Optional) [OneOf: no_storage_interfaces, storage_interface_list] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--no_storage_interfaces))
- `no_storage_static_routes` (Block, Optional) [OneOf: no_storage_static_routes, storage_static_routes] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--no_storage_static_routes))
- `operating_system_version` (String) Operating System Version. Desired Operating System version that is applied to all sites that are member of the fleet. Current Operating System version can be overridden via site config.
- `outside_virtual_network` (Block List) Outside (Site Local) Virtual Network. Default outside (site local) virtual network for the fleet (see [below for nested schema](#nestedblock--outside_virtual_network))
- `performance_enhancement_mode` (Block, Optional) Performance Enhancement Mode. x-required Optimize the site for L3 or L7 traffic processing. L7 optimized is the default. (see [below for nested schema](#nestedblock--performance_enhancement_mode))
- `sriov_interfaces` (Block, Optional) Custom SR-IOV interfaces Configuration List. List of all custom SR-IOV interfaces configuration (see [below for nested schema](#nestedblock--sriov_interfaces))
- `storage_class_list` (Block, Optional) Custom Storage Class List. Add additional custom storage classes in kubernetes for this fleet (see [below for nested schema](#nestedblock--storage_class_list))
- `storage_device_list` (Block, Optional) Custom Storage Device List. Add additional custom storage classes in kubernetes for this fleet (see [below for nested schema](#nestedblock--storage_device_list))
- `storage_interface_list` (Block, Optional) List of Interfaces. Add all interfaces belonging to this fleet (see [below for nested schema](#nestedblock--storage_interface_list))
- `storage_static_routes` (Block, Optional) Storage Static Routes List. List of storage static routes (see [below for nested schema](#nestedblock--storage_static_routes))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `usb_policy` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--usb_policy))
- `volterra_software_version` (String) Software Version. F5XC software version is human readable string matching released set of version components. The given software version is applied to all sites that are member of the fleet. Current software installed can be overridden via site config.

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--allow_all_usb"></a>
### Nested Schema for `allow_all_usb`


<a id="nestedblock--blocked_services"></a>
### Nested Schema for `blocked_services`

Optional:

- `dns` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--blocked_services--dns))
- `network_type` (String) Virtual Network Type. Different types of virtual networks understood by the system Virtual-network of type VIRTUAL_NETWORK_SITE_LOCAL provides connectivity to public (outside) network. This is an insecure network and is connected to public internet via NAT Gateways/firwalls Virtual-network of this type is local to every site. Two virtual networks of this type on different sites are neither related nor connected. Constraints: There can be atmost one virtual network of this type in a given site... Possible values include `VIRTUAL_NETWORK_SITE_LOCAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE`, `VIRTUAL_NETWORK_PER_SITE`, `VIRTUAL_NETWORK_PUBLIC`, `VIRTUAL_NETWORK_GLOBAL`, `VIRTUAL_NETWORK_SITE_SERVICE`, `VIRTUAL_NETWORK_VER_INTERNAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE_OUTSIDE`, `VIRTUAL_NETWORK_IP_AUTO`, `VIRTUAL_NETWORK_VOLTADN_PRIVATE_NETWORK`, and others.
- `ssh` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--blocked_services--ssh))
- `web_user_interface` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--blocked_services--web_user_interface))

<a id="nestedblock--blocked_services--dns"></a>
### Nested Schema for `blocked_services.dns`


<a id="nestedblock--blocked_services--ssh"></a>
### Nested Schema for `blocked_services.ssh`


<a id="nestedblock--blocked_services--web_user_interface"></a>
### Nested Schema for `blocked_services.web_user_interface`



<a id="nestedblock--bond_device_list"></a>
### Nested Schema for `bond_device_list`

Optional:

- `bond_devices` (Block List) Bond Devices. List of bond devices (see [below for nested schema](#nestedblock--bond_device_list--bond_devices))

<a id="nestedblock--bond_device_list--bond_devices"></a>
### Nested Schema for `bond_device_list.bond_devices`

Optional:

- `active_backup` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--bond_device_list--bond_devices--active_backup))
- `devices` (List of String) Member Ethernet Devices. Ethernet devices that will make up this bond
- `lacp` (Block, Optional) LACP parameters. LACP parameters for the bond device (see [below for nested schema](#nestedblock--bond_device_list--bond_devices--lacp))
- `link_polling_interval` (Number) Link Polling Interval. Link polling interval in milliseconds
- `link_up_delay` (Number) Link Up Delay. Milliseconds wait before link is declared up
- `name` (String) Bond Device Name. Name for the Bond. Ex 'bond0'

<a id="nestedblock--bond_device_list--bond_devices--active_backup"></a>
### Nested Schema for `bond_device_list.bond_devices.active_backup`


<a id="nestedblock--bond_device_list--bond_devices--lacp"></a>
### Nested Schema for `bond_device_list.bond_devices.lacp`

Optional:

- `rate` (Number) LACP Packet Interval. Interval in seconds to transmit LACP packets




<a id="nestedblock--dc_cluster_group"></a>
### Nested Schema for `dc_cluster_group`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--dc_cluster_group_inside"></a>
### Nested Schema for `dc_cluster_group_inside`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--default_config"></a>
### Nested Schema for `default_config`


<a id="nestedblock--default_sriov_interface"></a>
### Nested Schema for `default_sriov_interface`


<a id="nestedblock--default_storage_class"></a>
### Nested Schema for `default_storage_class`


<a id="nestedblock--deny_all_usb"></a>
### Nested Schema for `deny_all_usb`


<a id="nestedblock--device_list"></a>
### Nested Schema for `device_list`

Optional:

- `devices` (Block List) Devices. Configuration for all devices in the fleet. Examples of devices are - network interfaces, cameras, scanners etc. Configuration a device is applied on VER node if the VER node is member of this fleet and has an corresponding interface/device. The mapping from device configured in fleet with interface/device in VER node depends on the type of device and is documented in device instance specific sections (see [below for nested schema](#nestedblock--device_list--devices))

<a id="nestedblock--device_list--devices"></a>
### Nested Schema for `device_list.devices`

Optional:

- `name` (String) Device Name. Name of the device including the unit number (e.g. eth0 or disk1). The name must match name of device in host-os of node
- `network_device` (Block, Optional) Network Device Type. Represents physical network interface. The 'interface' reference points to a Network Interface object. Attributes such as Labels, MTU from Network Interface must be applied to the device. Device mapping to nodes A fleet can have many devices and nodes in VER customer edge site can have many interfaces. An interface in node inherits configuration from a device by matching, - device_name in Network Interface for the device - device name for physical-interface in the node (see [below for nested schema](#nestedblock--device_list--devices--network_device))
- `owner` (String) Device Owner Type. Defines ownership for a device. Device owner is invalid Device is owned by VER pod. usually it will be network interface device or accelerator like crypto engine. Device is available to be owned by vK8s workload on the site, like camera GPU etc. Device is not available to be owned by vK8s or VER. Can be exposed via some other service. Like TPM. Possible values are `DEVICE_OWNER_INVALID`, `DEVICE_OWNER_VER`, `DEVICE_OWNER_VK8S_WORK_LOAD`, `DEVICE_OWNER_HOST`.

<a id="nestedblock--device_list--devices--network_device"></a>
### Nested Schema for `device_list.devices.network_device`

Optional:

- `interface` (Block List) Network Interface. Network Interface attributes for the device. User network interface configuration for this network device. Attributes like labels, MTU from the 'interface' are applied to corresponding interface in VER node If network interface refers to a virtual-network, the virtual-netowrk type must be consistent with use attribute given below If use is NETWORK_INTERFACE_USE_REGULAR, the virtual-network must be of type VIRTUAL_NETWORK_SITE_LOCAL or VIRTUAL_NETWORK_SITE_LOCAL_INSIDE if us... (see [below for nested schema](#nestedblock--device_list--devices--network_device--interface))
- `use` (String) Network Device Use. Defines how the device is used If networking device is owned by VER, it is available for users to configure as required If networking device is owned by VER, it is included in bootstrap config and member of outside network. If networking device is owned by VER, it is included in bootstrap config and member of inside network. Possible values are `NETWORK_INTERFACE_USE_REGULAR`, `NETWORK_INTERFACE_USE_OUTSIDE`, `NETWORK_INTERFACE_USE_INSIDE`.

<a id="nestedblock--device_list--devices--network_device--interface"></a>
### Nested Schema for `device_list.devices.network_device.interface`





<a id="nestedblock--disable_gpu"></a>
### Nested Schema for `disable_gpu`


<a id="nestedblock--disable_vm"></a>
### Nested Schema for `disable_vm`


<a id="nestedblock--enable_gpu"></a>
### Nested Schema for `enable_gpu`


<a id="nestedblock--enable_vgpu"></a>
### Nested Schema for `enable_vgpu`

Optional:

- `feature_type` (String) Feature Type. Set feature to be enabled Operate with a degraded vGPU performance Enable NVIDIA vGPU Enable NVIDIA RTX Virtual Workstation Enable NVIDIA Virtual Compute Server. Possible values are `UNLICENSED`, `VGPU`, `VWS`, `VCS`.
- `server_address` (String) License Server Address. Set License Server Address
- `server_port` (Number) License Server Port Number. Set License Server port number


<a id="nestedblock--enable_vm"></a>
### Nested Schema for `enable_vm`


<a id="nestedblock--inside_virtual_network"></a>
### Nested Schema for `inside_virtual_network`

Optional:

- `kind` (String) Kind. When a configuration object(e.g. virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')
- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.
- `uid` (String) UID. When a configuration object(e.g. virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. route's) uid.


<a id="nestedblock--interface_list"></a>
### Nested Schema for `interface_list`

Optional:

- `interfaces` (Block List) List of Interfaces. Add all interfaces belonging to this fleet (see [below for nested schema](#nestedblock--interface_list--interfaces))

<a id="nestedblock--interface_list--interfaces"></a>
### Nested Schema for `interface_list.interfaces`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.



<a id="nestedblock--kubernetes_upgrade_drain"></a>
### Nested Schema for `kubernetes_upgrade_drain`

Optional:

- `disable_upgrade_drain` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--kubernetes_upgrade_drain--disable_upgrade_drain))
- `enable_upgrade_drain` (Block, Optional) Enable Node by Node Upgrade. Specify batch upgrade settings for worker nodes within a site. (see [below for nested schema](#nestedblock--kubernetes_upgrade_drain--enable_upgrade_drain))

<a id="nestedblock--kubernetes_upgrade_drain--disable_upgrade_drain"></a>
### Nested Schema for `kubernetes_upgrade_drain.disable_upgrade_drain`


<a id="nestedblock--kubernetes_upgrade_drain--enable_upgrade_drain"></a>
### Nested Schema for `kubernetes_upgrade_drain.enable_upgrade_drain`

Optional:

- `disable_vega_upgrade_mode` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--kubernetes_upgrade_drain--enable_upgrade_drain--disable_vega_upgrade_mode))
- `drain_max_unavailable_node_count` (Number) Node Batch Size Count.
- `drain_node_timeout` (Number) Upgrade Wait Time. Seconds to wait before initiating upgrade on the next set of nodes. Setting it to 0 will wait indefinitely for all services on nodes to be upgraded gracefully before proceeding to the next set of nodes. (Warning: It may block upgrade if services on a node cannot be gracefully upgraded. It is recommended to use the default value).
- `enable_vega_upgrade_mode` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--kubernetes_upgrade_drain--enable_upgrade_drain--enable_vega_upgrade_mode))

<a id="nestedblock--kubernetes_upgrade_drain--enable_upgrade_drain--disable_vega_upgrade_mode"></a>
### Nested Schema for `kubernetes_upgrade_drain.enable_upgrade_drain.disable_vega_upgrade_mode`


<a id="nestedblock--kubernetes_upgrade_drain--enable_upgrade_drain--enable_vega_upgrade_mode"></a>
### Nested Schema for `kubernetes_upgrade_drain.enable_upgrade_drain.enable_vega_upgrade_mode`




<a id="nestedblock--log_receiver"></a>
### Nested Schema for `log_receiver`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--logs_streaming_disabled"></a>
### Nested Schema for `logs_streaming_disabled`


<a id="nestedblock--network_connectors"></a>
### Nested Schema for `network_connectors`

Optional:

- `kind` (String) Kind. When a configuration object(e.g. virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')
- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.
- `uid` (String) UID. When a configuration object(e.g. virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. route's) uid.


<a id="nestedblock--network_firewall"></a>
### Nested Schema for `network_firewall`

Optional:

- `kind` (String) Kind. When a configuration object(e.g. virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')
- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.
- `uid` (String) UID. When a configuration object(e.g. virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. route's) uid.


<a id="nestedblock--no_bond_devices"></a>
### Nested Schema for `no_bond_devices`


<a id="nestedblock--no_dc_cluster_group"></a>
### Nested Schema for `no_dc_cluster_group`


<a id="nestedblock--no_storage_device"></a>
### Nested Schema for `no_storage_device`


<a id="nestedblock--no_storage_interfaces"></a>
### Nested Schema for `no_storage_interfaces`


<a id="nestedblock--no_storage_static_routes"></a>
### Nested Schema for `no_storage_static_routes`


<a id="nestedblock--outside_virtual_network"></a>
### Nested Schema for `outside_virtual_network`

Optional:

- `kind` (String) Kind. When a configuration object(e.g. virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')
- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.
- `uid` (String) UID. When a configuration object(e.g. virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. route's) uid.


<a id="nestedblock--performance_enhancement_mode"></a>
### Nested Schema for `performance_enhancement_mode`

Optional:

- `perf_mode_l3_enhanced` (Block, Optional) L3 Mode Enhanced Performance. x-required L3 enhanced performance mode options (see [below for nested schema](#nestedblock--performance_enhancement_mode--perf_mode_l3_enhanced))
- `perf_mode_l7_enhanced` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--performance_enhancement_mode--perf_mode_l7_enhanced))

<a id="nestedblock--performance_enhancement_mode--perf_mode_l3_enhanced"></a>
### Nested Schema for `performance_enhancement_mode.perf_mode_l3_enhanced`

Optional:

- `jumbo` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--performance_enhancement_mode--perf_mode_l3_enhanced--jumbo))
- `no_jumbo` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--performance_enhancement_mode--perf_mode_l3_enhanced--no_jumbo))

<a id="nestedblock--performance_enhancement_mode--perf_mode_l3_enhanced--jumbo"></a>
### Nested Schema for `performance_enhancement_mode.perf_mode_l3_enhanced.jumbo`


<a id="nestedblock--performance_enhancement_mode--perf_mode_l3_enhanced--no_jumbo"></a>
### Nested Schema for `performance_enhancement_mode.perf_mode_l3_enhanced.no_jumbo`



<a id="nestedblock--performance_enhancement_mode--perf_mode_l7_enhanced"></a>
### Nested Schema for `performance_enhancement_mode.perf_mode_l7_enhanced`



<a id="nestedblock--sriov_interfaces"></a>
### Nested Schema for `sriov_interfaces`

Optional:

- `sriov_interface` (Block List) Custom SR-IOV interfaces Configuration. Use custom SR-IOV interfaces Configuration (see [below for nested schema](#nestedblock--sriov_interfaces--sriov_interface))

<a id="nestedblock--sriov_interfaces--sriov_interface"></a>
### Nested Schema for `sriov_interfaces.sriov_interface`

Optional:

- `interface_name` (String) Name of physical interface. Name of SR-IOV physical interface
- `number_of_vfio_vfs` (Number) Number of virtual functions reserved for vfio. Number of virtual functions reserved for VNFs and DPDK-based CNFs
- `number_of_vfs` (Number) Total number of virtual functions. Total number of virtual functions



<a id="nestedblock--storage_class_list"></a>
### Nested Schema for `storage_class_list`

Optional:

- `storage_classes` (Block List) List of Storage Classes. List of custom storage classes (see [below for nested schema](#nestedblock--storage_class_list--storage_classes))

<a id="nestedblock--storage_class_list--storage_classes"></a>
### Nested Schema for `storage_class_list.storage_classes`

Optional:

- `advanced_storage_parameters` (Block, Optional) Advanced Parameters. Map of parameter name and string value (see [below for nested schema](#nestedblock--storage_class_list--storage_classes--advanced_storage_parameters))
- `allow_volume_expansion` (Boolean) Allow Volume Expansion. Allow volume expansion.
- `custom_storage` (Block, Optional) Custom StorageClass. Custom Storage Class allows to insert Kubernetes storageclass definition which will be applied into given site. (see [below for nested schema](#nestedblock--storage_class_list--storage_classes--custom_storage))
- `default_storage_class` (Boolean) Default Storage Class. Make this storage class default storage class for the K8s cluster
- `description` (String) Storage Class Description. Description for this storage class
- `hpe_storage` (Block, Optional) HPE Storage. Storage class Device configuration for HPE Storage (see [below for nested schema](#nestedblock--storage_class_list--storage_classes--hpe_storage))
- `netapp_trident` (Block, Optional) NetApp Trident Storage. Storage class Device configuration for NetApp Trident (see [below for nested schema](#nestedblock--storage_class_list--storage_classes--netapp_trident))
- `pure_service_orchestrator` (Block, Optional) Pure Storage Service Orchestrator. Storage class Device configuration for Pure Service Orchestrator (see [below for nested schema](#nestedblock--storage_class_list--storage_classes--pure_service_orchestrator))
- `reclaim_policy` (String) Reclaim Policy. Reclaim Policy
- `storage_class_name` (String) Storage Class Name. Name of the storage class as it will appear in K8s.
- `storage_device` (String) Storage Device. Storage device that this class will use. The Device name defined at previous step.

<a id="nestedblock--storage_class_list--storage_classes--advanced_storage_parameters"></a>
### Nested Schema for `storage_class_list.storage_classes.advanced_storage_parameters`


<a id="nestedblock--storage_class_list--storage_classes--custom_storage"></a>
### Nested Schema for `storage_class_list.storage_classes.custom_storage`

Optional:

- `yaml` (String) Storage Class YAML. K8s YAML for StorageClass


<a id="nestedblock--storage_class_list--storage_classes--hpe_storage"></a>
### Nested Schema for `storage_class_list.storage_classes.hpe_storage`

Optional:

- `allow_mutations` (String) allowMutations. mutation can override specified parameters
- `allow_overrides` (String) allowOverrides. PVC can override specified parameters
- `dedupe_enabled` (Boolean) dedupeEnabled. Indicates that the volume should enable deduplication.
- `description` (String) Description. The SecretName parameter is used to identify name of secret to identify backend storage's auth information
- `destroy_on_delete` (Boolean) destroyOnDelete. Indicates the backing Nimble volume (including snapshots) should be destroyed when the PVC is deleted
- `encrypted` (Boolean) encrypted. Indicates that the volume should be encrypted.
- `folder` (String) folder. The name of the folder in which to place the volume.
- `limit_iops` (String) limitIops. The IOPS limit of the volume.
- `limit_mbps` (String) limitMbps. The IOPS limit of the volume.
- `performance_policy` (String) performancePolicy. The name of the performance policy to assign to the volume.
- `pool` (String) pool. The name of the pool in which to place the volume.
- `protection_template` (String) protectionTemplate. The name of the performance policy to assign to the volume.
- `secret_name` (String) Secret Name. The SecretName parameter is used to identify name of secret to identify backend storage's auth information
- `secret_namespace` (String) Secret Namespace. The SecretNamespace parameter is used to identify name of namespace where secret resides
- `sync_on_detach` (Boolean) syncOnDetach. Indicates that a snapshot of the volume should be synced to the replication partner each time it is detached from a node.
- `thick` (Boolean) thick. Indicates that the volume should be thick provisioned.


<a id="nestedblock--storage_class_list--storage_classes--netapp_trident"></a>
### Nested Schema for `storage_class_list.storage_classes.netapp_trident`

Optional:

- `selector` (Block, Optional) Selector. Using the Selector field, each StorageClass calls out which virtual pool(s) may be used to host a volume. The volume will have the aspects defined in the chosen virtual pool. (see [below for nested schema](#nestedblock--storage_class_list--storage_classes--netapp_trident--selector))
- `storage_pools` (String) Storage Pools. The storagePools parameter is used to further restrict the set of pools that match any specified attributes

<a id="nestedblock--storage_class_list--storage_classes--netapp_trident--selector"></a>
### Nested Schema for `storage_class_list.storage_classes.netapp_trident.selector`



<a id="nestedblock--storage_class_list--storage_classes--pure_service_orchestrator"></a>
### Nested Schema for `storage_class_list.storage_classes.pure_service_orchestrator`

Optional:

- `backend` (String) Backend. Defines type of Pure storage backend block or file. The volume will have the aspects defined in the chosen virtual pool.
- `bandwidth_limit` (String) Bandwidth Limit. It must be between 1 MB/s and 512 GB/s. Enter the size as a number (bytes must be multiple of 512) or number with a single character unit symbol. Valid unit symbols are K, M, G, representing KiB, MiB, and GiB.
- `iops_limit` (Number) IOPS Limit. Enable IOPS limitation. It must be between 100 and 100 million. If value is 0, IOPS limit is not defined.




<a id="nestedblock--storage_device_list"></a>
### Nested Schema for `storage_device_list`

Optional:

- `storage_devices` (Block List) List of Storage Devices. List of custom storage devices (see [below for nested schema](#nestedblock--storage_device_list--storage_devices))

<a id="nestedblock--storage_device_list--storage_devices"></a>
### Nested Schema for `storage_device_list.storage_devices`

Optional:

- `advanced_advanced_parameters` (Block, Optional) Advanced Parameters. Map of parameter name and string value (see [below for nested schema](#nestedblock--storage_device_list--storage_devices--advanced_advanced_parameters))
- `custom_storage` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--storage_device_list--storage_devices--custom_storage))
- `hpe_storage` (Block, Optional) HPE Storage. Device configuration for HPE Storage (see [below for nested schema](#nestedblock--storage_device_list--storage_devices--hpe_storage))
- `netapp_trident` (Block, Optional) NetApp Trident. Device configuration for NetApp Trident Storage (see [below for nested schema](#nestedblock--storage_device_list--storage_devices--netapp_trident))
- `pure_service_orchestrator` (Block, Optional) Pure Storage Service Orchestrator. Device configuration for Pure Storage Service Orchestrator (see [below for nested schema](#nestedblock--storage_device_list--storage_devices--pure_service_orchestrator))
- `storage_device` (String) Storage Device. Storage device and device unit

<a id="nestedblock--storage_device_list--storage_devices--advanced_advanced_parameters"></a>
### Nested Schema for `storage_device_list.storage_devices.advanced_advanced_parameters`


<a id="nestedblock--storage_device_list--storage_devices--custom_storage"></a>
### Nested Schema for `storage_device_list.storage_devices.custom_storage`


<a id="nestedblock--storage_device_list--storage_devices--hpe_storage"></a>
### Nested Schema for `storage_device_list.storage_devices.hpe_storage`

Optional:

- `api_server_port` (Number) Storage server Port. Enter Storage Server Port
- `iscsi_chap_password` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--storage_device_list--storage_devices--hpe_storage--iscsi_chap_password))
- `iscsi_chap_user` (String) iscsi chapUser. chap Username to connect to the HPE storage
- `password` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--storage_device_list--storage_devices--hpe_storage--password))
- `storage_server_ip_address` (String) Storage Server IP address. Enter storage server IP address
- `storage_server_name` (String) Storage Server Name. Enter storage server Name
- `username` (String) Username. Username to connect to the HPE storage management IP

<a id="nestedblock--storage_device_list--storage_devices--hpe_storage--iscsi_chap_password"></a>
### Nested Schema for `storage_device_list.storage_devices.hpe_storage.iscsi_chap_password`


<a id="nestedblock--storage_device_list--storage_devices--hpe_storage--password"></a>
### Nested Schema for `storage_device_list.storage_devices.hpe_storage.password`



<a id="nestedblock--storage_device_list--storage_devices--netapp_trident"></a>
### Nested Schema for `storage_device_list.storage_devices.netapp_trident`

Optional:

- `netapp_backend_ontap_nas` (Block, Optional) Storage Backend NetApp ONTAP NAS. Configuration of storage backend for NetApp ONTAP NAS (see [below for nested schema](#nestedblock--storage_device_list--storage_devices--netapp_trident--netapp_backend_ontap_nas))
- `netapp_backend_ontap_san` (Block, Optional) Storage Backend NetApp ONTAP SAN. Configuration of storage backend for NetApp ONTAP SAN (see [below for nested schema](#nestedblock--storage_device_list--storage_devices--netapp_trident--netapp_backend_ontap_san))

<a id="nestedblock--storage_device_list--storage_devices--netapp_trident--netapp_backend_ontap_nas"></a>
### Nested Schema for `storage_device_list.storage_devices.netapp_trident.netapp_backend_ontap_nas`


<a id="nestedblock--storage_device_list--storage_devices--netapp_trident--netapp_backend_ontap_san"></a>
### Nested Schema for `storage_device_list.storage_devices.netapp_trident.netapp_backend_ontap_san`



<a id="nestedblock--storage_device_list--storage_devices--pure_service_orchestrator"></a>
### Nested Schema for `storage_device_list.storage_devices.pure_service_orchestrator`

Optional:

- `arrays` (Block, Optional) Arrays Configuration. Device configuration for PSO Arrays (see [below for nested schema](#nestedblock--storage_device_list--storage_devices--pure_service_orchestrator--arrays))
- `cluster_id` (String) Cluster ID. clusterID is added as a prefix for all volumes created by this PSO installation. clusterID is also used to identify the volumes used by the datastore, pso-db. clusterID MUST BE UNIQUE for multiple K8s clusters running on top of the same storage arrays. characters allowed: alphanumeric and underscores
- `enable_storage_topology` (Boolean) Enable Storage Topology. This option is to enable/disable the csi topology feature for pso-csi
- `enable_strict_topology` (Boolean) Enable Strict Topology. This option is to enable/disable the strict csi topology feature for pso-csi

<a id="nestedblock--storage_device_list--storage_devices--pure_service_orchestrator--arrays"></a>
### Nested Schema for `storage_device_list.storage_devices.pure_service_orchestrator.arrays`





<a id="nestedblock--storage_interface_list"></a>
### Nested Schema for `storage_interface_list`

Optional:

- `interfaces` (Block List) List of Interfaces. Add all interfaces belonging to this fleet (see [below for nested schema](#nestedblock--storage_interface_list--interfaces))

<a id="nestedblock--storage_interface_list--interfaces"></a>
### Nested Schema for `storage_interface_list.interfaces`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.



<a id="nestedblock--storage_static_routes"></a>
### Nested Schema for `storage_static_routes`

Optional:

- `storage_routes` (Block List) List of Static Routes. List of storage static routes (see [below for nested schema](#nestedblock--storage_static_routes--storage_routes))

<a id="nestedblock--storage_static_routes--storage_routes"></a>
### Nested Schema for `storage_static_routes.storage_routes`

Optional:

- `attrs` (List of String) Attributes. List of route attributes associated with the static route
- `labels` (Block, Optional) Static Route Labels. Add Labels for this Static Route, these labels can be used in network policy (see [below for nested schema](#nestedblock--storage_static_routes--storage_routes--labels))
- `nexthop` (Block, Optional) Nexthop. Identifies the next-hop for a route (see [below for nested schema](#nestedblock--storage_static_routes--storage_routes--nexthop))
- `subnets` (Block List) Subnets. List of route prefixes (see [below for nested schema](#nestedblock--storage_static_routes--storage_routes--subnets))

<a id="nestedblock--storage_static_routes--storage_routes--labels"></a>
### Nested Schema for `storage_static_routes.storage_routes.labels`


<a id="nestedblock--storage_static_routes--storage_routes--nexthop"></a>
### Nested Schema for `storage_static_routes.storage_routes.nexthop`

Optional:

- `interface` (Block List) Network Interface. Nexthop is network interface when type is 'Network-Interface' (see [below for nested schema](#nestedblock--storage_static_routes--storage_routes--nexthop--interface))
- `nexthop_address` (Block, Optional) IP Address. IP Address used to specify an IPv4 or IPv6 address (see [below for nested schema](#nestedblock--storage_static_routes--storage_routes--nexthop--nexthop_address))
- `type` (String) Nexthop Types. Defines types of next-hop Use default gateway on the local interface as gateway for route. Assumes there is only one local interface on the virtual network. Use the specified address as nexthop Use the network interface as nexthop Discard nexthop, used when attr type is Advertise Used in VoltADN private virtual network. Possible values are `NEXT_HOP_DEFAULT_GATEWAY`, `NEXT_HOP_USE_CONFIGURED`, `NEXT_HOP_NETWORK_INTERFACE`.

<a id="nestedblock--storage_static_routes--storage_routes--nexthop--interface"></a>
### Nested Schema for `storage_static_routes.storage_routes.nexthop.interface`


<a id="nestedblock--storage_static_routes--storage_routes--nexthop--nexthop_address"></a>
### Nested Schema for `storage_static_routes.storage_routes.nexthop.nexthop_address`



<a id="nestedblock--storage_static_routes--storage_routes--subnets"></a>
### Nested Schema for `storage_static_routes.storage_routes.subnets`

Optional:

- `ipv4` (Block, Optional) IPv4 Subnet. IPv4 subnets specified as prefix and prefix-length. Prefix length must be <= 32 (see [below for nested schema](#nestedblock--storage_static_routes--storage_routes--subnets--ipv4))
- `ipv6` (Block, Optional) IPv6 Subnet. IPv6 subnets specified as prefix and prefix-length. prefix-legnth must be <= 128 (see [below for nested schema](#nestedblock--storage_static_routes--storage_routes--subnets--ipv6))

<a id="nestedblock--storage_static_routes--storage_routes--subnets--ipv4"></a>
### Nested Schema for `storage_static_routes.storage_routes.subnets.ipv4`


<a id="nestedblock--storage_static_routes--storage_routes--subnets--ipv6"></a>
### Nested Schema for `storage_static_routes.storage_routes.subnets.ipv6`





<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).


<a id="nestedblock--usb_policy"></a>
### Nested Schema for `usb_policy`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_fleet.example system/example
```
