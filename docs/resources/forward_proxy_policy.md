---
page_title: "f5xc_forward_proxy_policy Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages a ForwardProxyPolicy resource in F5 Distributed Cloud for forward proxy policy configuration.
---

# f5xc_forward_proxy_policy (Resource)

Manages a ForwardProxyPolicy resource in F5 Distributed Cloud for forward proxy policy configuration.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Forward Proxy Policy Resource Example
# Manages a ForwardProxyPolicy resource in F5 Distributed Cloud for forward proxy policy configuration.

# Basic Forward Proxy Policy configuration
resource "f5xc_forward_proxy_policy" "example" {
  name      = "example-forward-proxy-policy"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Forward Proxy Policy configuration
  proxy_label_selector {
    expressions = ["app in (web, api)"]
  }

  drp_http_connect {
    any_proxy {}
    rule_list {
      rules {
        metadata {
          name = "allow-external"
        }
        spec {
          action = "ALLOW"
          dst_list {
            any_dst {}
          }
        }
      }
    }
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the ForwardProxyPolicy. Must be unique within the namespace.
- `namespace` (String) Namespace where the ForwardProxyPolicy will be created.

### Optional

- `allow_all` (Block, Optional) [OneOf: allow_all, allow_list, deny_list, rule_list] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--allow_all))
- `allow_list` (Block, Optional) Forward Proxy Rule. URL(s) and domains policy for forward proxy for a connection type (TLS or HTTP) (see [below for nested schema](#nestedblock--allow_list))
- `annotations` (Map of String) Annotations to apply to this resource.
- `any_proxy` (Block, Optional) [OneOf: any_proxy, drp_http_connect, network_connector, proxy_label_selector] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--any_proxy))
- `deny_list` (Block, Optional) Forward Proxy Rule. URL(s) and domains policy for forward proxy for a connection type (TLS or HTTP) (see [below for nested schema](#nestedblock--deny_list))
- `drp_http_connect` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--drp_http_connect))
- `labels` (Map of String) Labels to apply to this resource.
- `network_connector` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--network_connector))
- `proxy_label_selector` (Block, Optional) Label Selector. This type can be used to establish a 'selector reference' from one object(called selector) to a set of other objects(called selectees) based on the value of expresssions. A label selector is a label query over a set of resources. An empty label selector matches all objects. A null label selector matches no objects. Label selector is immutable. expressions is a list of strings of label selection expression. Each string has ',' separated values which are 'AND' and all strings ar... (see [below for nested schema](#nestedblock--proxy_label_selector))
- `rule_list` (Block, Optional) Custom Rule List. List of custom rules (see [below for nested schema](#nestedblock--rule_list))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--allow_all"></a>
### Nested Schema for `allow_all`


<a id="nestedblock--allow_list"></a>
### Nested Schema for `allow_list`

Optional:

- `default_action_allow` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--allow_list--default_action_allow))
- `default_action_deny` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--allow_list--default_action_deny))
- `default_action_next_policy` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--allow_list--default_action_next_policy))
- `dest_list` (Block List) L4 Destination List. L4 destinations for non-HTTP and non-TLS connections and TLS connections without SNI (see [below for nested schema](#nestedblock--allow_list--dest_list))
- `http_list` (Block List) HTTP URLs. URLs for HTTP connections (see [below for nested schema](#nestedblock--allow_list--http_list))
- `tls_list` (Block List) TLS Domains. Domains in SNI for TLS connections (see [below for nested schema](#nestedblock--allow_list--tls_list))

<a id="nestedblock--allow_list--default_action_allow"></a>
### Nested Schema for `allow_list.default_action_allow`


<a id="nestedblock--allow_list--default_action_deny"></a>
### Nested Schema for `allow_list.default_action_deny`


<a id="nestedblock--allow_list--default_action_next_policy"></a>
### Nested Schema for `allow_list.default_action_next_policy`


<a id="nestedblock--allow_list--dest_list"></a>
### Nested Schema for `allow_list.dest_list`

Optional:

- `ipv6_prefixes` (List of String) IPv6 Prefixes. Destination IPv6 prefixes.
- `port_ranges` (String) Port Ranges. A string containing a comma separated list of port ranges. Each port range consists of a single port or two ports separated by '-'.
- `prefixes` (List of String) IPv4 Prefixes. Destination IPv4 prefixes.


<a id="nestedblock--allow_list--http_list"></a>
### Nested Schema for `allow_list.http_list`

Optional:

- `any_path` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--allow_list--http_list--any_path))
- `exact_value` (String) Exact Values. Exact domain name
- `path_exact_value` (String) Exact Path. Exact Path to match.
- `path_prefix_value` (String) Prefix of Path. Prefix of Path e.g '/abc/xyz' will match '/abc/xyz/.*'
- `path_regex_value` (String) Regex of Path. Regular Expression value for the Path to match
- `regex_value` (String) Regex Values of Domains. Regular Expression value for the domain name
- `suffix_value` (String) Suffix Values. Suffix of domain names e.g 'xyz.com' will match '*.xyz.com'

<a id="nestedblock--allow_list--http_list--any_path"></a>
### Nested Schema for `allow_list.http_list.any_path`



<a id="nestedblock--allow_list--tls_list"></a>
### Nested Schema for `allow_list.tls_list`

Optional:

- `exact_value` (String) Exact Value. Exact domain name.
- `regex_value` (String) Regex Values of Domains. Regular Expression value for the domain name
- `suffix_value` (String) Suffix Value. Suffix of domain name e.g 'xyz.com' will match '*.xyz.com' and 'xyz.com'



<a id="nestedblock--any_proxy"></a>
### Nested Schema for `any_proxy`


<a id="nestedblock--deny_list"></a>
### Nested Schema for `deny_list`

Optional:

- `default_action_allow` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--deny_list--default_action_allow))
- `default_action_deny` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--deny_list--default_action_deny))
- `default_action_next_policy` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--deny_list--default_action_next_policy))
- `dest_list` (Block List) L4 Destination List. L4 destinations for non-HTTP and non-TLS connections and TLS connections without SNI (see [below for nested schema](#nestedblock--deny_list--dest_list))
- `http_list` (Block List) HTTP URLs. URLs for HTTP connections (see [below for nested schema](#nestedblock--deny_list--http_list))
- `tls_list` (Block List) TLS Domains. Domains in SNI for TLS connections (see [below for nested schema](#nestedblock--deny_list--tls_list))

<a id="nestedblock--deny_list--default_action_allow"></a>
### Nested Schema for `deny_list.default_action_allow`


<a id="nestedblock--deny_list--default_action_deny"></a>
### Nested Schema for `deny_list.default_action_deny`


<a id="nestedblock--deny_list--default_action_next_policy"></a>
### Nested Schema for `deny_list.default_action_next_policy`


<a id="nestedblock--deny_list--dest_list"></a>
### Nested Schema for `deny_list.dest_list`

Optional:

- `ipv6_prefixes` (List of String) IPv6 Prefixes. Destination IPv6 prefixes.
- `port_ranges` (String) Port Ranges. A string containing a comma separated list of port ranges. Each port range consists of a single port or two ports separated by '-'.
- `prefixes` (List of String) IPv4 Prefixes. Destination IPv4 prefixes.


<a id="nestedblock--deny_list--http_list"></a>
### Nested Schema for `deny_list.http_list`

Optional:

- `any_path` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--deny_list--http_list--any_path))
- `exact_value` (String) Exact Values. Exact domain name
- `path_exact_value` (String) Exact Path. Exact Path to match.
- `path_prefix_value` (String) Prefix of Path. Prefix of Path e.g '/abc/xyz' will match '/abc/xyz/.*'
- `path_regex_value` (String) Regex of Path. Regular Expression value for the Path to match
- `regex_value` (String) Regex Values of Domains. Regular Expression value for the domain name
- `suffix_value` (String) Suffix Values. Suffix of domain names e.g 'xyz.com' will match '*.xyz.com'

<a id="nestedblock--deny_list--http_list--any_path"></a>
### Nested Schema for `deny_list.http_list.any_path`



<a id="nestedblock--deny_list--tls_list"></a>
### Nested Schema for `deny_list.tls_list`

Optional:

- `exact_value` (String) Exact Value. Exact domain name.
- `regex_value` (String) Regex Values of Domains. Regular Expression value for the domain name
- `suffix_value` (String) Suffix Value. Suffix of domain name e.g 'xyz.com' will match '*.xyz.com' and 'xyz.com'



<a id="nestedblock--drp_http_connect"></a>
### Nested Schema for `drp_http_connect`


<a id="nestedblock--network_connector"></a>
### Nested Schema for `network_connector`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--proxy_label_selector"></a>
### Nested Schema for `proxy_label_selector`

Optional:

- `expressions` (List of String) Selector Expression. expressions contains the kubernetes style label expression for selections.


<a id="nestedblock--rule_list"></a>
### Nested Schema for `rule_list`

Optional:

- `rules` (Block List) Custom Rule List. List of custom rules (see [below for nested schema](#nestedblock--rule_list--rules))

<a id="nestedblock--rule_list--rules"></a>
### Nested Schema for `rule_list.rules`

Optional:

- `action` (String) Rule Action. The rule action determines the disposition of the input request API. If a policy matches a rule with an ALLOW action, the processing of the request proceeds forward. If it matches a rule with a DENY action, the processing of the request is terminated and an appropriate message/code returned to the originator. If it matches a rule with a NEXT_POLICY_SET action, evaluation of the current policy set terminates and evaluation of the next policy set in the chain begins. - DENY: DENY D... Possible values are `DENY`, `ALLOW`, `NEXT_POLICY`.
- `all_destinations` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--rule_list--rules--all_destinations))
- `all_sources` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--rule_list--rules--all_sources))
- `dst_asn_list` (Block, Optional) ASN Match List. An unordered set of RFC 6793 defined 4-byte AS numbers that can be used to create allow or deny lists for use in network policy or service policy. It can be used to create the allow list only for DNS Load Balancer. (see [below for nested schema](#nestedblock--rule_list--rules--dst_asn_list))
- `dst_asn_set` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--rule_list--rules--dst_asn_set))
- `dst_ip_prefix_set` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--rule_list--rules--dst_ip_prefix_set))
- `dst_label_selector` (Block, Optional) Label Selector. This type can be used to establish a 'selector reference' from one object(called selector) to a set of other objects(called selectees) based on the value of expresssions. A label selector is a label query over a set of resources. An empty label selector matches all objects. A null label selector matches no objects. Label selector is immutable. expressions is a list of strings of label selection expression. Each string has ',' separated values which are 'AND' and all strings ar... (see [below for nested schema](#nestedblock--rule_list--rules--dst_label_selector))
- `dst_prefix_list` (Block, Optional) IPv4 Prefix List. x-example: '192.168.20.0/24' List of IPv4 prefixes that represent an endpoint (see [below for nested schema](#nestedblock--rule_list--rules--dst_prefix_list))
- `http_list` (Block, Optional) URLListType. (see [below for nested schema](#nestedblock--rule_list--rules--http_list))
- `ip_prefix_set` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--rule_list--rules--ip_prefix_set))
- `label_selector` (Block, Optional) Label Selector. This type can be used to establish a 'selector reference' from one object(called selector) to a set of other objects(called selectees) based on the value of expresssions. A label selector is a label query over a set of resources. An empty label selector matches all objects. A null label selector matches no objects. Label selector is immutable. expressions is a list of strings of label selection expression. Each string has ',' separated values which are 'AND' and all strings ar... (see [below for nested schema](#nestedblock--rule_list--rules--label_selector))
- `metadata` (Block, Optional) Message Metadata. MessageMetaType is metadata (common attributes) of a message that only certain messages have. This information is propagated to the metadata of a child object that gets created from the containing message during view processing. The information in this type can be specified by user during create and replace APIs. (see [below for nested schema](#nestedblock--rule_list--rules--metadata))
- `no_http_connect_port` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--rule_list--rules--no_http_connect_port))
- `port_matcher` (Block, Optional) Port Matcher. A port matcher specifies a list of port ranges as match criteria. The match is considered successful if the input port falls within any of the port ranges. The result of the match is inverted if invert_matcher is true. (see [below for nested schema](#nestedblock--rule_list--rules--port_matcher))
- `prefix_list` (Block, Optional) IPv4 Prefix List. x-example: '192.168.20.0/24' List of IPv4 prefixes that represent an endpoint (see [below for nested schema](#nestedblock--rule_list--rules--prefix_list))
- `tls_list` (Block, Optional) DomainListType. (see [below for nested schema](#nestedblock--rule_list--rules--tls_list))
- `url_category_list` (Block, Optional) URL Category List Type. List of url categories (see [below for nested schema](#nestedblock--rule_list--rules--url_category_list))

<a id="nestedblock--rule_list--rules--all_destinations"></a>
### Nested Schema for `rule_list.rules.all_destinations`


<a id="nestedblock--rule_list--rules--all_sources"></a>
### Nested Schema for `rule_list.rules.all_sources`


<a id="nestedblock--rule_list--rules--dst_asn_list"></a>
### Nested Schema for `rule_list.rules.dst_asn_list`

Optional:

- `as_numbers` (List of String) AS Numbers. An unordered set of RFC 6793 defined 4-byte AS numbers that can be used to create allow or deny lists for use in network policy or service policy. It can be used to create the allow list only for DNS Load Balancer.


<a id="nestedblock--rule_list--rules--dst_asn_set"></a>
### Nested Schema for `rule_list.rules.dst_asn_set`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--rule_list--rules--dst_ip_prefix_set"></a>
### Nested Schema for `rule_list.rules.dst_ip_prefix_set`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--rule_list--rules--dst_label_selector"></a>
### Nested Schema for `rule_list.rules.dst_label_selector`

Optional:

- `expressions` (List of String) Selector Expression. expressions contains the kubernetes style label expression for selections.


<a id="nestedblock--rule_list--rules--dst_prefix_list"></a>
### Nested Schema for `rule_list.rules.dst_prefix_list`

Optional:

- `prefixes` (List of String) IPv4 Prefix List. List of IPv4 prefixes that represent an endpoint


<a id="nestedblock--rule_list--rules--http_list"></a>
### Nested Schema for `rule_list.rules.http_list`

Optional:

- `http_list` (Block List) HTTP URLs. URLs for HTTP connections (see [below for nested schema](#nestedblock--rule_list--rules--http_list--http_list))

<a id="nestedblock--rule_list--rules--http_list--http_list"></a>
### Nested Schema for `rule_list.rules.http_list.http_list`



<a id="nestedblock--rule_list--rules--ip_prefix_set"></a>
### Nested Schema for `rule_list.rules.ip_prefix_set`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--rule_list--rules--label_selector"></a>
### Nested Schema for `rule_list.rules.label_selector`

Optional:

- `expressions` (List of String) Selector Expression. expressions contains the kubernetes style label expression for selections.


<a id="nestedblock--rule_list--rules--metadata"></a>
### Nested Schema for `rule_list.rules.metadata`

Optional:

- `description` (String) Description. Human readable description.
- `name` (String) Name. This is the name of the message. The value of name has to follow DNS-1035 format.


<a id="nestedblock--rule_list--rules--no_http_connect_port"></a>
### Nested Schema for `rule_list.rules.no_http_connect_port`


<a id="nestedblock--rule_list--rules--port_matcher"></a>
### Nested Schema for `rule_list.rules.port_matcher`

Optional:

- `invert_matcher` (Boolean) Invert Port Matcher. Invert the match result.
- `ports` (List of String) Port Ranges. A list of strings, each of which is a single port value or a tuple of start and end port values separated by '-'. The start and end values are considered to be part of the range.


<a id="nestedblock--rule_list--rules--prefix_list"></a>
### Nested Schema for `rule_list.rules.prefix_list`

Optional:

- `prefixes` (List of String) IPv4 Prefix List. List of IPv4 prefixes that represent an endpoint


<a id="nestedblock--rule_list--rules--tls_list"></a>
### Nested Schema for `rule_list.rules.tls_list`

Optional:

- `tls_list` (Block List) TLS Domains. Domains in SNI for TLS connections (see [below for nested schema](#nestedblock--rule_list--rules--tls_list--tls_list))

<a id="nestedblock--rule_list--rules--tls_list--tls_list"></a>
### Nested Schema for `rule_list.rules.tls_list.tls_list`



<a id="nestedblock--rule_list--rules--url_category_list"></a>
### Nested Schema for `rule_list.rules.url_category_list`

Optional:

- `url_categories` (List of String) URL Categories. List of url categories to be selected




<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_forward_proxy_policy.example system/example
```
