---
page_title: "f5xc_ike_phase1_profile Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages a IKEPhase1Profile resource in F5 Distributed Cloud for ike phase1 profile configuration.
---

# f5xc_ike_phase1_profile (Resource)

Manages a IKEPhase1Profile resource in F5 Distributed Cloud for ike phase1 profile configuration.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Ike Phase1 Profile Resource Example
# Manages a IKEPhase1Profile resource in F5 Distributed Cloud for ike phase1 profile configuration.

# Basic Ike Phase1 Profile configuration
resource "f5xc_ike_phase1_profile" "example" {
  name      = "example-ike-phase1-profile"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
    # [OneOf: ike_keylifetime_hours, ike_keylifetime_minutes, u...
    ike_keylifetime_hours {
      # Configure ike_keylifetime_hours settings
    }
    # Minutes. Set IKE Key Lifetime in minutes
    ike_keylifetime_minutes {
      # Configure ike_keylifetime_minutes settings
    }
    # [OneOf: reauth_disabled, reauth_timeout_days, reauth_time...
    reauth_disabled {
      # Configure reauth_disabled settings
    }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the IKEPhase1Profile. Must be unique within the namespace.
- `namespace` (String) Namespace where the IKEPhase1Profile will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `authentication_algos` (List of String) Authentication Algorithms. Choose one or more Authentication Algorithm. Use None option when using the aes-gcm or aes-ccm encryption algorithms.
- `dh_group` (List of String) Diffie Hellman Groups. Choose the acceptable Diffie Hellman (DH) Group or Groups that you are willing to accept as part of this profile.
- `encryption_algos` (List of String) Configure Encryption Algorithms. Choose one or more encryption algorithms.
- `ike_keylifetime_hours` (Block, Optional) [OneOf: ike_keylifetime_hours, ike_keylifetime_minutes, use_default_keylifetime] Hours. Input Hours (see [below for nested schema](#nestedblock--ike_keylifetime_hours))
- `ike_keylifetime_minutes` (Block, Optional) Minutes. Set IKE Key Lifetime in minutes (see [below for nested schema](#nestedblock--ike_keylifetime_minutes))
- `labels` (Map of String) Labels to apply to this resource.
- `prf` (List of String) PseudoRandomFunction. Select PseudoRandomFunction for IKE SA.
- `reauth_disabled` (Block, Optional) [OneOf: reauth_disabled, reauth_timeout_days, reauth_timeout_hours] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--reauth_disabled))
- `reauth_timeout_days` (Block, Optional) Days. Set Duration in days (see [below for nested schema](#nestedblock--reauth_timeout_days))
- `reauth_timeout_hours` (Block, Optional) Hours. Input Hours (see [below for nested schema](#nestedblock--reauth_timeout_hours))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `use_default_keylifetime` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--use_default_keylifetime))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--ike_keylifetime_hours"></a>
### Nested Schema for `ike_keylifetime_hours`

Optional:

- `duration` (Number) Duration.


<a id="nestedblock--ike_keylifetime_minutes"></a>
### Nested Schema for `ike_keylifetime_minutes`

Optional:

- `duration` (Number) Duration.


<a id="nestedblock--reauth_disabled"></a>
### Nested Schema for `reauth_disabled`


<a id="nestedblock--reauth_timeout_days"></a>
### Nested Schema for `reauth_timeout_days`

Optional:

- `duration` (Number) Duration.


<a id="nestedblock--reauth_timeout_hours"></a>
### Nested Schema for `reauth_timeout_hours`

Optional:

- `duration` (Number) Duration.


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).


<a id="nestedblock--use_default_keylifetime"></a>
### Nested Schema for `use_default_keylifetime`

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_ike_phase1_profile.example system/example
```
