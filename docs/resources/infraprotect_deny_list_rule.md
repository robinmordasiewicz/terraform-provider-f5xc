---
page_title: "f5xc_infraprotect_deny_list_rule Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages DDoS transit Deny List Rule in F5 Distributed Cloud.
---

# f5xc_infraprotect_deny_list_rule (Resource)

Manages DDoS transit Deny List Rule in F5 Distributed Cloud.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Infraprotect Deny List Rule Resource Example
# Manages DDoS transit Deny List Rule in F5 Distributed Cloud.

# Basic Infraprotect Deny List Rule configuration
resource "f5xc_infraprotect_deny_list_rule" "example" {
  name      = "example-infraprotect-deny-list-rule"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
    # [OneOf: expiration_never, expiration_timestamp, one_day, ...
    expiration_never {
      # Configure expiration_never settings
    }
    # Empty. This can be used for messages where no values are ...
    one_day {
      # Configure one_day settings
    }
    # Empty. This can be used for messages where no values are ...
    one_hour {
      # Configure one_hour settings
    }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the InfraprotectDenyListRule. Must be unique within the namespace.
- `namespace` (String) Namespace where the InfraprotectDenyListRule will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `expiration_never` (Block, Optional) [OneOf: expiration_never, expiration_timestamp, one_day, one_hour, one_month, one_year] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--expiration_never))
- `expiration_timestamp` (String) Expiration Time (UTC). This deny list rule will expire at the given timestamp and will be removed from the system afterwards
- `labels` (Map of String) Labels to apply to this resource.
- `one_day` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--one_day))
- `one_hour` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--one_hour))
- `one_month` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--one_month))
- `one_year` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--one_year))
- `prefix` (String) Prefix. Prefix
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--expiration_never"></a>
### Nested Schema for `expiration_never`


<a id="nestedblock--one_day"></a>
### Nested Schema for `one_day`


<a id="nestedblock--one_hour"></a>
### Nested Schema for `one_hour`


<a id="nestedblock--one_month"></a>
### Nested Schema for `one_month`


<a id="nestedblock--one_year"></a>
### Nested Schema for `one_year`


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_infraprotect_deny_list_rule.example system/example
```
