---
page_title: "f5xc_infraprotect_tunnel Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages DDoS transit tunnel in F5 Distributed Cloud.
---

# f5xc_infraprotect_tunnel (Resource)

Manages DDoS transit tunnel in F5 Distributed Cloud.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Infraprotect Tunnel Resource Example
# Manages DDoS transit tunnel in F5 Distributed Cloud.

# Basic Infraprotect Tunnel configuration
resource "f5xc_infraprotect_tunnel" "example" {
  name      = "example-infraprotect-tunnel"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
  # Bandwidth Speed Configuration. Bandwidth max allowed
  bandwidth {
    # Configure bandwidth settings
  }
  # BGP. BGP information associated with a DDoS transit tunnel.
  bgp_information {
    # Configure bgp_information settings
  }
  # Object reference. This type establishes a direct referenc...
  asn {
    # Configure asn settings
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the InfraprotectTunnel. Must be unique within the namespace.
- `namespace` (String) Namespace where the InfraprotectTunnel will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `bandwidth` (Block, Optional) Bandwidth Speed Configuration. Bandwidth max allowed (see [below for nested schema](#nestedblock--bandwidth))
- `bgp_information` (Block, Optional) BGP. BGP information associated with a DDoS transit tunnel. (see [below for nested schema](#nestedblock--bgp_information))
- `firewall_rule_group` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--firewall_rule_group))
- `gre_ipv4` (Block, Optional) [OneOf: gre_ipv4, gre_ipv6, ip_in_ip, ipv6_to_ipv6] GRE IPv4 Tunnel. IPv4 Tunnel. (see [below for nested schema](#nestedblock--gre_ipv4))
- `gre_ipv6` (Block, Optional) GRE IPv6 Tunnel. IPv6 Tunnel. (see [below for nested schema](#nestedblock--gre_ipv6))
- `ip_in_ip` (Block, Optional) IP in IP Tunnel. IP in IP Tunnel. (see [below for nested schema](#nestedblock--ip_in_ip))
- `ipv6_to_ipv6` (Block, Optional) IPv6 to IPv6 Tunnel. IPv6 to IPv6 Tunnel. (see [below for nested schema](#nestedblock--ipv6_to_ipv6))
- `labels` (Map of String) Labels to apply to this resource.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `tunnel_location` (Block, Optional) Location. Location of a DDoS transit tunnel. (see [below for nested schema](#nestedblock--tunnel_location))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--bandwidth"></a>
### Nested Schema for `bandwidth`

Optional:

- `bandwidth_max_mb` (Number) Bandwidth Max in MB. Bandwidth max allowed


<a id="nestedblock--bgp_information"></a>
### Nested Schema for `bgp_information`

Optional:

- `asn` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--bgp_information--asn))
- `holddown_timer_seconds` (Number) Hold down Timer. BGP hold down timer, in seconds
- `no_secret` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--bgp_information--no_secret))
- `peer_secret_override` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--bgp_information--peer_secret_override))
- `use_default_secret` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--bgp_information--use_default_secret))

<a id="nestedblock--bgp_information--asn"></a>
### Nested Schema for `bgp_information.asn`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--bgp_information--no_secret"></a>
### Nested Schema for `bgp_information.no_secret`


<a id="nestedblock--bgp_information--peer_secret_override"></a>
### Nested Schema for `bgp_information.peer_secret_override`

Optional:

- `blindfold_secret_info` (Block, Optional) Blindfold Secret. BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management (see [below for nested schema](#nestedblock--bgp_information--peer_secret_override--blindfold_secret_info))
- `clear_secret_info` (Block, Optional) In-Clear Secret. ClearSecretInfoType specifies information about the Secret that is not encrypted. (see [below for nested schema](#nestedblock--bgp_information--peer_secret_override--clear_secret_info))

<a id="nestedblock--bgp_information--peer_secret_override--blindfold_secret_info"></a>
### Nested Schema for `bgp_information.peer_secret_override.blindfold_secret_info`

Optional:

- `decryption_provider` (String) Decryption Provider. Name of the Secret Management Access object that contains information about the backend Secret Management service.
- `location` (String) Location. Location is the uri_ref. It could be in url format for string:/// Or it could be a path if the store provider is an http/https location
- `store_provider` (String) Store Provider. Name of the Secret Management Access object that contains information about the store to get encrypted bytes This field needs to be provided only if the url scheme is not string:///


<a id="nestedblock--bgp_information--peer_secret_override--clear_secret_info"></a>
### Nested Schema for `bgp_information.peer_secret_override.clear_secret_info`

Optional:

- `provider_ref` (String) Provider. Name of the Secret Management Access object that contains information about the store to get encrypted bytes This field needs to be provided only if the url scheme is not string:///
- `url` (String) URL. URL of the secret. Currently supported URL schemes is string:///. For string:/// scheme, Secret needs to be encoded Base64 format. When asked for this secret, caller will get Secret bytes after Base64 decoding.



<a id="nestedblock--bgp_information--use_default_secret"></a>
### Nested Schema for `bgp_information.use_default_secret`



<a id="nestedblock--firewall_rule_group"></a>
### Nested Schema for `firewall_rule_group`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--gre_ipv4"></a>
### Nested Schema for `gre_ipv4`

Optional:

- `customer_endpoint_ipv4` (String) Customer Endpoint IP. IPv4 address for the customer endpoint of the tunnel
- `fragmentation_disabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--gre_ipv4--fragmentation_disabled))
- `fragmentation_enabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--gre_ipv4--fragmentation_enabled))
- `ipv6_interconnect_disabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--gre_ipv4--ipv6_interconnect_disabled))
- `ipv6_interconnect_enabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--gre_ipv4--ipv6_interconnect_enabled))
- `keepalive_disabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--gre_ipv4--keepalive_disabled))
- `keepalive_enabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--gre_ipv4--keepalive_enabled))

<a id="nestedblock--gre_ipv4--fragmentation_disabled"></a>
### Nested Schema for `gre_ipv4.fragmentation_disabled`


<a id="nestedblock--gre_ipv4--fragmentation_enabled"></a>
### Nested Schema for `gre_ipv4.fragmentation_enabled`


<a id="nestedblock--gre_ipv4--ipv6_interconnect_disabled"></a>
### Nested Schema for `gre_ipv4.ipv6_interconnect_disabled`


<a id="nestedblock--gre_ipv4--ipv6_interconnect_enabled"></a>
### Nested Schema for `gre_ipv4.ipv6_interconnect_enabled`


<a id="nestedblock--gre_ipv4--keepalive_disabled"></a>
### Nested Schema for `gre_ipv4.keepalive_disabled`


<a id="nestedblock--gre_ipv4--keepalive_enabled"></a>
### Nested Schema for `gre_ipv4.keepalive_enabled`



<a id="nestedblock--gre_ipv6"></a>
### Nested Schema for `gre_ipv6`

Optional:

- `customer_endpoint_ipv6` (String) Customer Endpoint IP. IPv6 address for the customer endpoint of the tunnel
- `ipv4_interconnect_disabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--gre_ipv6--ipv4_interconnect_disabled))
- `ipv4_interconnect_enabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--gre_ipv6--ipv4_interconnect_enabled))

<a id="nestedblock--gre_ipv6--ipv4_interconnect_disabled"></a>
### Nested Schema for `gre_ipv6.ipv4_interconnect_disabled`


<a id="nestedblock--gre_ipv6--ipv4_interconnect_enabled"></a>
### Nested Schema for `gre_ipv6.ipv4_interconnect_enabled`



<a id="nestedblock--ip_in_ip"></a>
### Nested Schema for `ip_in_ip`

Optional:

- `customer_endpoint_ipv4` (String) Customer Endpoint IP. IPv4 address for the customer endpoint of the tunnel


<a id="nestedblock--ipv6_to_ipv6"></a>
### Nested Schema for `ipv6_to_ipv6`

Optional:

- `customer_endpoint_ipv6` (String) Customer Endpoint IP. IPv6 address for the customer endpoint of the tunnel


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).


<a id="nestedblock--tunnel_location"></a>
### Nested Schema for `tunnel_location`

Optional:

- `name` (String) Location Name. Destination tunnel Location.
- `zone1` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tunnel_location--zone1))
- `zone2` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tunnel_location--zone2))

<a id="nestedblock--tunnel_location--zone1"></a>
### Nested Schema for `tunnel_location.zone1`


<a id="nestedblock--tunnel_location--zone2"></a>
### Nested Schema for `tunnel_location.zone2`

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_infraprotect_tunnel.example system/example
```
