---
page_title: "f5xc_network_firewall Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages a NetworkFirewall resource in F5 Distributed Cloud for network firewall is created by users in system namespace configuration.
---

# f5xc_network_firewall (Resource)

Manages a NetworkFirewall resource in F5 Distributed Cloud for network firewall is created by users in system namespace configuration.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Network Firewall Resource Example
# Manages a NetworkFirewall resource in F5 Distributed Cloud for network firewall is created by users in system namespace configuration.

# Basic Network Firewall configuration
resource "f5xc_network_firewall" "example" {
  name      = "example-network-firewall"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
  # [OneOf: active_enhanced_firewall_policies, active_network...
  active_enhanced_firewall_policies {
    # Configure active_enhanced_firewall_policies settings
  }
  # Enhanced Firewall Policy. Ordered List of Enhanced Firewa...
  enhanced_firewall_policies {
    # Configure enhanced_firewall_policies settings
  }
  # [OneOf: active_fast_acls, disable_fast_acl] Active Fast A...
  active_fast_acls {
    # Configure active_fast_acls settings
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the NetworkFirewall. Must be unique within the namespace.
- `namespace` (String) Namespace where the NetworkFirewall will be created.

### Optional

- `active_enhanced_firewall_policies` (Block, Optional) [OneOf: active_enhanced_firewall_policies, active_network_policies, disable_network_policy] Active Enhanced Network Policies Type. List of Enhanced Firewall Policies These policies use session-based rules and provide all options available under firewall policies with an additional option for service insertion. (see [below for nested schema](#nestedblock--active_enhanced_firewall_policies))
- `active_fast_acls` (Block, Optional) [OneOf: active_fast_acls, disable_fast_acl] Active Fast ACL(s). List of Fast ACL(s). (see [below for nested schema](#nestedblock--active_fast_acls))
- `active_forward_proxy_policies` (Block, Optional) [OneOf: active_forward_proxy_policies, disable_forward_proxy_policy] Active Forward Proxy Policies Type. Ordered List of Forward Proxy Policies active (see [below for nested schema](#nestedblock--active_forward_proxy_policies))
- `active_network_policies` (Block, Optional) Active Firewall Policies Type. List of firewall policy views. (see [below for nested schema](#nestedblock--active_network_policies))
- `annotations` (Map of String) Annotations to apply to this resource.
- `disable_fast_acl` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--disable_fast_acl))
- `disable_forward_proxy_policy` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--disable_forward_proxy_policy))
- `disable_network_policy` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--disable_network_policy))
- `labels` (Map of String) Labels to apply to this resource.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--active_enhanced_firewall_policies"></a>
### Nested Schema for `active_enhanced_firewall_policies`

Optional:

- `enhanced_firewall_policies` (Block List) Enhanced Firewall Policy. Ordered List of Enhanced Firewall Policies active (see [below for nested schema](#nestedblock--active_enhanced_firewall_policies--enhanced_firewall_policies))

<a id="nestedblock--active_enhanced_firewall_policies--enhanced_firewall_policies"></a>
### Nested Schema for `active_enhanced_firewall_policies.enhanced_firewall_policies`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.



<a id="nestedblock--active_fast_acls"></a>
### Nested Schema for `active_fast_acls`

Optional:

- `fast_acls` (Block List) Fast ACL(s). Ordered List of Fast ACL(s) active for this network firewall (see [below for nested schema](#nestedblock--active_fast_acls--fast_acls))

<a id="nestedblock--active_fast_acls--fast_acls"></a>
### Nested Schema for `active_fast_acls.fast_acls`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.



<a id="nestedblock--active_forward_proxy_policies"></a>
### Nested Schema for `active_forward_proxy_policies`

Optional:

- `forward_proxy_policies` (Block List) Forward Proxy Policies. Ordered List of Forward Proxy Policies active (see [below for nested schema](#nestedblock--active_forward_proxy_policies--forward_proxy_policies))

<a id="nestedblock--active_forward_proxy_policies--forward_proxy_policies"></a>
### Nested Schema for `active_forward_proxy_policies.forward_proxy_policies`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.



<a id="nestedblock--active_network_policies"></a>
### Nested Schema for `active_network_policies`

Optional:

- `network_policies` (Block List) Firewall Policy. Ordered List of Firewall Policies active for this network firewall (see [below for nested schema](#nestedblock--active_network_policies--network_policies))

<a id="nestedblock--active_network_policies--network_policies"></a>
### Nested Schema for `active_network_policies.network_policies`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.



<a id="nestedblock--disable_fast_acl"></a>
### Nested Schema for `disable_fast_acl`


<a id="nestedblock--disable_forward_proxy_policy"></a>
### Nested Schema for `disable_forward_proxy_policy`


<a id="nestedblock--disable_network_policy"></a>
### Nested Schema for `disable_network_policy`


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_network_firewall.example system/example
```
