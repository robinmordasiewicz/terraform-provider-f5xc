---
page_title: "f5xc_policy_based_routing Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages a PolicyBasedRouting resource in F5 Distributed Cloud for network policy based routing create configuration.
---

# f5xc_policy_based_routing (Resource)

Manages a PolicyBasedRouting resource in F5 Distributed Cloud for network policy based routing create configuration.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Policy Based Routing Resource Example
# Manages a PolicyBasedRouting resource in F5 Distributed Cloud for network policy based routing create configuration.

# Basic Policy Based Routing configuration
resource "f5xc_policy_based_routing" "example" {
  name      = "example-policy-based-routing"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
    # [OneOf: forward_proxy_pbr, network_pbr] L3/L4 routing rul...
    forward_proxy_pbr {
      # Configure forward_proxy_pbr settings
    }
    # L3/L4 routing rules. Network(L3/L4) routing policy rules.
    forward_proxy_pbr_rules {
      # Configure forward_proxy_pbr_rules settings
    }
    # Empty. This can be used for messages where no values are ...
    all_destinations {
      # Configure all_destinations settings
    }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the PolicyBasedRouting. Must be unique within the namespace.
- `namespace` (String) Namespace where the PolicyBasedRouting will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `forward_proxy_pbr` (Block, Optional) [OneOf: forward_proxy_pbr, network_pbr] L3/L4 routing rule. Network(L3/L4) routing policy rule (see [below for nested schema](#nestedblock--forward_proxy_pbr))
- `forwarding_class_list` (Block List) Default Forwarding Classes. Ordered list of forwarding Class to be used if source application match and no rule match. (see [below for nested schema](#nestedblock--forwarding_class_list))
- `labels` (Map of String) Labels to apply to this resource.
- `network_pbr` (Block, Optional) Network (L2/L3) routing Policy. Network(L3/L4) routing policy rule (see [below for nested schema](#nestedblock--network_pbr))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--forward_proxy_pbr"></a>
### Nested Schema for `forward_proxy_pbr`

Optional:

- `forward_proxy_pbr_rules` (Block List) L3/L4 routing rules. Network(L3/L4) routing policy rules. (see [below for nested schema](#nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules))

<a id="nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules"></a>
### Nested Schema for `forward_proxy_pbr.forward_proxy_pbr_rules`

Optional:

- `all_destinations` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--all_destinations))
- `all_sources` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--all_sources))
- `forwarding_class_list` (Block List) Forwarding Class. Ordered list of forwarding Class to be used if no rule match. (see [below for nested schema](#nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--forwarding_class_list))
- `http_list` (Block, Optional) URLListType. (see [below for nested schema](#nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--http_list))
- `ip_prefix_set` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--ip_prefix_set))
- `label_selector` (Block, Optional) Label Selector. This type can be used to establish a 'selector reference' from one object(called selector) to a set of other objects(called selectees) based on the value of expresssions. A label selector is a label query over a set of resources. An empty label selector matches all objects. A null label selector matches no objects. Label selector is immutable. expressions is a list of strings of label selection expression. Each string has ',' separated values which are 'AND' and all strings ar... (see [below for nested schema](#nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--label_selector))
- `metadata` (Block, Optional) Message Metadata. MessageMetaType is metadata (common attributes) of a message that only certain messages have. This information is propagated to the metadata of a child object that gets created from the containing message during view processing. The information in this type can be specified by user during create and replace APIs. (see [below for nested schema](#nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--metadata))
- `prefix_list` (Block, Optional) IPv4 Prefix List. x-example: '192.168.20.0/24' List of IPv4 prefixes that represent an endpoint (see [below for nested schema](#nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--prefix_list))
- `tls_list` (Block, Optional) DomainListType. (see [below for nested schema](#nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--tls_list))

<a id="nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--all_destinations"></a>
### Nested Schema for `forward_proxy_pbr.forward_proxy_pbr_rules.all_destinations`


<a id="nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--all_sources"></a>
### Nested Schema for `forward_proxy_pbr.forward_proxy_pbr_rules.all_sources`


<a id="nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--forwarding_class_list"></a>
### Nested Schema for `forward_proxy_pbr.forward_proxy_pbr_rules.forwarding_class_list`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--http_list"></a>
### Nested Schema for `forward_proxy_pbr.forward_proxy_pbr_rules.http_list`

Optional:

- `http_list` (Block List) HTTP URLs. URLs for HTTP connections (see [below for nested schema](#nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--http_list--http_list))

<a id="nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--http_list--http_list"></a>
### Nested Schema for `forward_proxy_pbr.forward_proxy_pbr_rules.http_list.http_list`



<a id="nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--ip_prefix_set"></a>
### Nested Schema for `forward_proxy_pbr.forward_proxy_pbr_rules.ip_prefix_set`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--label_selector"></a>
### Nested Schema for `forward_proxy_pbr.forward_proxy_pbr_rules.label_selector`

Optional:

- `expressions` (List of String) Selector Expression. expressions contains the kubernetes style label expression for selections.


<a id="nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--metadata"></a>
### Nested Schema for `forward_proxy_pbr.forward_proxy_pbr_rules.metadata`

Optional:

- `description` (String) Description. Human readable description.
- `name` (String) Name. This is the name of the message. The value of name has to follow DNS-1035 format.


<a id="nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--prefix_list"></a>
### Nested Schema for `forward_proxy_pbr.forward_proxy_pbr_rules.prefix_list`

Optional:

- `prefixes` (List of String) IPv4 Prefix List. List of IPv4 prefixes that represent an endpoint


<a id="nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--tls_list"></a>
### Nested Schema for `forward_proxy_pbr.forward_proxy_pbr_rules.tls_list`

Optional:

- `tls_list` (Block List) TLS Domains. Domains in SNI for TLS connections (see [below for nested schema](#nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--tls_list--tls_list))

<a id="nestedblock--forward_proxy_pbr--forward_proxy_pbr_rules--tls_list--tls_list"></a>
### Nested Schema for `forward_proxy_pbr.forward_proxy_pbr_rules.tls_list.tls_list`





<a id="nestedblock--forwarding_class_list"></a>
### Nested Schema for `forwarding_class_list`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--network_pbr"></a>
### Nested Schema for `network_pbr`

Optional:

- `any` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--network_pbr--any))
- `label_selector` (Block, Optional) Label Selector. This type can be used to establish a 'selector reference' from one object(called selector) to a set of other objects(called selectees) based on the value of expresssions. A label selector is a label query over a set of resources. An empty label selector matches all objects. A null label selector matches no objects. Label selector is immutable. expressions is a list of strings of label selection expression. Each string has ',' separated values which are 'AND' and all strings ar... (see [below for nested schema](#nestedblock--network_pbr--label_selector))
- `network_pbr_rules` (Block List) L3/L4 Destination Routing Rules. Network(L3/L4) routing policy rule (see [below for nested schema](#nestedblock--network_pbr--network_pbr_rules))
- `prefix_list` (Block, Optional) IPv4 Prefix List. x-example: '192.168.20.0/24' List of IPv4 prefixes that represent an endpoint (see [below for nested schema](#nestedblock--network_pbr--prefix_list))

<a id="nestedblock--network_pbr--any"></a>
### Nested Schema for `network_pbr.any`


<a id="nestedblock--network_pbr--label_selector"></a>
### Nested Schema for `network_pbr.label_selector`

Optional:

- `expressions` (List of String) Selector Expression. expressions contains the kubernetes style label expression for selections.


<a id="nestedblock--network_pbr--network_pbr_rules"></a>
### Nested Schema for `network_pbr.network_pbr_rules`

Optional:

- `all_tcp_traffic` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--network_pbr--network_pbr_rules--all_tcp_traffic))
- `all_traffic` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--network_pbr--network_pbr_rules--all_traffic))
- `all_udp_traffic` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--network_pbr--network_pbr_rules--all_udp_traffic))
- `any` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--network_pbr--network_pbr_rules--any))
- `applications` (Block, Optional) Applications. Application protocols like HTTP, SNMP (see [below for nested schema](#nestedblock--network_pbr--network_pbr_rules--applications))
- `dns_name` (String) DNS Name to IP. Resolve hostname to get the IP
- `forwarding_class_list` (Block List) Forwarding Classes. Ordered list of forwarding Class to be used if rule match. (see [below for nested schema](#nestedblock--network_pbr--network_pbr_rules--forwarding_class_list))
- `ip_prefix_set` (Block, Optional) IP Prefix Set Reference. A list of references to ip_prefix_set objects. (see [below for nested schema](#nestedblock--network_pbr--network_pbr_rules--ip_prefix_set))
- `metadata` (Block, Optional) Message Metadata. MessageMetaType is metadata (common attributes) of a message that only certain messages have. This information is propagated to the metadata of a child object that gets created from the containing message during view processing. The information in this type can be specified by user during create and replace APIs. (see [below for nested schema](#nestedblock--network_pbr--network_pbr_rules--metadata))
- `prefix_list` (Block, Optional) IPv4 Prefix List. x-example: '192.168.20.0/24' List of IPv4 prefixes that represent an endpoint (see [below for nested schema](#nestedblock--network_pbr--network_pbr_rules--prefix_list))
- `protocol_port_range` (Block, Optional) Protocol and Port. Protocol and Port ranges (see [below for nested schema](#nestedblock--network_pbr--network_pbr_rules--protocol_port_range))

<a id="nestedblock--network_pbr--network_pbr_rules--all_tcp_traffic"></a>
### Nested Schema for `network_pbr.network_pbr_rules.all_tcp_traffic`


<a id="nestedblock--network_pbr--network_pbr_rules--all_traffic"></a>
### Nested Schema for `network_pbr.network_pbr_rules.all_traffic`


<a id="nestedblock--network_pbr--network_pbr_rules--all_udp_traffic"></a>
### Nested Schema for `network_pbr.network_pbr_rules.all_udp_traffic`


<a id="nestedblock--network_pbr--network_pbr_rules--any"></a>
### Nested Schema for `network_pbr.network_pbr_rules.any`


<a id="nestedblock--network_pbr--network_pbr_rules--applications"></a>
### Nested Schema for `network_pbr.network_pbr_rules.applications`

Optional:

- `applications` (List of String) Application Protocols. Application protocols like HTTP, SNMP


<a id="nestedblock--network_pbr--network_pbr_rules--forwarding_class_list"></a>
### Nested Schema for `network_pbr.network_pbr_rules.forwarding_class_list`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--network_pbr--network_pbr_rules--ip_prefix_set"></a>
### Nested Schema for `network_pbr.network_pbr_rules.ip_prefix_set`

Optional:

- `ref` (Block List) Reference. A list of references to ip_prefix_set objects. (see [below for nested schema](#nestedblock--network_pbr--network_pbr_rules--ip_prefix_set--ref))

<a id="nestedblock--network_pbr--network_pbr_rules--ip_prefix_set--ref"></a>
### Nested Schema for `network_pbr.network_pbr_rules.ip_prefix_set.ref`



<a id="nestedblock--network_pbr--network_pbr_rules--metadata"></a>
### Nested Schema for `network_pbr.network_pbr_rules.metadata`

Optional:

- `description` (String) Description. Human readable description.
- `name` (String) Name. This is the name of the message. The value of name has to follow DNS-1035 format.


<a id="nestedblock--network_pbr--network_pbr_rules--prefix_list"></a>
### Nested Schema for `network_pbr.network_pbr_rules.prefix_list`

Optional:

- `prefixes` (List of String) IPv4 Prefix List. List of IPv4 prefixes that represent an endpoint


<a id="nestedblock--network_pbr--network_pbr_rules--protocol_port_range"></a>
### Nested Schema for `network_pbr.network_pbr_rules.protocol_port_range`

Optional:

- `port_ranges` (List of String) List of Port Ranges. List of port ranges. Each range is a single port or a pair of start and end ports e.g. 8080-8192
- `protocol` (String) Protocol. Protocol in IP packet to be used as match criteria Values are tcp, udp, and icmp



<a id="nestedblock--network_pbr--prefix_list"></a>
### Nested Schema for `network_pbr.prefix_list`

Optional:

- `prefixes` (List of String) IPv4 Prefix List. List of IPv4 prefixes that represent an endpoint



<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_policy_based_routing.example system/example
```
