---
page_title: "f5xc_protocol_inspection Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages Protocol Inspection Specification in a given namespace. If one already exists it will give an error. in F5 Distributed Cloud.
---

# f5xc_protocol_inspection (Resource)

Manages Protocol Inspection Specification in a given namespace. If one already exists it will give an error. in F5 Distributed Cloud.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Protocol Inspection Resource Example
# Manages Protocol Inspection Specification in a given namespace. If one already exists it will give an error. in F5 Distributed Cloud.

# Basic Protocol Inspection configuration
resource "f5xc_protocol_inspection" "example" {
  name      = "example-protocol-inspection"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Resource-specific configuration
  # Enable/Disable Compliance Checks. x-required Enable Disab...
  enable_disable_compliance_checks {
    # Configure enable_disable_compliance_checks settings
  }
  # Empty. This can be used for messages where no values are ...
  disable_compliance_checks {
    # Configure disable_compliance_checks settings
  }
  # Object reference. This type establishes a direct referenc...
  enable_compliance_checks {
    # Configure enable_compliance_checks settings
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the ProtocolInspection. Must be unique within the namespace.
- `namespace` (String) Namespace where the ProtocolInspection will be created.

### Optional

- `action` (String) Action. Action after inspection - ALLOW: Allow Allow traffic - DENY: Deny Throw RST error for TCP and ICMP error for UDP - DROP: DROP Silently drop traffic. Possible values are `ALLOW`, `DENY`, `DROP`.
- `annotations` (Map of String) Annotations to apply to this resource.
- `enable_disable_compliance_checks` (Block, Optional) Enable/Disable Compliance Checks. x-required Enable Disable Compliance Checks Choice (see [below for nested schema](#nestedblock--enable_disable_compliance_checks))
- `enable_disable_signatures` (Block, Optional) Enable/Disable Signatures Choice. x-required Enable Disable Signature Choice (see [below for nested schema](#nestedblock--enable_disable_signatures))
- `labels` (Map of String) Labels to apply to this resource.
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--enable_disable_compliance_checks"></a>
### Nested Schema for `enable_disable_compliance_checks`

Optional:

- `disable_compliance_checks` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--enable_disable_compliance_checks--disable_compliance_checks))
- `enable_compliance_checks` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--enable_disable_compliance_checks--enable_compliance_checks))

<a id="nestedblock--enable_disable_compliance_checks--disable_compliance_checks"></a>
### Nested Schema for `enable_disable_compliance_checks.disable_compliance_checks`


<a id="nestedblock--enable_disable_compliance_checks--enable_compliance_checks"></a>
### Nested Schema for `enable_disable_compliance_checks.enable_compliance_checks`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.



<a id="nestedblock--enable_disable_signatures"></a>
### Nested Schema for `enable_disable_signatures`

Optional:

- `disable_signature` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--enable_disable_signatures--disable_signature))
- `enable_signature` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--enable_disable_signatures--enable_signature))

<a id="nestedblock--enable_disable_signatures--disable_signature"></a>
### Nested Schema for `enable_disable_signatures.disable_signature`


<a id="nestedblock--enable_disable_signatures--enable_signature"></a>
### Nested Schema for `enable_disable_signatures.enable_signature`



<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_protocol_inspection.example system/example
```
