---
page_title: "f5xc_service_policy Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages service_policy creates a new object in the storage backend for metadata.namespace. in F5 Distributed Cloud.
---

# f5xc_service_policy (Resource)

Manages service_policy creates a new object in the storage backend for metadata.namespace. in F5 Distributed Cloud.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Service Policy Resource Example
# Manages service_policy creates a new object in the storage backend for metadata.namespace. in F5 Distributed Cloud.

# Basic Service Policy configuration
resource "f5xc_service_policy" "example" {
  name      = "example-service-policy"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Service Policy configuration
  algo = "FIRST_MATCH"

  # Allow specific paths
  rules {
    metadata {
      name = "allow-api"
    }
    spec {
      action = "ALLOW"
      path {
        prefix = "/api/"
      }
    }
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the ServicePolicy. Must be unique within the namespace.
- `namespace` (String) Namespace where the ServicePolicy will be created.

### Optional

- `allow_all_requests` (Block, Optional) [OneOf: allow_all_requests, allow_list, deny_all_requests, deny_list, rule_list] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--allow_all_requests))
- `allow_list` (Block, Optional) Source List. List of sources. A request belongs to this list if it satisfies any of the match criteria. (see [below for nested schema](#nestedblock--allow_list))
- `annotations` (Map of String) Annotations to apply to this resource.
- `any_server` (Block, Optional) [OneOf: any_server, server_name, server_name_matcher, server_selector] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--any_server))
- `deny_all_requests` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--deny_all_requests))
- `deny_list` (Block, Optional) Source List. List of sources. A request belongs to this list if it satisfies any of the match criteria. (see [below for nested schema](#nestedblock--deny_list))
- `labels` (Map of String) Labels to apply to this resource.
- `rule_list` (Block, Optional) Rule List. A list of rules. The order of evaluation of the rules depends on the rule combining algorithm. (see [below for nested schema](#nestedblock--rule_list))
- `server_name` (String) Server Name. The expected name of the server to which the request API is directed. The actual names for the server are extracted from the HTTP Host header and the name of the virtual_host to which the request is directed. If the request is directed to a virtual K8s service, the actual names also contain the name of that service. The predicate evaluates to true if any of the actual names is the same as the expected server name.
- `server_name_matcher` (Block, Optional) Matcher. A matcher specifies multiple criteria for matching an input string. The match is considered successful if any of the criteria are satisfied. The set of supported match criteria includes a list of exact values and a list of regular expressions. (see [below for nested schema](#nestedblock--server_name_matcher))
- `server_selector` (Block, Optional) Label Selector. This type can be used to establish a 'selector reference' from one object(called selector) to a set of other objects(called selectees) based on the value of expresssions. A label selector is a label query over a set of resources. An empty label selector matches all objects. A null label selector matches no objects. Label selector is immutable. expressions is a list of strings of label selection expression. Each string has ',' separated values which are 'AND' and all strings ar... (see [below for nested schema](#nestedblock--server_selector))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--allow_all_requests"></a>
### Nested Schema for `allow_all_requests`


<a id="nestedblock--allow_list"></a>
### Nested Schema for `allow_list`

Optional:

- `asn_list` (Block, Optional) ASN Match List. An unordered set of RFC 6793 defined 4-byte AS numbers that can be used to create allow or deny lists for use in network policy or service policy. It can be used to create the allow list only for DNS Load Balancer. (see [below for nested schema](#nestedblock--allow_list--asn_list))
- `asn_set` (Block List) BGP ASN Set. Addresses that belong to the ASNs in the given bgp_asn_set The ASN is obtained by performing a lookup for the source IPv4 Address in a GeoIP DB. (see [below for nested schema](#nestedblock--allow_list--asn_set))
- `country_list` (List of String) Country List. Addresses that belong to one of the countries in the given list The country is obtained by performing a lookup for the source IPv4 Address in a GeoIP DB.
- `default_action_allow` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--allow_list--default_action_allow))
- `default_action_deny` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--allow_list--default_action_deny))
- `default_action_next_policy` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--allow_list--default_action_next_policy))
- `ip_prefix_set` (Block List) IP Prefix Set. Addresses that are covered by the prefixes in the given ip_prefix_set (see [below for nested schema](#nestedblock--allow_list--ip_prefix_set))
- `prefix_list` (Block, Optional) IPv4 Prefix List. x-example: '192.168.20.0/24' List of IPv4 prefixes that represent an endpoint (see [below for nested schema](#nestedblock--allow_list--prefix_list))
- `tls_fingerprint_classes` (List of String) TLS Fingerprint Classes. A list of known classes of TLS fingerprints to match the input TLS JA3 fingerprint against.
- `tls_fingerprint_values` (List of String) TLS Fingerprint Values. A list of exact TLS JA3 fingerprints to match the input TLS JA3 fingerprint against.

<a id="nestedblock--allow_list--asn_list"></a>
### Nested Schema for `allow_list.asn_list`

Optional:

- `as_numbers` (List of String) AS Numbers. An unordered set of RFC 6793 defined 4-byte AS numbers that can be used to create allow or deny lists for use in network policy or service policy. It can be used to create the allow list only for DNS Load Balancer.


<a id="nestedblock--allow_list--asn_set"></a>
### Nested Schema for `allow_list.asn_set`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--allow_list--default_action_allow"></a>
### Nested Schema for `allow_list.default_action_allow`


<a id="nestedblock--allow_list--default_action_deny"></a>
### Nested Schema for `allow_list.default_action_deny`


<a id="nestedblock--allow_list--default_action_next_policy"></a>
### Nested Schema for `allow_list.default_action_next_policy`


<a id="nestedblock--allow_list--ip_prefix_set"></a>
### Nested Schema for `allow_list.ip_prefix_set`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--allow_list--prefix_list"></a>
### Nested Schema for `allow_list.prefix_list`

Optional:

- `prefixes` (List of String) IPv4 Prefix List. List of IPv4 prefixes that represent an endpoint



<a id="nestedblock--any_server"></a>
### Nested Schema for `any_server`


<a id="nestedblock--deny_all_requests"></a>
### Nested Schema for `deny_all_requests`


<a id="nestedblock--deny_list"></a>
### Nested Schema for `deny_list`

Optional:

- `asn_list` (Block, Optional) ASN Match List. An unordered set of RFC 6793 defined 4-byte AS numbers that can be used to create allow or deny lists for use in network policy or service policy. It can be used to create the allow list only for DNS Load Balancer. (see [below for nested schema](#nestedblock--deny_list--asn_list))
- `asn_set` (Block List) BGP ASN Set. Addresses that belong to the ASNs in the given bgp_asn_set The ASN is obtained by performing a lookup for the source IPv4 Address in a GeoIP DB. (see [below for nested schema](#nestedblock--deny_list--asn_set))
- `country_list` (List of String) Country List. Addresses that belong to one of the countries in the given list The country is obtained by performing a lookup for the source IPv4 Address in a GeoIP DB.
- `default_action_allow` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--deny_list--default_action_allow))
- `default_action_deny` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--deny_list--default_action_deny))
- `default_action_next_policy` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--deny_list--default_action_next_policy))
- `ip_prefix_set` (Block List) IP Prefix Set. Addresses that are covered by the prefixes in the given ip_prefix_set (see [below for nested schema](#nestedblock--deny_list--ip_prefix_set))
- `prefix_list` (Block, Optional) IPv4 Prefix List. x-example: '192.168.20.0/24' List of IPv4 prefixes that represent an endpoint (see [below for nested schema](#nestedblock--deny_list--prefix_list))
- `tls_fingerprint_classes` (List of String) TLS Fingerprint Classes. A list of known classes of TLS fingerprints to match the input TLS JA3 fingerprint against.
- `tls_fingerprint_values` (List of String) TLS Fingerprint Values. A list of exact TLS JA3 fingerprints to match the input TLS JA3 fingerprint against.

<a id="nestedblock--deny_list--asn_list"></a>
### Nested Schema for `deny_list.asn_list`

Optional:

- `as_numbers` (List of String) AS Numbers. An unordered set of RFC 6793 defined 4-byte AS numbers that can be used to create allow or deny lists for use in network policy or service policy. It can be used to create the allow list only for DNS Load Balancer.


<a id="nestedblock--deny_list--asn_set"></a>
### Nested Schema for `deny_list.asn_set`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--deny_list--default_action_allow"></a>
### Nested Schema for `deny_list.default_action_allow`


<a id="nestedblock--deny_list--default_action_deny"></a>
### Nested Schema for `deny_list.default_action_deny`


<a id="nestedblock--deny_list--default_action_next_policy"></a>
### Nested Schema for `deny_list.default_action_next_policy`


<a id="nestedblock--deny_list--ip_prefix_set"></a>
### Nested Schema for `deny_list.ip_prefix_set`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--deny_list--prefix_list"></a>
### Nested Schema for `deny_list.prefix_list`

Optional:

- `prefixes` (List of String) IPv4 Prefix List. List of IPv4 prefixes that represent an endpoint



<a id="nestedblock--rule_list"></a>
### Nested Schema for `rule_list`

Optional:

- `rules` (Block List) Rules. Define the list of rules (with an order) that should be evaluated by this service policy. Rules are evaluated from top to bottom in the list. (see [below for nested schema](#nestedblock--rule_list--rules))

<a id="nestedblock--rule_list--rules"></a>
### Nested Schema for `rule_list.rules`

Optional:

- `metadata` (Block, Optional) Message Metadata. MessageMetaType is metadata (common attributes) of a message that only certain messages have. This information is propagated to the metadata of a child object that gets created from the containing message during view processing. The information in this type can be specified by user during create and replace APIs. (see [below for nested schema](#nestedblock--rule_list--rules--metadata))
- `spec` (Block, Optional) Specification. Shape of service_policy_rule in the storage backend. (see [below for nested schema](#nestedblock--rule_list--rules--spec))

<a id="nestedblock--rule_list--rules--metadata"></a>
### Nested Schema for `rule_list.rules.metadata`

Optional:

- `description` (String) Description. Human readable description.
- `name` (String) Name. This is the name of the message. The value of name has to follow DNS-1035 format.


<a id="nestedblock--rule_list--rules--spec"></a>
### Nested Schema for `rule_list.rules.spec`

Optional:

- `action` (String) Rule Action. The rule action determines the disposition of the input request API. If a policy matches a rule with an ALLOW action, the processing of the request proceeds forward. If it matches a rule with a DENY action, the processing of the request is terminated and an appropriate message/code returned to the originator. If it matches a rule with a NEXT_POLICY_SET action, evaluation of the current policy set terminates and evaluation of the next policy set in the chain begins. - DENY: DENY D... Possible values are `DENY`, `ALLOW`, `NEXT_POLICY`.
- `any_asn` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--rule_list--rules--spec--any_asn))
- `any_client` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--rule_list--rules--spec--any_client))
- `any_ip` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--rule_list--rules--spec--any_ip))
- `api_group_matcher` (Block, Optional) String Matcher. A matcher specifies a list of values for matching an input string. The match is considered successful if the input value is present in the list. The result of the match is inverted if invert_matcher is true. (see [below for nested schema](#nestedblock--rule_list--rules--spec--api_group_matcher))
- `arg_matchers` (Block List) Argument Matchers. A list of predicates for all POST args that need to be matched. The criteria for matching each arg are described in individual instances of ArgMatcherType. The actual arg values are extracted from the request API as a list of strings for each arg selector name. Note that all specified arg matcher predicates must evaluate to true. (see [below for nested schema](#nestedblock--rule_list--rules--spec--arg_matchers))
- `asn_list` (Block, Optional) ASN Match List. An unordered set of RFC 6793 defined 4-byte AS numbers that can be used to create allow or deny lists for use in network policy or service policy. It can be used to create the allow list only for DNS Load Balancer. (see [below for nested schema](#nestedblock--rule_list--rules--spec--asn_list))
- `asn_matcher` (Block, Optional) ASN Matcher. Match any AS number contained in the list of bgp_asn_sets. (see [below for nested schema](#nestedblock--rule_list--rules--spec--asn_matcher))
- `body_matcher` (Block, Optional) Matcher. A matcher specifies multiple criteria for matching an input string. The match is considered successful if any of the criteria are satisfied. The set of supported match criteria includes a list of exact values and a list of regular expressions. (see [below for nested schema](#nestedblock--rule_list--rules--spec--body_matcher))
- `bot_action` (Block, Optional) Bot Action. Modify Bot protection behavior for a matching request. The modification could be to entirely skip Bot processing. (see [below for nested schema](#nestedblock--rule_list--rules--spec--bot_action))
- `client_name` (String) Client Name. The expected name of the client invoking the request API. The predicate evaluates to true if any of the actual names is the same as the expected client name.
- `client_name_matcher` (Block, Optional) Matcher. A matcher specifies multiple criteria for matching an input string. The match is considered successful if any of the criteria are satisfied. The set of supported match criteria includes a list of exact values and a list of regular expressions. (see [below for nested schema](#nestedblock--rule_list--rules--spec--client_name_matcher))
- `client_selector` (Block, Optional) Label Selector. This type can be used to establish a 'selector reference' from one object(called selector) to a set of other objects(called selectees) based on the value of expresssions. A label selector is a label query over a set of resources. An empty label selector matches all objects. A null label selector matches no objects. Label selector is immutable. expressions is a list of strings of label selection expression. Each string has ',' separated values which are 'AND' and all strings ar... (see [below for nested schema](#nestedblock--rule_list--rules--spec--client_selector))
- `cookie_matchers` (Block List) Cookie Matchers. A list of predicates for all cookies that need to be matched. The criteria for matching each cookie is described in individual instances of CookieMatcherType. The actual cookie values are extracted from the request API as a list of strings for each cookie name. Note that all specified cookie matcher predicates must evaluate to true. (see [below for nested schema](#nestedblock--rule_list--rules--spec--cookie_matchers))
- `domain_matcher` (Block, Optional) Matcher. A matcher specifies multiple criteria for matching an input string. The match is considered successful if any of the criteria are satisfied. The set of supported match criteria includes a list of exact values and a list of regular expressions. (see [below for nested schema](#nestedblock--rule_list--rules--spec--domain_matcher))
- `expiration_timestamp` (String) Expiration Timestamp. The expiration_timestamp is the RFC 3339 format timestamp at which the containing rule is considered to be logically expired. The rule continues to exist in the configuration but is not applied anymore.
- `headers` (Block List) HTTP Headers. A list of predicates for various HTTP headers that need to match. The criteria for matching each HTTP header are described in individual HeaderMatcherType instances. The actual HTTP header values are extracted from the request API as a list of strings for each HTTP header type. Note that all specified header predicates must evaluate to true. (see [below for nested schema](#nestedblock--rule_list--rules--spec--headers))
- `http_method` (Block, Optional) HTTP Method Matcher. A http method matcher specifies a list of methods to match an input HTTP method. The match is considered successful if the input method is a member of the list. The result of the match based on the method list is inverted if invert_matcher is true. (see [below for nested schema](#nestedblock--rule_list--rules--spec--http_method))
- `ip_matcher` (Block, Optional) IP Prefix Matcher. Match any ip prefix contained in the list of ip_prefix_sets. The result of the match is inverted if invert_matcher is true. (see [below for nested schema](#nestedblock--rule_list--rules--spec--ip_matcher))
- `ip_prefix_list` (Block, Optional) IP Prefix Match List. List of IP Prefix strings to match against. (see [below for nested schema](#nestedblock--rule_list--rules--spec--ip_prefix_list))
- `ip_threat_category_list` (Block, Optional) IP Threat Category List Type. List of ip threat categories (see [below for nested schema](#nestedblock--rule_list--rules--spec--ip_threat_category_list))
- `ja4_tls_fingerprint` (Block, Optional) JA4 TLS Fingerprint Matcher. An extended version of JA3 that includes additional fields for more comprehensive fingerprinting of SSL/TLS clients and potentially has a different structure and length. (see [below for nested schema](#nestedblock--rule_list--rules--spec--ja4_tls_fingerprint))
- `jwt_claims` (Block List) JWT Claims. A list of predicates for various JWT claims that need to match. The criteria for matching each JWT claim are described in individual JWTClaimMatcherType instances. The actual JWT claims values are extracted from the JWT payload as a list of strings. Note that all specified JWT claim predicates must evaluate to true. (see [below for nested schema](#nestedblock--rule_list--rules--spec--jwt_claims))
- `label_matcher` (Block, Optional) Label Matcher. A label matcher specifies a list of label keys whose values need to match for source/client and destination/server. Note that the actual label values are not specified and do not matter. This allows an ability to scope grouping by the label key name. (see [below for nested schema](#nestedblock--rule_list--rules--spec--label_matcher))
- `mum_action` (Block, Optional) Select Modification Action. Modify behavior for a matching request. The modification could be to entirely skip processing. (see [below for nested schema](#nestedblock--rule_list--rules--spec--mum_action))
- `path` (Block, Optional) Path Matcher. A path matcher specifies multiple criteria for matching an HTTP path string. The match is considered successful if any of the criteria are satisfied. The set of supported match criteria includes a list of path prefixes, a list of exact path values and a list of regular expressions. (see [below for nested schema](#nestedblock--rule_list--rules--spec--path))
- `port_matcher` (Block, Optional) Port Matcher. A port matcher specifies a list of port ranges as match criteria. The match is considered successful if the input port falls within any of the port ranges. The result of the match is inverted if invert_matcher is true. (see [below for nested schema](#nestedblock--rule_list--rules--spec--port_matcher))
- `query_params` (Block List) HTTP Query Parameters. A list of predicates for all query parameters that need to be matched. The criteria for matching each query parameter are described in individual instances of QueryParameterMatcherType. The actual query parameter values are extracted from the request API as a list of strings for each query parameter name. Note that all specified query parameter predicates must evaluate to true. (see [below for nested schema](#nestedblock--rule_list--rules--spec--query_params))
- `request_constraints` (Block, Optional) Request Constraints. (see [below for nested schema](#nestedblock--rule_list--rules--spec--request_constraints))
- `segment_policy` (Block, Optional) Configure Segments. Configure source and destination segment for policy (see [below for nested schema](#nestedblock--rule_list--rules--spec--segment_policy))
- `tls_fingerprint_matcher` (Block, Optional) TLS Fingerprint Matcher. A TLS fingerprint matcher specifies multiple criteria for matching a TLS fingerprint. The set of supported positve match criteria includes a list of known classes of TLS fingerprints and a list of exact values. The match is considered successful if either of these positive criteria are satisfied and the input fingerprint is not one of the excluded values. (see [below for nested schema](#nestedblock--rule_list--rules--spec--tls_fingerprint_matcher))
- `user_identity_matcher` (Block, Optional) Matcher. A matcher specifies multiple criteria for matching an input string. The match is considered successful if any of the criteria are satisfied. The set of supported match criteria includes a list of exact values and a list of regular expressions. (see [below for nested schema](#nestedblock--rule_list--rules--spec--user_identity_matcher))
- `waf_action` (Block, Optional) App Firewall Action. Modify App Firewall behavior for a matching request. The modification could either be to entirely skip firewall processing or to customize the firewall rules to be applied as defined by App Firewall Rule Control settings. (see [below for nested schema](#nestedblock--rule_list--rules--spec--waf_action))

<a id="nestedblock--rule_list--rules--spec--any_asn"></a>
### Nested Schema for `rule_list.rules.spec.any_asn`


<a id="nestedblock--rule_list--rules--spec--any_client"></a>
### Nested Schema for `rule_list.rules.spec.any_client`


<a id="nestedblock--rule_list--rules--spec--any_ip"></a>
### Nested Schema for `rule_list.rules.spec.any_ip`


<a id="nestedblock--rule_list--rules--spec--api_group_matcher"></a>
### Nested Schema for `rule_list.rules.spec.api_group_matcher`


<a id="nestedblock--rule_list--rules--spec--arg_matchers"></a>
### Nested Schema for `rule_list.rules.spec.arg_matchers`


<a id="nestedblock--rule_list--rules--spec--asn_list"></a>
### Nested Schema for `rule_list.rules.spec.asn_list`


<a id="nestedblock--rule_list--rules--spec--asn_matcher"></a>
### Nested Schema for `rule_list.rules.spec.asn_matcher`


<a id="nestedblock--rule_list--rules--spec--body_matcher"></a>
### Nested Schema for `rule_list.rules.spec.body_matcher`


<a id="nestedblock--rule_list--rules--spec--bot_action"></a>
### Nested Schema for `rule_list.rules.spec.bot_action`


<a id="nestedblock--rule_list--rules--spec--client_name_matcher"></a>
### Nested Schema for `rule_list.rules.spec.client_name_matcher`


<a id="nestedblock--rule_list--rules--spec--client_selector"></a>
### Nested Schema for `rule_list.rules.spec.client_selector`


<a id="nestedblock--rule_list--rules--spec--cookie_matchers"></a>
### Nested Schema for `rule_list.rules.spec.cookie_matchers`


<a id="nestedblock--rule_list--rules--spec--domain_matcher"></a>
### Nested Schema for `rule_list.rules.spec.domain_matcher`


<a id="nestedblock--rule_list--rules--spec--headers"></a>
### Nested Schema for `rule_list.rules.spec.headers`


<a id="nestedblock--rule_list--rules--spec--http_method"></a>
### Nested Schema for `rule_list.rules.spec.http_method`


<a id="nestedblock--rule_list--rules--spec--ip_matcher"></a>
### Nested Schema for `rule_list.rules.spec.ip_matcher`


<a id="nestedblock--rule_list--rules--spec--ip_prefix_list"></a>
### Nested Schema for `rule_list.rules.spec.ip_prefix_list`


<a id="nestedblock--rule_list--rules--spec--ip_threat_category_list"></a>
### Nested Schema for `rule_list.rules.spec.ip_threat_category_list`


<a id="nestedblock--rule_list--rules--spec--ja4_tls_fingerprint"></a>
### Nested Schema for `rule_list.rules.spec.ja4_tls_fingerprint`


<a id="nestedblock--rule_list--rules--spec--jwt_claims"></a>
### Nested Schema for `rule_list.rules.spec.jwt_claims`


<a id="nestedblock--rule_list--rules--spec--label_matcher"></a>
### Nested Schema for `rule_list.rules.spec.label_matcher`


<a id="nestedblock--rule_list--rules--spec--mum_action"></a>
### Nested Schema for `rule_list.rules.spec.mum_action`


<a id="nestedblock--rule_list--rules--spec--path"></a>
### Nested Schema for `rule_list.rules.spec.path`


<a id="nestedblock--rule_list--rules--spec--port_matcher"></a>
### Nested Schema for `rule_list.rules.spec.port_matcher`


<a id="nestedblock--rule_list--rules--spec--query_params"></a>
### Nested Schema for `rule_list.rules.spec.query_params`


<a id="nestedblock--rule_list--rules--spec--request_constraints"></a>
### Nested Schema for `rule_list.rules.spec.request_constraints`


<a id="nestedblock--rule_list--rules--spec--segment_policy"></a>
### Nested Schema for `rule_list.rules.spec.segment_policy`


<a id="nestedblock--rule_list--rules--spec--tls_fingerprint_matcher"></a>
### Nested Schema for `rule_list.rules.spec.tls_fingerprint_matcher`


<a id="nestedblock--rule_list--rules--spec--user_identity_matcher"></a>
### Nested Schema for `rule_list.rules.spec.user_identity_matcher`


<a id="nestedblock--rule_list--rules--spec--waf_action"></a>
### Nested Schema for `rule_list.rules.spec.waf_action`





<a id="nestedblock--server_name_matcher"></a>
### Nested Schema for `server_name_matcher`

Optional:

- `exact_values` (List of String) Exact Values. A list of exact values to match the input against.
- `regex_values` (List of String) Regex Values. A list of regular expressions to match the input against.


<a id="nestedblock--server_selector"></a>
### Nested Schema for `server_selector`

Optional:

- `expressions` (List of String) Selector Expression. expressions contains the kubernetes style label expression for selections.


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_service_policy.example system/example
```
