---
page_title: "f5xc_site_mesh_group Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages Site Mesh Group in system namespace of user in F5 Distributed Cloud.
---

# f5xc_site_mesh_group (Resource)

Manages Site Mesh Group in system namespace of user in F5 Distributed Cloud.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Site Mesh Group Resource Example
# Manages Site Mesh Group in system namespace of user in F5 Distributed Cloud.

# Basic Site Mesh Group configuration
resource "f5xc_site_mesh_group" "example" {
  name      = "example-site-mesh-group"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Site Mesh Group configuration
  type = "SITE_MESH_GROUP_TYPE_FULL_MESH"

  # Control and data plane settings
  full_mesh {
    control_and_data_plane_mesh {}
  }

  # Hub status
  hub {}

  # Virtual site reference
  virtual_site {
    name      = "example-virtual-site"
    namespace = "system"
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the SiteMeshGroup. Must be unique within the namespace.
- `namespace` (String) Namespace where the SiteMeshGroup will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `disable_re_fallback` (Block, Optional) [OneOf: disable_re_fallback, enable_re_fallback] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--disable_re_fallback))
- `enable_re_fallback` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--enable_re_fallback))
- `full_mesh` (Block, Optional) [OneOf: full_mesh, hub_mesh, spoke_mesh] Full Mesh. Details of Full Mesh Group Type (see [below for nested schema](#nestedblock--full_mesh))
- `hub_mesh` (Block, Optional) Hub Full Mesh. Details of Hub Full Mesh Group Type (see [below for nested schema](#nestedblock--hub_mesh))
- `labels` (Map of String) Labels to apply to this resource.
- `spoke_mesh` (Block, Optional) Spoke. Details of Spoke Mesh Group Type (see [below for nested schema](#nestedblock--spoke_mesh))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `virtual_site` (Block List) Virtual Site (Sites in this group). Set of sites for which this mesh group config is valid. If 'Type' is Spoke, then it gives set of spoke sites. If 'Type' is Hub, then it gives set of hub sites. If 'Type' is Full Mesh, then it gives set of sites that are connected in full mesh. (see [below for nested schema](#nestedblock--virtual_site))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--disable_re_fallback"></a>
### Nested Schema for `disable_re_fallback`


<a id="nestedblock--enable_re_fallback"></a>
### Nested Schema for `enable_re_fallback`


<a id="nestedblock--full_mesh"></a>
### Nested Schema for `full_mesh`

Optional:

- `control_and_data_plane_mesh` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--full_mesh--control_and_data_plane_mesh))
- `data_plane_mesh` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--full_mesh--data_plane_mesh))

<a id="nestedblock--full_mesh--control_and_data_plane_mesh"></a>
### Nested Schema for `full_mesh.control_and_data_plane_mesh`


<a id="nestedblock--full_mesh--data_plane_mesh"></a>
### Nested Schema for `full_mesh.data_plane_mesh`



<a id="nestedblock--hub_mesh"></a>
### Nested Schema for `hub_mesh`

Optional:

- `control_and_data_plane_mesh` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--hub_mesh--control_and_data_plane_mesh))
- `data_plane_mesh` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--hub_mesh--data_plane_mesh))

<a id="nestedblock--hub_mesh--control_and_data_plane_mesh"></a>
### Nested Schema for `hub_mesh.control_and_data_plane_mesh`


<a id="nestedblock--hub_mesh--data_plane_mesh"></a>
### Nested Schema for `hub_mesh.data_plane_mesh`



<a id="nestedblock--spoke_mesh"></a>
### Nested Schema for `spoke_mesh`

Optional:

- `control_and_data_plane_mesh` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--spoke_mesh--control_and_data_plane_mesh))
- `data_plane_mesh` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--spoke_mesh--data_plane_mesh))
- `hub_mesh_group` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--spoke_mesh--hub_mesh_group))

<a id="nestedblock--spoke_mesh--control_and_data_plane_mesh"></a>
### Nested Schema for `spoke_mesh.control_and_data_plane_mesh`


<a id="nestedblock--spoke_mesh--data_plane_mesh"></a>
### Nested Schema for `spoke_mesh.data_plane_mesh`


<a id="nestedblock--spoke_mesh--hub_mesh_group"></a>
### Nested Schema for `spoke_mesh.hub_mesh_group`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.



<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).


<a id="nestedblock--virtual_site"></a>
### Nested Schema for `virtual_site`

Optional:

- `kind` (String) Kind. When a configuration object(e.g. virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')
- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.
- `uid` (String) UID. When a configuration object(e.g. virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. route's) uid.

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_site_mesh_group.example system/example
```
