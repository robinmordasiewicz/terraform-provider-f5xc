---
page_title: "f5xc_tcp_loadbalancer Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages a TCPLoadBalancer resource in F5 Distributed Cloud for load balancing TCP traffic across origin pools.
---

# f5xc_tcp_loadbalancer (Resource)

Manages a TCPLoadBalancer resource in F5 Distributed Cloud for load balancing TCP traffic across origin pools.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Tcp Loadbalancer Resource Example
# Manages a TCPLoadBalancer resource in F5 Distributed Cloud for load balancing TCP traffic across origin pools.

# Basic Tcp Loadbalancer configuration
resource "f5xc_tcp_loadbalancer" "example" {
  name      = "example-tcp-loadbalancer"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # TCP Load Balancer specific configuration
  listen_port = 8443

  # Advertise on public internet
  advertise_on_internet {
    default_vip {}
  }

  # Origin pools
  origin_pools_weights {
    pool {
      name      = "example-tcp-pool"
      namespace = "system"
    }
    weight = 1
  }

  # DNS for TCP load balancer
  dns_volterra_managed = true

  # No retract cluster by default
  retract_cluster {}
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the TCPLoadBalancer. Must be unique within the namespace.
- `namespace` (String) Namespace where the TCPLoadBalancer will be created.

### Optional

- `active_service_policies` (Block, Optional) [OneOf: active_service_policies, no_service_policies, service_policies_from_namespace] Service Policy List. List of service policies. (see [below for nested schema](#nestedblock--active_service_policies))
- `advertise_custom` (Block, Optional) [OneOf: advertise_custom, advertise_on_public, advertise_on_public_default_vip, do_not_advertise] Advertise Custom. This defines a way to advertise a VIP on specific sites (see [below for nested schema](#nestedblock--advertise_custom))
- `advertise_on_public` (Block, Optional) Advertise Public. This defines a way to advertise a load balancer on public. If optional public_ip is provided, it will only be advertised on RE sites where that public_ip is available (see [below for nested schema](#nestedblock--advertise_on_public))
- `advertise_on_public_default_vip` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--advertise_on_public_default_vip))
- `annotations` (Map of String) Annotations to apply to this resource.
- `default_lb_with_sni` (Block, Optional) [OneOf: default_lb_with_sni, no_sni, sni] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--default_lb_with_sni))
- `dns_volterra_managed` (Boolean) Automatically Manage DNS Records. DNS records for domains will be managed automatically by Volterra. This requires the domain to be delegated to F5XC using the Delegated Domain feature.
- `do_not_advertise` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--do_not_advertise))
- `do_not_retract_cluster` (Block, Optional) [OneOf: do_not_retract_cluster, retract_cluster] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--do_not_retract_cluster))
- `domains` (List of String) Domains. A list of Domains (host/authority header) that will be matched to this Load Balancer. Supported Domains and search order: 1. Exact Domain names: www.foo.com. 2. Domains starting with a Wildcard: *.foo.com. Not supported Domains: - Just a Wildcard: * - A Wildcard and TLD with no root Domain: *.com. - A Wildcard not matching a whole DNS label. e.g. *.foo.com and *.bar.foo.com are valid Wildcards however *bar.foo.com, *-bar.foo.com, and bar*.foo.com are all invalid. Additional notes: A ...
- `hash_policy_choice_least_active` (Block, Optional) [OneOf: hash_policy_choice_least_active, hash_policy_choice_random, hash_policy_choice_round_robin, hash_policy_choice_source_ip_stickiness] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--hash_policy_choice_least_active))
- `hash_policy_choice_random` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--hash_policy_choice_random))
- `hash_policy_choice_round_robin` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--hash_policy_choice_round_robin))
- `hash_policy_choice_source_ip_stickiness` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--hash_policy_choice_source_ip_stickiness))
- `idle_timeout` (Number) Idle Timeout. The amount of time that a stream can exist without upstream or downstream activity, in milliseconds.
- `labels` (Map of String) Labels to apply to this resource.
- `listen_port` (Number) [OneOf: listen_port, port_ranges] Listen Port. Listen Port for this load balancer
- `no_service_policies` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--no_service_policies))
- `no_sni` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--no_sni))
- `origin_pools_weights` (Block List) Origin Pools. Origin pools and weights used for this load balancer. (see [below for nested schema](#nestedblock--origin_pools_weights))
- `port_ranges` (String) Port Ranges. A string containing a comma separated list of port ranges. Each port range consists of a single port or two ports separated by '-'.
- `retract_cluster` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--retract_cluster))
- `service_policies_from_namespace` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--service_policies_from_namespace))
- `sni` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--sni))
- `tcp` (Block, Optional) [OneOf: tcp, tls_tcp, tls_tcp_auto_cert] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tcp))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `tls_tcp` (Block, Optional) BYOC TLS over TCP Choice. Choice for selecting TLS over TCP proxy with bring your own certificates (see [below for nested schema](#nestedblock--tls_tcp))
- `tls_tcp_auto_cert` (Block, Optional) TLS over TCP with Auto Certs Choice. Choice for selecting TLS over TCP proxy with automatic certificates (see [below for nested schema](#nestedblock--tls_tcp_auto_cert))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--active_service_policies"></a>
### Nested Schema for `active_service_policies`

Optional:

- `policies` (Block List) Policies. Service Policies is a sequential engine where policies (and rules within the policy) are evaluated one after the other. It's important to define the correct order (policies evaluated from top to bottom in the list) for service policies, to get the intended result. For each request, its characteristics are evaluated based on the match criteria in each service policy starting at the top. If there is a match in the current policy, then the policy takes effect, and no more policies are ... (see [below for nested schema](#nestedblock--active_service_policies--policies))

<a id="nestedblock--active_service_policies--policies"></a>
### Nested Schema for `active_service_policies.policies`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.



<a id="nestedblock--advertise_custom"></a>
### Nested Schema for `advertise_custom`

Optional:

- `advertise_where` (Block List) List of Sites to Advertise. Where should this load balancer be available (see [below for nested schema](#nestedblock--advertise_custom--advertise_where))

<a id="nestedblock--advertise_custom--advertise_where"></a>
### Nested Schema for `advertise_custom.advertise_where`

Optional:

- `advertise_on_public` (Block, Optional) Advertise Public. This defines a way to advertise a load balancer on public. If optional public_ip is provided, it will only be advertised on RE sites where that public_ip is available (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--advertise_on_public))
- `port` (Number) Listen Port. Port to Listen.
- `port_ranges` (String) Listen Port Ranges. A string containing a comma separated list of port ranges. Each port range consists of a single port or two ports separated by '-'.
- `site` (Block, Optional) Site. This defines a reference to a CE site along with network type and an optional ip address where a load balancer could be advertised (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--site))
- `use_default_port` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--use_default_port))
- `virtual_network` (Block, Optional) Virtual Network. Parameters to advertise on a given virtual network (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--virtual_network))
- `virtual_site` (Block, Optional) Virtual Site. This defines a reference to a customer site virtual site along with network type where a load balancer could be advertised (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--virtual_site))
- `virtual_site_with_vip` (Block, Optional) Virtual Site with Specified VIP. This defines a reference to a customer site virtual site along with network type and IP where a load balancer could be advertised (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--virtual_site_with_vip))
- `vk8s_service` (Block, Optional) vK8s Services on RE. This defines a reference to a RE site or virtual site where a load balancer could be advertised in the vK8s service network (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--vk8s_service))

<a id="nestedblock--advertise_custom--advertise_where--advertise_on_public"></a>
### Nested Schema for `advertise_custom.advertise_where.advertise_on_public`

Optional:

- `public_ip` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--advertise_on_public--public_ip))

<a id="nestedblock--advertise_custom--advertise_where--advertise_on_public--public_ip"></a>
### Nested Schema for `advertise_custom.advertise_where.advertise_on_public.public_ip`



<a id="nestedblock--advertise_custom--advertise_where--site"></a>
### Nested Schema for `advertise_custom.advertise_where.site`

Optional:

- `ip` (String) IP Address. Use given IP address as VIP on the site
- `network` (String) Site Network. This defines network types to be used on site All inside and outside networks. All inside and outside networks with internet VIP support. All inside networks. All outside networks. All outside networks with internet VIP support. vK8s service network. - SITE_NETWORK_IP_FABRIC: VER IP Fabric network for the site This Virtual network type is used for exposing virtual host on IP Fabric network on the VER site or for endpoint in IP Fabric network. Possible values are `SITE_NETWORK_INSIDE_AND_OUTSIDE`, `SITE_NETWORK_INSIDE`, `SITE_NETWORK_OUTSIDE`, `SITE_NETWORK_SERVICE`, `SITE_NETWORK_OUTSIDE_WITH_INTERNET_VIP`, `SITE_NETWORK_INSIDE_AND_OUTSIDE_WITH_INTERNET_VIP`, `SITE_NETWORK_IP_FABRIC`.
- `site` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--site--site))

<a id="nestedblock--advertise_custom--advertise_where--site--site"></a>
### Nested Schema for `advertise_custom.advertise_where.site.site`



<a id="nestedblock--advertise_custom--advertise_where--use_default_port"></a>
### Nested Schema for `advertise_custom.advertise_where.use_default_port`


<a id="nestedblock--advertise_custom--advertise_where--virtual_network"></a>
### Nested Schema for `advertise_custom.advertise_where.virtual_network`

Optional:

- `default_v6_vip` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--virtual_network--default_v6_vip))
- `default_vip` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--virtual_network--default_vip))
- `specific_v6_vip` (String) Specific V6 VIP. Use given IPV6 address as VIP on virtual Network
- `specific_vip` (String) Specific V4 VIP. Use given IPV4 address as VIP on virtual Network
- `virtual_network` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--virtual_network--virtual_network))

<a id="nestedblock--advertise_custom--advertise_where--virtual_network--default_v6_vip"></a>
### Nested Schema for `advertise_custom.advertise_where.virtual_network.default_v6_vip`


<a id="nestedblock--advertise_custom--advertise_where--virtual_network--default_vip"></a>
### Nested Schema for `advertise_custom.advertise_where.virtual_network.default_vip`


<a id="nestedblock--advertise_custom--advertise_where--virtual_network--virtual_network"></a>
### Nested Schema for `advertise_custom.advertise_where.virtual_network.virtual_network`



<a id="nestedblock--advertise_custom--advertise_where--virtual_site"></a>
### Nested Schema for `advertise_custom.advertise_where.virtual_site`

Optional:

- `network` (String) Site Network. This defines network types to be used on site All inside and outside networks. All inside and outside networks with internet VIP support. All inside networks. All outside networks. All outside networks with internet VIP support. vK8s service network. - SITE_NETWORK_IP_FABRIC: VER IP Fabric network for the site This Virtual network type is used for exposing virtual host on IP Fabric network on the VER site or for endpoint in IP Fabric network. Possible values are `SITE_NETWORK_INSIDE_AND_OUTSIDE`, `SITE_NETWORK_INSIDE`, `SITE_NETWORK_OUTSIDE`, `SITE_NETWORK_SERVICE`, `SITE_NETWORK_OUTSIDE_WITH_INTERNET_VIP`, `SITE_NETWORK_INSIDE_AND_OUTSIDE_WITH_INTERNET_VIP`, `SITE_NETWORK_IP_FABRIC`.
- `virtual_site` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--virtual_site--virtual_site))

<a id="nestedblock--advertise_custom--advertise_where--virtual_site--virtual_site"></a>
### Nested Schema for `advertise_custom.advertise_where.virtual_site.virtual_site`



<a id="nestedblock--advertise_custom--advertise_where--virtual_site_with_vip"></a>
### Nested Schema for `advertise_custom.advertise_where.virtual_site_with_vip`

Optional:

- `ip` (String) IP Address. Use given IP address as VIP on the site
- `network` (String) Site Network. This defines network types to be used on virtual-site with specified VIP All outside networks. All inside networks. Possible values are `SITE_NETWORK_SPECIFIED_VIP_OUTSIDE`, `SITE_NETWORK_SPECIFIED_VIP_INSIDE`.
- `virtual_site` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--virtual_site_with_vip--virtual_site))

<a id="nestedblock--advertise_custom--advertise_where--virtual_site_with_vip--virtual_site"></a>
### Nested Schema for `advertise_custom.advertise_where.virtual_site_with_vip.virtual_site`



<a id="nestedblock--advertise_custom--advertise_where--vk8s_service"></a>
### Nested Schema for `advertise_custom.advertise_where.vk8s_service`

Optional:

- `site` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--vk8s_service--site))
- `virtual_site` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--advertise_custom--advertise_where--vk8s_service--virtual_site))

<a id="nestedblock--advertise_custom--advertise_where--vk8s_service--site"></a>
### Nested Schema for `advertise_custom.advertise_where.vk8s_service.site`


<a id="nestedblock--advertise_custom--advertise_where--vk8s_service--virtual_site"></a>
### Nested Schema for `advertise_custom.advertise_where.vk8s_service.virtual_site`





<a id="nestedblock--advertise_on_public"></a>
### Nested Schema for `advertise_on_public`

Optional:

- `public_ip` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--advertise_on_public--public_ip))

<a id="nestedblock--advertise_on_public--public_ip"></a>
### Nested Schema for `advertise_on_public.public_ip`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.



<a id="nestedblock--advertise_on_public_default_vip"></a>
### Nested Schema for `advertise_on_public_default_vip`


<a id="nestedblock--default_lb_with_sni"></a>
### Nested Schema for `default_lb_with_sni`


<a id="nestedblock--do_not_advertise"></a>
### Nested Schema for `do_not_advertise`


<a id="nestedblock--do_not_retract_cluster"></a>
### Nested Schema for `do_not_retract_cluster`


<a id="nestedblock--hash_policy_choice_least_active"></a>
### Nested Schema for `hash_policy_choice_least_active`


<a id="nestedblock--hash_policy_choice_random"></a>
### Nested Schema for `hash_policy_choice_random`


<a id="nestedblock--hash_policy_choice_round_robin"></a>
### Nested Schema for `hash_policy_choice_round_robin`


<a id="nestedblock--hash_policy_choice_source_ip_stickiness"></a>
### Nested Schema for `hash_policy_choice_source_ip_stickiness`


<a id="nestedblock--no_service_policies"></a>
### Nested Schema for `no_service_policies`


<a id="nestedblock--no_sni"></a>
### Nested Schema for `no_sni`


<a id="nestedblock--origin_pools_weights"></a>
### Nested Schema for `origin_pools_weights`

Optional:

- `cluster` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--origin_pools_weights--cluster))
- `endpoint_subsets` (Block, Optional) Origin Servers Subsets. Upstream origin pool may be configured to divide its origin servers into subsets based on metadata attached to the origin servers. Routes may then specify the metadata that a endpoint must match in order to be selected by the load balancer For origin servers which are discovered in K8S or Consul cluster, the label of the service is merged with endpoint's labels. In case of Consul, the label is derived from the 'Tag' field. For labels that are common between configured ... (see [below for nested schema](#nestedblock--origin_pools_weights--endpoint_subsets))
- `pool` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--origin_pools_weights--pool))
- `priority` (Number) Priority. Priority of this origin pool, valid only with multiple origin pools. Value of 0 will make the pool as lowest priority origin pool Priority of 1 means highest priority and is considered active. When active origin pool is not available, lower priority origin pools are made active as per the increasing priority.
- `weight` (Number) Weight. Weight of this origin pool, valid only with multiple origin pool. Value of 0 will disable the pool

<a id="nestedblock--origin_pools_weights--cluster"></a>
### Nested Schema for `origin_pools_weights.cluster`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--origin_pools_weights--endpoint_subsets"></a>
### Nested Schema for `origin_pools_weights.endpoint_subsets`


<a id="nestedblock--origin_pools_weights--pool"></a>
### Nested Schema for `origin_pools_weights.pool`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.



<a id="nestedblock--retract_cluster"></a>
### Nested Schema for `retract_cluster`


<a id="nestedblock--service_policies_from_namespace"></a>
### Nested Schema for `service_policies_from_namespace`


<a id="nestedblock--sni"></a>
### Nested Schema for `sni`


<a id="nestedblock--tcp"></a>
### Nested Schema for `tcp`


<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).


<a id="nestedblock--tls_tcp"></a>
### Nested Schema for `tls_tcp`

Optional:

- `tls_cert_params` (Block, Optional) TLS Parameters. Select TLS Parameters and Certificates (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params))
- `tls_parameters` (Block, Optional) Inline TLS Parameters. Inline TLS parameters (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters))

<a id="nestedblock--tls_tcp--tls_cert_params"></a>
### Nested Schema for `tls_tcp.tls_cert_params`

Optional:

- `certificates` (Block List) Certificates. Select one or more certificates with any domain names. (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--certificates))
- `no_mtls` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--no_mtls))
- `tls_config` (Block, Optional) TLS Config. This defines various options to configure TLS configuration parameters (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--tls_config))
- `use_mtls` (Block, Optional) Clients TLS validation context. Validation context for downstream client TLS connections (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--use_mtls))

<a id="nestedblock--tls_tcp--tls_cert_params--certificates"></a>
### Nested Schema for `tls_tcp.tls_cert_params.certificates`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--tls_tcp--tls_cert_params--no_mtls"></a>
### Nested Schema for `tls_tcp.tls_cert_params.no_mtls`


<a id="nestedblock--tls_tcp--tls_cert_params--tls_config"></a>
### Nested Schema for `tls_tcp.tls_cert_params.tls_config`

Optional:

- `custom_security` (Block, Optional) Custom Ciphers. This defines TLS protocol config including min/max versions and allowed ciphers (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--tls_config--custom_security))
- `default_security` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--tls_config--default_security))
- `low_security` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--tls_config--low_security))
- `medium_security` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--tls_config--medium_security))

<a id="nestedblock--tls_tcp--tls_cert_params--tls_config--custom_security"></a>
### Nested Schema for `tls_tcp.tls_cert_params.tls_config.custom_security`


<a id="nestedblock--tls_tcp--tls_cert_params--tls_config--default_security"></a>
### Nested Schema for `tls_tcp.tls_cert_params.tls_config.default_security`


<a id="nestedblock--tls_tcp--tls_cert_params--tls_config--low_security"></a>
### Nested Schema for `tls_tcp.tls_cert_params.tls_config.low_security`


<a id="nestedblock--tls_tcp--tls_cert_params--tls_config--medium_security"></a>
### Nested Schema for `tls_tcp.tls_cert_params.tls_config.medium_security`



<a id="nestedblock--tls_tcp--tls_cert_params--use_mtls"></a>
### Nested Schema for `tls_tcp.tls_cert_params.use_mtls`

Optional:

- `client_certificate_optional` (Boolean) Client Certificate Optional. Client certificate is optional. If the client has provided a certificate, the load balancer will verify it. If certification verification fails, the connection will be terminated. If the client does not provide a certificate, the connection will be accepted.
- `crl` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--use_mtls--crl))
- `no_crl` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--use_mtls--no_crl))
- `trusted_ca` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--use_mtls--trusted_ca))
- `trusted_ca_url` (String) Inline Root CA Certificate (legacy). Upload a Root CA Certificate specifically for this Load Balancer
- `xfcc_disabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--use_mtls--xfcc_disabled))
- `xfcc_options` (Block, Optional) XFCC Header Elements. X-Forwarded-Client-Cert header elements to be added to requests (see [below for nested schema](#nestedblock--tls_tcp--tls_cert_params--use_mtls--xfcc_options))

<a id="nestedblock--tls_tcp--tls_cert_params--use_mtls--crl"></a>
### Nested Schema for `tls_tcp.tls_cert_params.use_mtls.crl`


<a id="nestedblock--tls_tcp--tls_cert_params--use_mtls--no_crl"></a>
### Nested Schema for `tls_tcp.tls_cert_params.use_mtls.no_crl`


<a id="nestedblock--tls_tcp--tls_cert_params--use_mtls--trusted_ca"></a>
### Nested Schema for `tls_tcp.tls_cert_params.use_mtls.trusted_ca`


<a id="nestedblock--tls_tcp--tls_cert_params--use_mtls--xfcc_disabled"></a>
### Nested Schema for `tls_tcp.tls_cert_params.use_mtls.xfcc_disabled`


<a id="nestedblock--tls_tcp--tls_cert_params--use_mtls--xfcc_options"></a>
### Nested Schema for `tls_tcp.tls_cert_params.use_mtls.xfcc_options`




<a id="nestedblock--tls_tcp--tls_parameters"></a>
### Nested Schema for `tls_tcp.tls_parameters`

Optional:

- `no_mtls` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--no_mtls))
- `tls_certificates` (Block List) TLS Certificates. Users can add one or more certificates that share the same set of domains. for example, domain.com and *.domain.com - but use different signature algorithms (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--tls_certificates))
- `tls_config` (Block, Optional) TLS Config. This defines various options to configure TLS configuration parameters (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--tls_config))
- `use_mtls` (Block, Optional) Clients TLS validation context. Validation context for downstream client TLS connections (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--use_mtls))

<a id="nestedblock--tls_tcp--tls_parameters--no_mtls"></a>
### Nested Schema for `tls_tcp.tls_parameters.no_mtls`


<a id="nestedblock--tls_tcp--tls_parameters--tls_certificates"></a>
### Nested Schema for `tls_tcp.tls_parameters.tls_certificates`

Optional:

- `certificate_url` (String) Certificate. TLS certificate. Certificate or certificate chain in PEM format including the PEM headers.
- `custom_hash_algorithms` (Block, Optional) Hash Algorithms. Specifies the hash algorithms to be used (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--tls_certificates--custom_hash_algorithms))
- `description` (String) Description. Description for the certificate
- `disable_ocsp_stapling` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--tls_certificates--disable_ocsp_stapling))
- `private_key` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--tls_certificates--private_key))
- `use_system_defaults` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--tls_certificates--use_system_defaults))

<a id="nestedblock--tls_tcp--tls_parameters--tls_certificates--custom_hash_algorithms"></a>
### Nested Schema for `tls_tcp.tls_parameters.tls_certificates.custom_hash_algorithms`


<a id="nestedblock--tls_tcp--tls_parameters--tls_certificates--disable_ocsp_stapling"></a>
### Nested Schema for `tls_tcp.tls_parameters.tls_certificates.disable_ocsp_stapling`


<a id="nestedblock--tls_tcp--tls_parameters--tls_certificates--private_key"></a>
### Nested Schema for `tls_tcp.tls_parameters.tls_certificates.private_key`


<a id="nestedblock--tls_tcp--tls_parameters--tls_certificates--use_system_defaults"></a>
### Nested Schema for `tls_tcp.tls_parameters.tls_certificates.use_system_defaults`



<a id="nestedblock--tls_tcp--tls_parameters--tls_config"></a>
### Nested Schema for `tls_tcp.tls_parameters.tls_config`

Optional:

- `custom_security` (Block, Optional) Custom Ciphers. This defines TLS protocol config including min/max versions and allowed ciphers (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--tls_config--custom_security))
- `default_security` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--tls_config--default_security))
- `low_security` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--tls_config--low_security))
- `medium_security` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--tls_config--medium_security))

<a id="nestedblock--tls_tcp--tls_parameters--tls_config--custom_security"></a>
### Nested Schema for `tls_tcp.tls_parameters.tls_config.custom_security`


<a id="nestedblock--tls_tcp--tls_parameters--tls_config--default_security"></a>
### Nested Schema for `tls_tcp.tls_parameters.tls_config.default_security`


<a id="nestedblock--tls_tcp--tls_parameters--tls_config--low_security"></a>
### Nested Schema for `tls_tcp.tls_parameters.tls_config.low_security`


<a id="nestedblock--tls_tcp--tls_parameters--tls_config--medium_security"></a>
### Nested Schema for `tls_tcp.tls_parameters.tls_config.medium_security`



<a id="nestedblock--tls_tcp--tls_parameters--use_mtls"></a>
### Nested Schema for `tls_tcp.tls_parameters.use_mtls`

Optional:

- `client_certificate_optional` (Boolean) Client Certificate Optional. Client certificate is optional. If the client has provided a certificate, the load balancer will verify it. If certification verification fails, the connection will be terminated. If the client does not provide a certificate, the connection will be accepted.
- `crl` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--use_mtls--crl))
- `no_crl` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--use_mtls--no_crl))
- `trusted_ca` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--use_mtls--trusted_ca))
- `trusted_ca_url` (String) Inline Root CA Certificate (legacy). Upload a Root CA Certificate specifically for this Load Balancer
- `xfcc_disabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--use_mtls--xfcc_disabled))
- `xfcc_options` (Block, Optional) XFCC Header Elements. X-Forwarded-Client-Cert header elements to be added to requests (see [below for nested schema](#nestedblock--tls_tcp--tls_parameters--use_mtls--xfcc_options))

<a id="nestedblock--tls_tcp--tls_parameters--use_mtls--crl"></a>
### Nested Schema for `tls_tcp.tls_parameters.use_mtls.crl`


<a id="nestedblock--tls_tcp--tls_parameters--use_mtls--no_crl"></a>
### Nested Schema for `tls_tcp.tls_parameters.use_mtls.no_crl`


<a id="nestedblock--tls_tcp--tls_parameters--use_mtls--trusted_ca"></a>
### Nested Schema for `tls_tcp.tls_parameters.use_mtls.trusted_ca`


<a id="nestedblock--tls_tcp--tls_parameters--use_mtls--xfcc_disabled"></a>
### Nested Schema for `tls_tcp.tls_parameters.use_mtls.xfcc_disabled`


<a id="nestedblock--tls_tcp--tls_parameters--use_mtls--xfcc_options"></a>
### Nested Schema for `tls_tcp.tls_parameters.use_mtls.xfcc_options`





<a id="nestedblock--tls_tcp_auto_cert"></a>
### Nested Schema for `tls_tcp_auto_cert`

Optional:

- `no_mtls` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp_auto_cert--no_mtls))
- `tls_config` (Block, Optional) TLS Config. This defines various options to configure TLS configuration parameters (see [below for nested schema](#nestedblock--tls_tcp_auto_cert--tls_config))
- `use_mtls` (Block, Optional) Clients TLS validation context. Validation context for downstream client TLS connections (see [below for nested schema](#nestedblock--tls_tcp_auto_cert--use_mtls))

<a id="nestedblock--tls_tcp_auto_cert--no_mtls"></a>
### Nested Schema for `tls_tcp_auto_cert.no_mtls`


<a id="nestedblock--tls_tcp_auto_cert--tls_config"></a>
### Nested Schema for `tls_tcp_auto_cert.tls_config`

Optional:

- `custom_security` (Block, Optional) Custom Ciphers. This defines TLS protocol config including min/max versions and allowed ciphers (see [below for nested schema](#nestedblock--tls_tcp_auto_cert--tls_config--custom_security))
- `default_security` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp_auto_cert--tls_config--default_security))
- `low_security` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp_auto_cert--tls_config--low_security))
- `medium_security` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp_auto_cert--tls_config--medium_security))

<a id="nestedblock--tls_tcp_auto_cert--tls_config--custom_security"></a>
### Nested Schema for `tls_tcp_auto_cert.tls_config.custom_security`

Optional:

- `cipher_suites` (List of String) Cipher Suites. The TLS listener will only support the specified cipher list.
- `max_version` (String) TLS Protocol. TlsProtocol is enumeration of supported TLS versions F5 Distributed Cloud will choose the optimal TLS version. Possible values are `TLS_AUTO`, `TLSv1_0`, `TLSv1_1`, `TLSv1_2`, `TLSv1_3`.
- `min_version` (String) TLS Protocol. TlsProtocol is enumeration of supported TLS versions F5 Distributed Cloud will choose the optimal TLS version. Possible values are `TLS_AUTO`, `TLSv1_0`, `TLSv1_1`, `TLSv1_2`, `TLSv1_3`.


<a id="nestedblock--tls_tcp_auto_cert--tls_config--default_security"></a>
### Nested Schema for `tls_tcp_auto_cert.tls_config.default_security`


<a id="nestedblock--tls_tcp_auto_cert--tls_config--low_security"></a>
### Nested Schema for `tls_tcp_auto_cert.tls_config.low_security`


<a id="nestedblock--tls_tcp_auto_cert--tls_config--medium_security"></a>
### Nested Schema for `tls_tcp_auto_cert.tls_config.medium_security`



<a id="nestedblock--tls_tcp_auto_cert--use_mtls"></a>
### Nested Schema for `tls_tcp_auto_cert.use_mtls`

Optional:

- `client_certificate_optional` (Boolean) Client Certificate Optional. Client certificate is optional. If the client has provided a certificate, the load balancer will verify it. If certification verification fails, the connection will be terminated. If the client does not provide a certificate, the connection will be accepted.
- `crl` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--tls_tcp_auto_cert--use_mtls--crl))
- `no_crl` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp_auto_cert--use_mtls--no_crl))
- `trusted_ca` (Block, Optional) Object reference. This type establishes a direct reference from one object(the referrer) to another(the referred). Such a reference is in form of tenant/namespace/name (see [below for nested schema](#nestedblock--tls_tcp_auto_cert--use_mtls--trusted_ca))
- `trusted_ca_url` (String) Inline Root CA Certificate (legacy). Upload a Root CA Certificate specifically for this Load Balancer
- `xfcc_disabled` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--tls_tcp_auto_cert--use_mtls--xfcc_disabled))
- `xfcc_options` (Block, Optional) XFCC Header Elements. X-Forwarded-Client-Cert header elements to be added to requests (see [below for nested schema](#nestedblock--tls_tcp_auto_cert--use_mtls--xfcc_options))

<a id="nestedblock--tls_tcp_auto_cert--use_mtls--crl"></a>
### Nested Schema for `tls_tcp_auto_cert.use_mtls.crl`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--tls_tcp_auto_cert--use_mtls--no_crl"></a>
### Nested Schema for `tls_tcp_auto_cert.use_mtls.no_crl`


<a id="nestedblock--tls_tcp_auto_cert--use_mtls--trusted_ca"></a>
### Nested Schema for `tls_tcp_auto_cert.use_mtls.trusted_ca`

Optional:

- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.


<a id="nestedblock--tls_tcp_auto_cert--use_mtls--xfcc_disabled"></a>
### Nested Schema for `tls_tcp_auto_cert.use_mtls.xfcc_disabled`


<a id="nestedblock--tls_tcp_auto_cert--use_mtls--xfcc_options"></a>
### Nested Schema for `tls_tcp_auto_cert.use_mtls.xfcc_options`

Optional:

- `xfcc_header_elements` (List of String) XFCC Header Elements. X-Forwarded-Client-Cert header elements to be added to requests

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_tcp_loadbalancer.example system/example
```
