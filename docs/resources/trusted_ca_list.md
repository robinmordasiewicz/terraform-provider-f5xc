---
page_title: "f5xc_trusted_ca_list Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Shape of the Root CA Certificate specification
---

# f5xc_trusted_ca_list (Resource)

Shape of the Root CA Certificate specification

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Trusted Ca List Resource Example
# Shape of the Root CA Certificate specification

# Basic Trusted Ca List configuration
resource "f5xc_trusted_ca_list" "example" {
  name      = "my-trusted-ca-list"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Trusted CA List configuration
  trusted_ca_url = "string:///LS0tLS1CRUdJTi..."
}
```


<!-- schema generated by tfplugindocs -->
## Argument Reference

The following arguments are required:

`name` - (Required) Name of the TrustedCaList. Must be unique within the namespace (`String`).

`namespace` - (Required) Namespace where the TrustedCaList will be created (`String`).


The following arguments are optional:

`annotations` - (Optional) Annotations to apply to this resource (`Map`).

`labels` - (Optional) Labels to apply to this resource (`Map`).

`trusted_ca_url` - (Optional) Root CA Certificate. Trusted CA certificates for validating certificates ves.io.schema.rules.string.max_len: 512000 ves.io.schema.rules.string.truststore_url: true (`String`).


### Attributes Reference

In addition to all arguments above, the following attributes are exported:

`id` - (Optional) Unique identifier for the resource (`String`).


## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_trusted_ca_list.example system/example
```

