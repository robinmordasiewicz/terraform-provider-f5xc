---
page_title: "f5xc_tunnel Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages tunnel in a given namespace. If one already exist it will give a error. in F5 Distributed Cloud.
---

# f5xc_tunnel (Resource)

Manages tunnel in a given namespace. If one already exist it will give a error. in F5 Distributed Cloud.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Tunnel Resource Example
# Manages tunnel in a given namespace. If one already exist it will give a error. in F5 Distributed Cloud.

# Basic Tunnel configuration
resource "f5xc_tunnel" "example" {
  name      = "example-tunnel"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Tunnel configuration
  remote_ip_address = "203.0.113.1"
  local_ip_address  = "203.0.113.2"

  # IPsec tunnel
  ipsec {
    psk = "pre-shared-key-here"
    ike_params {
      ike_version = "IKE_V2"
    }
  }

  # Site reference
  site {
    name      = "example-site"
    namespace = "system"
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the Tunnel. Must be unique within the namespace.
- `namespace` (String) Namespace where the Tunnel will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `labels` (Map of String) Labels to apply to this resource.
- `local_ip` (Block, Optional) Local IP Address Selector. Defines the options to select local ip address and virtual network for tunnel object Options available are - 1. Local Interface - Network Interface from which IP address and network will be selected 2. IP Address - IP address and network can be configured explicitly (see [below for nested schema](#nestedblock--local_ip))
- `params` (Block, Optional) Tunnel Parameters. Tunnel configuration parameters for supported encapsulation 1. IPSec is supported with PSK for which PSK can be configured (see [below for nested schema](#nestedblock--params))
- `remote_ip` (Block, Optional) Remote IP Address Selector. Defines the options to select remote ip address for tunnel object Options available are - 1. IP Address - Specifies the remote IP to which tunnel has to be connected 2. Remote endpoint - Is a map of IP address on per ver node basis (see [below for nested schema](#nestedblock--remote_ip))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))
- `tunnel_type` (String) Tunnel Type. Supported tunnel types are IPSec IPSEC tunnel type with PSK GRE tunnel type. Possible values are `IPSEC_PSK`, `GRE`.

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--local_ip"></a>
### Nested Schema for `local_ip`

Optional:

- `intf` (Block, Optional) Interface Type. Provides the local interface to pick up source IP and network for transporting encapsulated packet (see [below for nested schema](#nestedblock--local_ip--intf))
- `ip_address` (Block, Optional) Local IP Address Type. Provides the configuration to pick up source IP and network for transporting encapsulated packet (see [below for nested schema](#nestedblock--local_ip--ip_address))

<a id="nestedblock--local_ip--intf"></a>
### Nested Schema for `local_ip.intf`

Optional:

- `local_intf` (Block List) Local Interface. Local interface to be used for filling in source information of IP and network for transport (see [below for nested schema](#nestedblock--local_ip--intf--local_intf))

<a id="nestedblock--local_ip--intf--local_intf"></a>
### Nested Schema for `local_ip.intf.local_intf`

Optional:

- `kind` (String) Kind. When a configuration object(e.g. virtual_host) refers to another(e.g route) then kind will hold the referred object's kind (e.g. 'route')
- `name` (String) Name. When a configuration object(e.g. virtual_host) refers to another(e.g route) then name will hold the referred object's(e.g. route's) name.
- `namespace` (String) Namespace. When a configuration object(e.g. virtual_host) refers to another(e.g route) then namespace will hold the referred object's(e.g. route's) namespace.
- `tenant` (String) Tenant. When a configuration object(e.g. virtual_host) refers to another(e.g route) then tenant will hold the referred object's(e.g. route's) tenant.
- `uid` (String) UID. When a configuration object(e.g. virtual_host) refers to another(e.g route) then uid will hold the referred object's(e.g. route's) uid.



<a id="nestedblock--local_ip--ip_address"></a>
### Nested Schema for `local_ip.ip_address`

Optional:

- `auto` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--local_ip--ip_address--auto))
- `ip_address` (Block, Optional) IP Address. IP Address used to specify an IPv4 or IPv6 address (see [below for nested schema](#nestedblock--local_ip--ip_address--ip_address))
- `virtual_network_type` (Block, Optional) Virtual Network Type. Different types of virtual networks understood by the system (see [below for nested schema](#nestedblock--local_ip--ip_address--virtual_network_type))

<a id="nestedblock--local_ip--ip_address--auto"></a>
### Nested Schema for `local_ip.ip_address.auto`


<a id="nestedblock--local_ip--ip_address--ip_address"></a>
### Nested Schema for `local_ip.ip_address.ip_address`

Optional:

- `ipv4` (Block, Optional) IPv4 Address. IPv4 Address in dot-decimal notation (see [below for nested schema](#nestedblock--local_ip--ip_address--ip_address--ipv4))
- `ipv6` (Block, Optional) IPv6 Address. IPv6 Address specified as hexadecimal numbers separated by ':' (see [below for nested schema](#nestedblock--local_ip--ip_address--ip_address--ipv6))

<a id="nestedblock--local_ip--ip_address--ip_address--ipv4"></a>
### Nested Schema for `local_ip.ip_address.ip_address.ipv4`


<a id="nestedblock--local_ip--ip_address--ip_address--ipv6"></a>
### Nested Schema for `local_ip.ip_address.ip_address.ipv6`



<a id="nestedblock--local_ip--ip_address--virtual_network_type"></a>
### Nested Schema for `local_ip.ip_address.virtual_network_type`

Optional:

- `public` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--local_ip--ip_address--virtual_network_type--public))
- `site_local` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--local_ip--ip_address--virtual_network_type--site_local))
- `site_local_inside` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--local_ip--ip_address--virtual_network_type--site_local_inside))

<a id="nestedblock--local_ip--ip_address--virtual_network_type--public"></a>
### Nested Schema for `local_ip.ip_address.virtual_network_type.public`


<a id="nestedblock--local_ip--ip_address--virtual_network_type--site_local"></a>
### Nested Schema for `local_ip.ip_address.virtual_network_type.site_local`


<a id="nestedblock--local_ip--ip_address--virtual_network_type--site_local_inside"></a>
### Nested Schema for `local_ip.ip_address.virtual_network_type.site_local_inside`





<a id="nestedblock--params"></a>
### Nested Schema for `params`

Optional:

- `ipsec` (Block, Optional) IPSEC tunnel parameters. Configuration for IPSec encapsulation are: 1. PSK - pre shared key to be used by IKE (see [below for nested schema](#nestedblock--params--ipsec))

<a id="nestedblock--params--ipsec"></a>
### Nested Schema for `params.ipsec`

Optional:

- `ipsec_psk` (Block, Optional) Secret. SecretType is used in an object to indicate a sensitive/confidential field (see [below for nested schema](#nestedblock--params--ipsec--ipsec_psk))

<a id="nestedblock--params--ipsec--ipsec_psk"></a>
### Nested Schema for `params.ipsec.ipsec_psk`

Optional:

- `blindfold_secret_info` (Block, Optional) Blindfold Secret. BlindfoldSecretInfoType specifies information about the Secret managed by F5XC Secret Management (see [below for nested schema](#nestedblock--params--ipsec--ipsec_psk--blindfold_secret_info))
- `clear_secret_info` (Block, Optional) In-Clear Secret. ClearSecretInfoType specifies information about the Secret that is not encrypted. (see [below for nested schema](#nestedblock--params--ipsec--ipsec_psk--clear_secret_info))

<a id="nestedblock--params--ipsec--ipsec_psk--blindfold_secret_info"></a>
### Nested Schema for `params.ipsec.ipsec_psk.blindfold_secret_info`


<a id="nestedblock--params--ipsec--ipsec_psk--clear_secret_info"></a>
### Nested Schema for `params.ipsec.ipsec_psk.clear_secret_info`





<a id="nestedblock--remote_ip"></a>
### Nested Schema for `remote_ip`

Optional:

- `endpoints` (Block, Optional) Remote Endpoint Type. Provides a map of ver node name to remote node attributes Ver node should use these attributes to configure as remote tunnel (see [below for nested schema](#nestedblock--remote_ip--endpoints))
- `ip` (Block, Optional) IP Address. IP Address used to specify an IPv4 or IPv6 address (see [below for nested schema](#nestedblock--remote_ip--ip))

<a id="nestedblock--remote_ip--endpoints"></a>
### Nested Schema for `remote_ip.endpoints`

Optional:

- `endpoints` (Block, Optional) Remote Endpoints. Map of remote attributes to which tunnel will be established on per site node basis Every node can have a different attributes and IP address to connect to Key is ver node name and value is Remote node attributes (see [below for nested schema](#nestedblock--remote_ip--endpoints--endpoints))

<a id="nestedblock--remote_ip--endpoints--endpoints"></a>
### Nested Schema for `remote_ip.endpoints.endpoints`



<a id="nestedblock--remote_ip--ip"></a>
### Nested Schema for `remote_ip.ip`

Optional:

- `ipv4` (Block, Optional) IPv4 Address. IPv4 Address in dot-decimal notation (see [below for nested schema](#nestedblock--remote_ip--ip--ipv4))
- `ipv6` (Block, Optional) IPv6 Address. IPv6 Address specified as hexadecimal numbers separated by ':' (see [below for nested schema](#nestedblock--remote_ip--ip--ipv6))

<a id="nestedblock--remote_ip--ip--ipv4"></a>
### Nested Schema for `remote_ip.ip.ipv4`

Optional:

- `addr` (String) IPv4 Address. IPv4 Address in string form with dot-decimal notation


<a id="nestedblock--remote_ip--ip--ipv6"></a>
### Nested Schema for `remote_ip.ip.ipv6`

Optional:

- `addr` (String) IPv6 Address. IPv6 Address in form of string. IPv6 address must be specified as hexadecimal numbers separated by ':' The address can be compacted by suppressing zeros e.g. '2001:db8:0:0:0:0:2:1' becomes '2001:db8::2:1' or '2001:db8:0:0:0:2:0:0' becomes '2001:db8::2::'




<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_tunnel.example system/example
```
