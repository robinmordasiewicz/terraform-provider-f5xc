---
page_title: "f5xc_virtual_network Resource - terraform-provider-f5xc"
subcategory: ""
description: |-
  Manages virtual network in given namespace in F5 Distributed Cloud.
---

# f5xc_virtual_network (Resource)

Manages virtual network in given namespace in F5 Distributed Cloud.

~> **Note** For more information about this resource, please refer to the [F5 XC API Documentation](https://docs.cloud.f5.com/docs/api/).

## Example Usage

```terraform
# Virtual Network Resource Example
# Manages virtual network in given namespace in F5 Distributed Cloud.

# Basic Virtual Network configuration
resource "f5xc_virtual_network" "example" {
  name      = "example-virtual-network"
  namespace = "system"

  labels = {
    environment = "production"
    managed_by  = "terraform"
  }

  annotations = {
    "owner" = "platform-team"
  }

  # Virtual Network configuration
  site_local_network {}

  # DHCP range for the network
  srv6_network {
    enterprise_network {
      srv6_network_ns_params {
        namespace = "system"
      }
    }
  }
}
```


<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Name of the VirtualNetwork. Must be unique within the namespace.
- `namespace` (String) Namespace where the VirtualNetwork will be created.

### Optional

- `annotations` (Map of String) Annotations to apply to this resource.
- `global_network` (Block, Optional) [OneOf: global_network, legacy_type, site_local_inside_network, site_local_network] Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--global_network))
- `labels` (Map of String) Labels to apply to this resource.
- `legacy_type` (String) Virtual Network Type. Different types of virtual networks understood by the system Virtual-network of type VIRTUAL_NETWORK_SITE_LOCAL provides connectivity to public (outside) network. This is an insecure network and is connected to public internet via NAT Gateways/firwalls Virtual-network of this type is local to every site. Two virtual networks of this type on different sites are neither related nor connected. Constraints: There can be atmost one virtual network of this type in a given site... Possible values include `VIRTUAL_NETWORK_SITE_LOCAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE`, `VIRTUAL_NETWORK_PER_SITE`, `VIRTUAL_NETWORK_PUBLIC`, `VIRTUAL_NETWORK_GLOBAL`, `VIRTUAL_NETWORK_SITE_SERVICE`, `VIRTUAL_NETWORK_VER_INTERNAL`, `VIRTUAL_NETWORK_SITE_LOCAL_INSIDE_OUTSIDE`, `VIRTUAL_NETWORK_IP_AUTO`, `VIRTUAL_NETWORK_VOLTADN_PRIVATE_NETWORK`, and others.
- `site_local_inside_network` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--site_local_inside_network))
- `site_local_network` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--site_local_network))
- `static_routes` (Block List) Static Routes. List of static routes on the virtual network (see [below for nested schema](#nestedblock--static_routes))
- `timeouts` (Block, Optional) (see [below for nested schema](#nestedblock--timeouts))

### Read-Only

- `id` (String) Unique identifier for the resource.

<a id="nestedblock--global_network"></a>
### Nested Schema for `global_network`


<a id="nestedblock--site_local_inside_network"></a>
### Nested Schema for `site_local_inside_network`


<a id="nestedblock--site_local_network"></a>
### Nested Schema for `site_local_network`


<a id="nestedblock--static_routes"></a>
### Nested Schema for `static_routes`

Optional:

- `attrs` (List of String) Attributes. List of attributes that control forwarding, dynamic routing and control plane (host) reachability
- `default_gateway` (Block, Optional) Empty. This can be used for messages where no values are needed (see [below for nested schema](#nestedblock--static_routes--default_gateway))
- `ip_address` (String) IP Address. Traffic matching the ip prefixes is sent to this IP Address
- `ip_prefixes` (List of String) IP Prefixes. List of route prefixes that have common next hop and attributes
- `node_interface` (Block, Optional) NodeInterfaceType. On multinode site, this type holds the information about per node interfaces (see [below for nested schema](#nestedblock--static_routes--node_interface))

<a id="nestedblock--static_routes--default_gateway"></a>
### Nested Schema for `static_routes.default_gateway`


<a id="nestedblock--static_routes--node_interface"></a>
### Nested Schema for `static_routes.node_interface`

Optional:

- `list` (Block List) Node Interface Info. On a multinode site, this list holds the nodes and corresponding networking_interface (see [below for nested schema](#nestedblock--static_routes--node_interface--list))

<a id="nestedblock--static_routes--node_interface--list"></a>
### Nested Schema for `static_routes.node_interface.list`

Optional:

- `interface` (Block List) Interface. Interface reference on this node (see [below for nested schema](#nestedblock--static_routes--node_interface--list--interface))
- `node` (String) Node. Node name on this site

<a id="nestedblock--static_routes--node_interface--list--interface"></a>
### Nested Schema for `static_routes.node_interface.list.interface`





<a id="nestedblock--timeouts"></a>
### Nested Schema for `timeouts`

Optional:

- `create` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
- `delete` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
- `read` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Read operations occur during any refresh or planning operation when refresh is enabled.
- `update` (String) A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).

## Import

Import is supported using the following syntax:

```shell
# Import using namespace/name format
terraform import f5xc_virtual_network.example system/example
```
